root = true

[*]
charset = utf-8
# The tool parses the markdown it writes, anchored on line ends. A CRLF checkout would break it.
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true
