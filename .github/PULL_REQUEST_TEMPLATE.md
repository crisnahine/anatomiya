**What changed**

**Why**

**How you checked it**

If this changes what gets written, paste the scan output before and after.

- [ ] `node --test 'test/**/*.test.mjs'` passes
- [ ] `npm run check:docs` passes, which CI also runs
- [ ] DECISIONS.md updated if this changes a decision or its status
