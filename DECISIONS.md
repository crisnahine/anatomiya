# anatomiya build contract

Every row is a finding that survived review or a probe that overturned an assumption, reduced to the
decision it forces on the code. This replaces the v1/v2/v3 specs as the thing to build against; those
are history. Status is what the code does **today**, not what it should do.

Sources: five-lens verification of v3 (26 blocking findings), six empirical probes, a ten-repo
public measurement, a 4,616-comment classification, and a 237-entry glossary re-audit.

---

## A. Delivery

| # | Decision | Why | Status |
|---|---|---|---|
| A1 | Write to `.claude/rules/anatomiya-*.md` | Measured: reaches subagents, negation works, central so one exclude line hides it, and a writer bug cannot destroy a hand-written `CLAUDE.md` | **done** `rules.mjs` (`isGeneratedName`), `write.mjs`. The last clause was a property of the call sites rather than of the code: every planned name is now checked to be a bare prefixed `.md` before anything is created, so an area id carrying a separator refuses the whole write instead of resolving somewhere else. It holds because the id is a digest today, and it would hold if it stopped being one. One write escaped it between 0.2.4 and 0.2.6, the re-delivery hook installed into `.claude/settings.local.json`, and the correction in A24 puts it back: a scan writes nothing outside `.claude/rules/` and `.claude/anatomiya/` again |
| A2 | Never end a `paths` glob in a bare `/**` | Measured: the matcher strips `/**`, so `["app/**","!app/vendor/**"]` becomes `["app","!app/vendor"]` and the exclusion silently fails | **done** `areas.mjs` |
| A3 | Ownership = filename prefix AND `generator: anatomiya` frontmatter AND known to `facts.json`. All three, or leave it alone | v3 said "never delete a file lacking the key" and "remove any unknown `anatomiya-*`" in adjacent sentences | **done** `rules.mjs` (`auditRules`, `knownNames`, `isOwned`, and `readHead` exported for the read side), `write.mjs`, `hook.mjs` (`ownMap`, `readOwned`). The third fact was the one nobody read: the writer removed on prefix plus key plus absence from this scan, which deletes an older build's output and anything a store wiped between runs. The map is read before the new one replaces it, and no map at all makes nothing removable rather than everything. A file carrying two of the three is reported and left where it is. The rule is about removal: a hand-written file that took a name this scan writes is replaced, because a generated name is this tool's by construction and refusing the whole map over one would let a repository stop the map being built at all. That is the only case, it is reported on the summary, and the overview does not then call it a file this tool did not write. The rule reaches the read side too, arriving late and from a report: the hook echoed whatever sat at the overview's path, so a file this tool never wrote went into the model's context on every prompt and every tool call, and the only reason it had not bitten anybody is that the path is a name nobody else types. It asks the frontmatter now, which is the one of the three facts a hook can check: the prefix is the path it just built, and `facts.json` belongs to a scan rather than to a session. So the read side is two of three by construction, and the gap is worth stating rather than hiding, since a file carrying our key is a file anyone can write. Whose it is also decides whether the walk stops: a hand-written file in a subdirectory used to silence the repository's own map for every session under it, and the walk now goes past it to the real one above. Second-order, and the reason the read goes through `readHead` rather than a plain read: the path is whatever is sitting there, and this runs on every tool call. A named pipe there never returned at all, which is a session that never returns, measured; a five-megabyte file carrying our key was read whole and echoed whole, 100 MB resident and every byte of it into the context window. `readHead` opens non-blocking, types the shape before reading a byte, and stops at `HEAD_BYTES`, the size this repository had already decided a rule file may be. The frontmatter test itself was reading across the closing fence, so somebody else's block with our key further down came back as ours, which is the direction that deletes; the lines between the fences are now any line that is not itself a fence |
| A4 | The overview says how many files we generated and names the other `.md` files in `.claude/rules/`; the check and the scan name them too. Every one of the three listings is bounded, at the budget its surface can afford (F17) | `.claude/rules/` is a *repository* directory. A cloned repo can ship a rule file with no `paths` key that loads unconditionally from the moment of clone, in our house style, forever | **done** `render.mjs` `check.mjs` `write.mjs` `summary.mjs`. This row asked for the enumeration on the other side: every file we generated, so any other one is identifiable by absence. Measured against A9, that is 4 KB of hex filenames added back to the file that loads on every turn, on the repositories where nothing is wrong. Naming the others instead is the same guarantee at zero cost when clean, it names the file a reader would otherwise have to go and find, and it reaches the reader in the file that is loading beside it. The overview names up to six and counts the rest, because past a handful the fact is that the directory is somebody else's and the names stop being the useful part; the check and the scan are read once rather than every turn, so they name twenty. The overview names only the files this tool did not write and gives its own leftovers as a count, since the reader's move is the same whichever of those it is and that section is paid for on every turn. Both surfaces now also split "somebody else wrote this" from "we wrote this and no map names it": the check reported the first only, and by prefix, which called a hand-written `anatomiya-notes.md` ours |
| A5 | Overview must be byte-stable across scans with no source change | The token economics only work on a cached read. No timestamps, no counts that move per commit, no filesystem-order dependence | **done** `render.mjs`, pinned end to end by `cli.test.mjs` over two real scans and by `corpus.test.mjs` for the ordering. The last clause held by accident: `git ls-files` answers sorted, so nothing established that the map does not care, and `discover`'s own sort was the only thing standing between a reordered corpus and a rewritten always-loaded file |
| A6 | Each generated file stays under ~40 lines | Measured: a rewrite does not re-attach inside one context window (A17 qualifies "session" here, and the bound is unaffected: the window is the unit a long stretch of work happens in); the change notice truncates head and tail, so a mid-file edit reaches the model in neither copy | **done** `render.mjs` (`MAX_LINES`, `fit`). It was a hope about how many dimensions an area holds, and thirteen stated JS claims already clear it. An area file drops its counts before its directives and says how many of each kind did not fit; the overview's listing takes whatever the rest of the file leaves. The `paths` list is exempt, because a glob dropped to save a line mis-delivers the whole file, so the bound is measured over what a reader reads. Measured across 35 repositories: 17 hold an area whose cover alone runs past forty lines, the worst at 170 patterns on canvas-lms, and every one of those bodies came to ten lines or fewer. What overflows is the routing, and it sits above the directives rather than around them, so the reading stays in the tail, which is the half a change notice keeps. An area needing 170 negations to be spelled exactly is a question for A10 rather than for this row |
| A7 | The plugin never opens its own rules files with the Read tool; use `cat` | Measured: reading a context file permanently suppresses its automatic injection for that path for the rest of the process | **done** `commands/*.md`, pinned by `cli.test.mjs`. `pin.md` runs a scan in its own step three and carried neither this rule nor A8's |
| A8 | Print one line after a scan: a running session holds the old map, restart to pick up the new one | Measured, same cause as A6. A17 found the one path that picks the new map up without a restart, and it does not change the line: a compaction re-reads every instruction file from disk, so a session that compacts after a re-scan is holding the new counts. Nothing can make a session compact, and telling a reader to wait for one is telling them to wait | **done** `summary.mjs` (`RESTART`, printed by both `scanLines` and `pinLines`), `plugins/anatomiya/commands/scan.md`, `plugins/anatomiya/commands/pin.md`. The pin is where a human is sent off to re-scan, so it says it too |
| A9 | The overview names only the areas that state something, at every repository size, and gives the rest as a count | Measured on a 5,489-file Rails repo: 151 areas, 143 of them stating nothing, 89% of an always-loaded file spent on a directory name and a file count that `ls` already gives. Rebuilt at that shape the listing is 8,576 bytes against 1,030. Nothing becomes unreachable: a counts-only area keeps its own path-scoped file, and the `## Areas (n)` heading still counts them all | **done** `render.mjs` |
| A10 | An area's `paths` matches the files its counts were taken over, and no others. Sharing a root with a deeper area means one glob per directory it holds files in, or one recursive glob and a negation per foreign subtree, whichever is shorter | Measured on a 5,495-file Rails repo: 37 of 156 areas over-reached, `app` by 2,425 files. `app/workers/workers` states a directive at 124 of 125 and its glob delivered it to `app/workers/workers/google`, which measured the same dimension at 14 of 22 and was suppressed by the ratio gate. The gate decides where a claim may be stated and the delivery channel walked around it | **done** `areas.mjs` (`assignGlobs`). A directory whose own name is glob syntax, which a repository may legally have, cannot root an area and cannot be named by a cover: spelled into a pattern it stops meaning that directory, reaching the whole tree in one direction and nothing at all in the other. Its files fold into an ancestor that can be spelled, whose recursive tail still reaches them. Where neither exact shape can be spelled, the cover falls back to one recursive pattern from the area root, which describes more of the tree than the exact shapes do and is still bounded by the area |
| A11 | A scan of a repository with no tracked source says how many source files are untracked, counted by the same filters `collect` applies | The corpus is tracked files, so a repository whose first commit has not landed got an empty map, exit 0, and an overview stating that 0 files were uncovered while five sat on disk. The count is shown beside "commit them and scan again", so a file a later scan would drop must not be in it | **done** `corpus.mjs` (`countUntrackedSource`, `classify`), `scan.mjs`, `render.mjs` (`untrackedSentence`), `summary.mjs` (`scanLines`) |
| A12 | An area's `paths` carries one pattern per bare filename it holds, beside the extension glob | A brace of extensions cannot spell `Rakefile`, so admitting a file with no extension reopens A2 from the other side: counted and never delivered. Emitted per cover entry, so a negation cuts the bare name out of a foreign subtree exactly as it cuts the extension glob, and only for names the area actually holds | **done** `areas.mjs`. The pattern stays in its two halves, `dir` and `tail`, all the way to the renderer, so the joined form is never taken apart again by a second reading of the grammar |
| A13 | A run whose parser never answered for a whole language writes nothing and removes nothing | Measured: `env -i PATH=/usr/bin:/bin` on a 200-file Rails repository charged every file as a crash, dropped every area, and deleted three correct area files in the same run that reported it could not read them. Suppressing only the removals leaves the overview claiming zero areas beside area files that still load, so `facts.json` is suppressed with the rest. The condition is a crash on every file, not an absence of readable ones: a syntax error means the parser answered | **done** `write.mjs` `scan.mjs` `parse.mjs` (`classify`) |
| A14 | The uncovered count names its two causes apart | Measured: six `.js` files holding JSX in one directory were reported as "too few per directory" at a floor of three, and a scan with no ruby printed that line beside "202 files could not be parsed". Discovery finding nowhere to put a file and an area counting nothing are different facts with different fixes; only the first was ever what the sentence said | **done** `render.mjs` `write.mjs` |
| A15 | A stated side the model already writes unprompted renders as a counts line, never as a directive; the check enforces it unchanged | Measured elsewhere: context files pay only for non-standard practices (arXiv:2602.11988), and this tool's own A/B (G5) found no effect on a claim the model writes anyway. The check keeps full severity because compliance decays within a session (about 5.6% lower odds per generated function, arXiv:2605.10039), so a default is still worth catching once the model drifts off it. Which side a model writes is a committed table, `plugins/anatomiya/lib/model-defaults.json`, with provenance per entry; `none` and an absent key fail open, so an unmeasured row keeps stating. The table is written by `scripts/measure-defaults.mjs`, which parses the model's own output through the scan's predicates, so the table's number and the map's number are one number. A side or class is declared the default at 0.8 of at least 20 sites, the same shape as the ratio gate's evidence floor: below either bound the honest answer is `none`, which fails open. Batches of one model accumulate rather than replace, because 19 sites and 10 sites are 29. The trials run through the same headless CLI the A/B uses, deliberately: the maps are read by agents inside that harness, so the default that decides a line's worth is the agent as deployed, harness prompt included, not the raw model. That is how doc_comment_style measured counter (1 of 40 exports commented) where the literature predicted comment-heavy, and the measurement wins. The six prompts also name the files the model must create, so the filename tally measures the prompt author; the file_naming_case class stays unmeasured until the tasks stop naming files. A learned row holds one class per key across languages, so a future batch dominated by one language could declare that language's class; split the tasks per language before believing a class the table did not hold before. A table change ships as a plugin version, so it moves nothing until a human re-scans, and that re-scan can flip stated lines to counts and un-name areas at once: a larger rendered diff than a registry addition produces. Six shared scenario prompts feed every dimension at once; a construct they never elicit stays under the 20-site floor and reads none, which fails open | **done** `plugins/anatomiya/lib/model-defaults.mjs`, `reduce.mjs` (`verdictFor`), `render.mjs`, `facts.mjs` (schema 10). The flag is stored only when true, so an older record reads as never matching, which is what its scan decided |
| A16 | The overview carries this fixed sentence, which is not a count: `When unsure what this code does, read it, grep it, or run it instead of guessing, and say what you could not verify.` | The map states facts; the agent still has to go and look. The trigger clause and "read your tools, do not guess" are the OpenAI GPT-4.1 tool-calling reminder, the only such wording with a measured result (about 20% on SWE-bench Verified, as part of a three-line bundle); "read" is first because the Read tool is the one call that attaches an area file; "say what you could not verify" is Anthropic's abstention wording, which the Fable 5 page reports nearly eliminated fabricated status reports. No persistence clause and no "double-check": both over-trigger on current models and cannot be scoped in a file that loads every turn. `docs/research/one-line-that-stops-guessing.md` carries the sources | **done** `render.mjs` (`renderOverview`), pinned by a render test; constant, so A5 holds |
| A17 | Nothing is built to put a map back into a running session. A delivery lasts one context window, and the platform already rebuilds it | Measured over 12,500 transcripts, `docs/measurements/2026-08-17-context-delivery.md`: 912 deliveries in 334 sessions, 84 paths delivered more than once, 46 of those with a compaction in between. Of the twelve sessions that compacted after taking a delivery, nine took a path back; on this tool's own files all six repeats followed a compaction. So the dedup is against the window, not a per-session latch, and the two halves come back differently: the overview carries no `paths` key and is re-injected from disk at the boundary, an area file returns on the next read that matches it. The platform documents both under [what survives compaction](https://code.claude.com/docs/en/context-window#what-survives-compaction), and its `InstructionsLoaded` hook carries `compact` as a load reason. Issue #44 read the same once-per-area delivery and concluded compaction was terminal, from a session that never compacted: none of the five transcripts holding a delivery on that repository has a boundary in it. A `SessionStart` hook on `compact` would therefore duplicate the platform and spend the window the compaction had just freed, which is the cost the issue itself named. What stands is the stretch between two rebuilds, where the counts sit at their delivery turn and the work moves away from them; `SessionStart` fires at boundaries and cannot reach it | **done** as a decision not to build; `scripts/measure-delivery.mjs` is what re-measures it. The measurement is the deliverable: A6, A7 and A8 all rest on how long a delivery lasts and none of them had a count behind the word "session" |
| A18 | Two delivery facts are written down rather than rediscovered: a Read that fails still attaches the area file its path matched, and a subagent is served by the same channel, with the session's working directory deciding what it holds before its first Read | Both were folklore. The first is measured here: a Read of `scripts/ab/does-not-exist.mjs` returned the file-not-found error and attached `anatomiya-area-7fb4de45.md` in the same turn, so delivery keys on the path of the attempt rather than on the read succeeding. It falls the right way and nothing said so: an agent probing whether the file it is about to create already exists is handed that directory's counts at the moment it is about to write one, which is the read-before-write habit the overview's own sentence asks for. The second corrects the shape #47 filed it in. Five subagent transcripts on 2.1.220 and 2.1.233, every one with the mapped repository as the working directory, deliver `CLAUDE.md` and the overview on the subagent's first turn ahead of any tool call, so the overview is not absent from a subagent as such. What is measured absent is a nested repository: started one directory up, the map is nested rather than root and nothing arrives until a file under it is touched, which a Bash-only subagent never does. That is the #34 gap stated exactly, and it is narrower than the reading that a subagent does not get the map. Both halves re-run from the transcript store rather than from memory: `scripts/measure-delivery.mjs` counts the deliveries a subagent took, and the transcripts holding one are `find ~/.claude/projects -path '*subagents*' -name '*.jsonl' \| xargs grep -l nested_memory`, where the delivery sits at the second entry of the file and the first tool call after it at all | **done** as documentation, `README.md` Limits and `docs/how-it-works.md` section 7. No code: the channel is the platform's, and what was missing was the sentence |
| A19 | Each command is one in-process entry that answers with a record, deciding a write and performing it are two calls, and `plugins/anatomiya/bin/anatomiya.mjs` is argv, calls, printing and exit codes | The recipes lived in the argv parser, so the only copy of what a pin does could be exercised only by spawning a process, and the wording of every printed line sat inside whichever printer emitted it. Four readers scrape those lines, the agent through `plugins/anatomiya/commands/scan.md`, the corpus harness, the CI smoke greps and the tests, which is a contract nothing held in one place. The write had the shape from the other end: the one caller that wanted the map without creating it derived every rendered body a second time, and a second derivation of the same thing is a drift waiting for a field to move | **done** `commands.mjs` (`runScan`, `runPin`, `runCheck`, `runDoctor`, `runSetup`), `summary.mjs` (`SUMMARY_SCHEMA`, `scanSummary`, `scanLines`, `pinSummary`, `pinLines`), `write.mjs` (`planMap`, `commitMap`, and `writeMap` composed from them), `plugins/anatomiya/bin/anatomiya.mjs`. The summary is the facts and the lines are the words, kept apart so a caller that is not a terminal reads the same answer rather than a second derivation of it (A20). Every refusal belongs to the half that plans: a `--deep` scan with no checker installed refuses before the parse rather than after a minute of it, a corpus only partly read refuses to pin at all, and the escaped-directory and occupied-name refusals (F2, F17) fire on a dry run, where a plan reporting a clean write that cannot happen is the one answer worse than the failure. A plan is committed to the root it was planned for and to no other, since the two calls can now be separated by anything. `runPin` writes the pin before it answers, because the old order printed the sha it had pinned and then failed |
| A20 | The check answers with a record: a schema, coded caveats and one encoder pass. Text, `json` and `github` are three writers over that one record, at the CLI edge | The report was prose, so anything but a human told "the diff could not be read" apart from "one file was read from the working tree" by matching wording nobody promised to keep, and the acceptance harness read the numbers by regex off stdout. Twenty-three sites answered with a sentence and no code | **done** `check-report.mjs` (`CAVEATS`, `encodeReport`, `formatReport`, `formatReportJson`, `formatReportGithub`), `check.mjs` (`CHECK_SCHEMA`, and the `rules` field carrying `escaped`, `listed` and `unreadable` rather than folding them into caveat prose), `plugins/anatomiya/bin/anatomiya.mjs` (`--format`), written down in `docs/how-it-works.md` section 8. 26 codes over 23 sites: one site takes two codes from its caller, `no-merge-base` is reached from two of them, and the four causes of an unread file carry four codes because the reader's next move differs for each, which is B13's rule arriving at the check. The encoding runs once over the record rather than at each printer, so no writer can be the one that missed a repository-controlled value, and findings set the exit code in none of the three. `github` is a check option only, since nothing else here has findings. The writers are a module of their own, because the record is the only thing between them and the pipeline: `check.mjs` reaches 36 of the 52 modules in `lib/` and three of that module's four readers wanted a writer or the caveat table and paid all 36 for it, while the writers reach the encoder and the rules lister and nothing else. All three read the caveats, the cap and the rules audit, not only the findings: annotations carrying the counts alone are what a run with no map, no base and no readable diff emitted, which is the same clean-looking empty answer B13 and F15 each closed on another surface |
| A21 | `dropped.denied` is a count, matching `excluded`, `escaped` and `generated` beside it | A boolean among three counts answers a different question by accident, and nothing today reads it, which is exactly how the wrong shape would go unnoticed until a caller finally does | **done** `corpus.mjs` (`collect`), pinned by `test/corpus.test.mjs`. The special case is deleted rather than flipped from `= true` to `++`: once denial counts the way the other three already do, it falls straight through the classifier's own `if (drop) { dropped[drop]++; ... }`, the same line `excluded`, `escaped` and `generated` already share. `notSource` keeps its own branch, because it does one thing more: feeding the roster's `others` list |
| A22 | The overview's "Not covered" section names a language this tool has no dimension for, counted over the whole corpus rather than over what the roster printed | `plugins/anatomiya/lib/langs.mjs` declares three: js, jsx, ruby. Everything else failed `isSource()` and joined the same undifferentiated bucket as images and markdown, and nothing in the rendered prose ever said so. Measured: appsmith's `app/server` is 2,374 files, 2,077 of them `.java` with a real JUnit suite, reduced in the whole map to one raw extension count on a root line; next.js holds 1,016 `.rs` files across its own bundler and compiler, and the words Rust and cargo appeared in zero of its 501 generated files. A reader had no way to learn that a third of a repository, or an entire embedded subsystem, was never read | **done** `render.mjs` (`OTHER_LANGUAGE_EXTS`, `unreadLanguageFiles`), `scan.mjs` (`corpus.otherExts`), `facts.mjs` (schema 13), pinned by `test/render.test.mjs`. The count is taken from the corpus's own tally over every non-source file rather than summed back off the roster: a root prints its top two extensions and folds the rest away, which held 781 of next.js's 1,016 Rust files, and a row about what could not be read is the last place to state a number it cannot stand behind. The list of other languages is short and closed rather than exhaustive, so a missing entry is silence about a language rather than a wrong name for one, and a record written before schema 13 falls back to the roster and undercounts the same way, which is still better than the silence it replaced |
| A23 | An area's kinds line is tried on the same floor a stated directive already gets, rather than gated behind a check on the room the `paths` list left | `MAX_LINES - head.length >= MIN_BODY_LINES` decided whether the kinds line was attempted at all, so an area carved into enough children to fill its `paths` list with exclusion globs lost its kinds line before a single directive competed for the body's own guaranteed floor. Measured on cal.diy: `packages/app-store` (250 files, 38 exclusion lines) and `packages/features` (117, 34) printed no kinds line and no roster line, though the data existed and was already counted in the summary. The kinds line is the one sentence a reader wants before anything the file asks of them, so it should not be the first thing a large `paths` list costs | **done** `render.mjs` (`renderArea`). Roster lines still wait on whatever room is left after the kinds line and the directives, since what a file imports is a second reading and not a first one; A33 later took the line off the budget entirely, because leaving it to compete meant it gave way in exactly the directories holding the most directives (A6, A33, H10) |
| A24 | The plugin declares a hook that re-delivers the map after every turn and every tool call, stamped with the moment it was read, in its own `hooks/hooks.json`; a scan takes out the one an older version wrote into `.claude/settings.local.json` | The always-loaded channel puts the overview in the system prompt once per turn and that is where H1 leaves it. What it does not carry is recency: a run three hundred tool calls deep was handed the map at the start and nothing since has said it again, and nothing anywhere says when it was read, so a map that has gone stale against the code reads exactly like one that has not. The hook says both. "What is deliberately not built" refuses a hook as the delivery channel and the 10-to-40% adherence it cites was measured on a hook *instead of* the always-loaded file; this is a hook *on top of* it, so the channel that scored 100% is unchanged and nothing depends on the weaker one | **done** `hooks/hooks.json` (the declaration itself), `hook.mjs` (`echoContext`, `ownMap`, `readOwned`, `planRemoval`, `commitRemoval`, `removeStaleHook`, `HOOK_COMMAND`), `validate.mjs` (which refuses a declaration the loader would silently ignore), `commands.mjs` (`runScan`, `runEcho`), `plugins/anatomiya/bin/anatomiya.mjs` (`echo`), pinned by `test/hook.test.mjs`. Three events, not two: `PostToolUse` fires only when a call succeeds, so `PostToolUseFailure` sits beside it, or a run of denied edits is the run that hears the map least. The map is found by walking up from wherever the hook fired rather than joining onto the directory it was handed, since a hook carries the session's own cwd and the map is written once at the root; walked rather than asked of `git rev-parse` because this runs on every tool call and a subprocess per call to learn a path already on disk is a subprocess per call. The stdin read releases the handle rather than only resolving its promise: an open pipe is a live handle, and a harness that opens one and writes nothing held the process for as long as it held the pipe, measured at 8 seconds against the 2 the bound claimed. The cost is real and chosen: roughly 500 tokens per turn and per tool call, so a 300-call session spends about 150,000 on it. That figure was counted on a 2,468-file repository and carried forward by arithmetic when the head grew, not counted a second time. No knob, because this tool ships no options; the number is here so it is a decision rather than a surprise. The text it echoes is descriptive and says the code outranks it: a hook that injected directives is the shape that gets flagged as prompt injection, and this one hands back counts and a sentence telling the reader to run `anatomiya scan .` rather than believe a stale copy. Installed after the map is written and never on a dry run or a blind run, because a hook pointed at a map that was never written echoes nothing on every tool call for the life of the session. `${CLAUDE_PLUGIN_ROOT}` names the command, the way `commands/*.md` already reach this CLI, so a plugin that moves does not leave a broken hook behind, and where that variable is substituted is the whole of this row's correction. It is substituted only for a hook a plugin declares in `hooks/hooks.json`. Written into a repository's own `settings.local.json`, which is what 0.2.4 through 0.2.6 did, it is not substituted at all: Claude Code refuses the hook by name on every prompt and every tool call for the life of that session, which is worse than no hook, and no test could have caught it because every test in this repository runs the command directly rather than through the loader. Reported from the field on 2.1.238 and reproduced from the settings file a scan had written. The declaration moved into the plugin, where the loader substitutes it, and what the scan does to a repository is now subtraction only: it takes out any entry running this tool's `echo`, on any spelling that shipped, and leaves the permission lists, other people's hooks and any event that still holds one exactly as they were, removing the file only when it holds nothing else. That restores A1, since `.claude/rules/` is again the only place this writes. The cost of the move is that a plugin hook cannot be scoped to a repository and runs in every session the plugin is installed for, so the scoping is the hook's own: it walks up for a map and answers `{}` where there is none, writing nothing and creating nothing. The cost of that answer is one node process per turn and per tool call in every session the plugin is installed for, and it is almost entirely node's own start: three independent runs on one laptop put the median at 104ms, 73ms and 237ms, and swapping the `node` on `PATH` between an nvm build, a Homebrew one and `/usr/local` moved it from 57ms to 141ms with one 772ms cold start. So the honest figure is tens to a couple of hundred milliseconds, decided by which node answers and what else the machine is doing, and a single number here would be a measurement of this laptop rather than of the tool. An earlier draft said 50ms off one warm run and the next draft 237ms off twenty, and neither reproduced. `echo` is deliberately absent from the usage block and has no `commands/*.md`: a person never runs it and an agent must not, and the docs checker derives the slash-command set from that block. Measured, from a report that the plugin does nothing in a worktree: a session inside a worktree below the scanned checkout, which is Claude Code's own `.claude/worktrees/` layout, was handed the main checkout's map, stamped as re-read just now, against a branch the counts never described, and a nested repository or unscanned submodule was handed the parent's the same way. The walk now ends at a boundary: anything named `.git`, at any level holding no map of its own. The map is asked for before the boundary at each level, so a scanned checkout still answers from anywhere below its root, whichever shape its own marker takes. The cwd and the marker are both asked about links rather than around them, F2's reasoning arriving at the read side, and a level that cannot be resolved or cannot be looked at answers no map rather than throwing: a hook that exits non-zero interrupts the session it exists to help, on every turn and every tool call for the life of that session. Three things stand and are named rather than parsed for, because this runs on every tool call: a fixture named `.git` reads as a boundary, a nested repository in another VCS has no marker at all, and the map itself is opened through its links, so a `.claude` or an overview symlinked out of the checkout is read and echoed. The last is deliberate: the write side refuses it because writing outside the repository destroys what this tool does not own, while a reader that refused it would break the people who keep `.claude` in their dotfiles, and a hostile clone can ship a real file to the same effect |
| A25 | Nothing is written outside `.claude/rules/` and `.claude/anatomiya/`. What is left in the local settings scope is a removal, contained by F2, subtracting rather than replacing, and refusing a file it cannot parse | A1 keeps this tool's writes inside `.claude/rules/` so a writer bug cannot destroy something maintained by hand, and A24 needs a settings file, which is exactly such a thing: it holds permission lists and other people's hooks. Four rules keep A1's reasoning intact rather than spending it. F2 first, and it was missing: `join` normalises `..` and resolves no link, so a tracked `settings.local.json -> ../../x`, or one link at `.claude`, both of which survive a clone, put this write in a file the repository does not own. Measured before the containment call existed, against a real scan: a JSON file two directories outside the repository came back with our hooks merged into it. The local scope, because `settings.json` is the shared file a team commits and `settings.local.json` is Claude Code's own per-developer one, which is also where a map that is already git-ignored belongs. Merge-only, keyed on the command string, so installing twice adds one hook and a sibling on the same event is left beside it rather than under it. And a settings file that does not parse throws instead of being replaced: a writer that reads unreadable as absent is a writer that deletes what it could not read. The same rule at every level a spread reaches, because only the top one was checked at first: `"hooks": "PreToolUse"` spread into `{"0":"P","1":"r",...}` beside the real entries and was written back that way, and an event whose value was a string spread into a list of characters | **done** `hook.mjs` (`planRemoval`, `commitRemoval`, `removeStaleHook`), `rules.mjs` (`EXCLUDE_LINES`), pinned by `test/hook.test.mjs`, which holds a permission list and a foreign hook across a removal. The exclude line is part of the same decision: a scan that leaves an untracked file without documenting it hands a reader a dirty `git status`, and the README spelled those lines by hand with nothing reading them, so `check-docs` asserts every `EXCLUDE_LINES` entry appears there. The row was written when this tool installed a hook into that file, and A24 records why it no longer does: the variable that hook named is substituted only inside a plugin, so the entry never ran. Every rule below survived the reversal, because reading a file to subtract from it needs the same care as writing one. `settings.local.json` left `EXCLUDE_LINES` with it, since a scan no longer leaves anything there |
| A26 | The command word is required, and four surfaces say what the run actually did | `plugins/anatomiya/bin/anatomiya.mjs` defaulted the command to `scan`, which writes, so typing the bare name to see what the tool does replaced the map: 14 seconds and a rewritten `.claude/rules/` for somebody who only wanted to look. A mistyped command was read as a path and refused as `not a git repository: frobnicate`, which names the wrong fix, while a mistyped option was already refused by name with the usage three lines further down. The default was undocumented, untested and unreferenced: every call site in the repository, the plugin's own command files included, passes a command word. The other three are the same shape, a fact the run knew and did not say. A crash count is a `SIGKILL` off the pool's wall clock or its memory poll, so it measures the machine rather than the tree, and it moved two lines of the always-loaded overview between two scans of unchanged source, the line itself and the area listing entry it cost; it stays on the summary, which is not cached. `--deep` costs about 26x the parse and on a measured repository added 110 slots not one of which states, and the map, the facts store and every area file said so while the terminal the caller was watching did not. They are not empty: those 110 hold 1,970 counted sites, `law_of_demeter` at 128 of 140 in one directory among them. What the caller is not told is that the extra cost buys counts and no sentences. A first pin printed 134 lines of which 127 said the same thing once per directory, because every area is new by arithmetic and the rest of the output is a fixed seven lines. And the check computed how far the map had drifted, compared it once against the 25% cliff and threw it away, so below the cliff the reader was told nothing at all | **done** `plugins/anatomiya/bin/anatomiya.mjs`, `render.mjs` (`MACHINE_DEPENDENT`, and `degradedSemanticSentence`, one sentence both surfaces read for the same reason `untrackedSentence` is one), `summary.mjs`, `baseline.mjs` (`formatDelta`), `check.mjs` and `check-report.mjs` (the drift note, with a 5% floor so a freshly pinned repository does not print 0% into every context window; the share is not bounded by 1, since numerator and denominator are different sets) |
| A27 | Every plugin this repository's marketplace lists is validated as a plugin by `scripts/validate.mjs`: its own `.claude-plugin/plugin.json`, the paths that manifest names, and the file each of its hooks runs. The workflow's own inline copy of those checks is gone, and the script is the copy CI runs | A second entry was checked for one thing, its directory existing, so a plugin with no manifest, a manifest naming another plugin, a version that is not semver, or a hook running a file the plugin does not ship all shipped green. Two copies of the contract had already drifted apart in one commit: the inline heredoc read `owner` and the `plugin.json` path keys and the script did not, the script read the second plugin and the heredoc did not, and neither copy of the overlap was under test. Returned as sentences rather than printed, because the failure cases only exist as trees on disk and that is what put 51 cases under test. A review round then read the loader itself and found the script refusing what the loader installs, an object source naming a remote, a prompt-type hook with no command, and a bare `.`, which the entry schema rewrites to `./` before anything reads it, and passing what the loader drops: a hook with no `type`, a `../` spelling, which the schema refuses even where it re-enters the repository, a duplicate name behind a remote source, and a link out of the repository under a manifest directory, a hook file or a declared path. A first correction took the loader's own marketplace validator, which skips any source carrying `..` from its checks, for the install path, and refused what installs; the install path is a traversal guard, so a source that starts with `./` and resolves inside the marketplace is a source, whatever its name carries. Two rules of the loader's are the script's now: a local source starts with `./` and resolves inside, and a hook is one of five literal kinds, `command` with its exec-form `args`, `prompt`, `agent`, `http` or `mcp_tool` | **done** `scripts/validate.mjs` (`validate`, `manifestProblems`, `declaredPathProblems`, `hookProblems`, `escapes`), pinned by `test/validate.test.mjs`, run by the `plugin manifests` job |
| A28 | A hook that runs on every prompt is a Node module, not a shell script, and any state it keeps lives beside this account's own Claude Code configuration rather than in the temporary directory, in a directory this account owns with no access for anyone else. A state directory that is a symlink, is not a directory, or is open to other accounts is refused, and the sweep removes only files named like a session that hold a count | Claude Code runs on Windows, where a `.sh` invoked by path has no interpreter, and it ships Node either way, which is why A24's hook is a Node module too. The state half was reproduced on a temporary directory: with the state path a symlink to a directory, the counter write truncated a file inside it to `1`, and the sweep deleted a 30-day-old file that was never a counter. A predictable path under the temporary directory is one another account can create first, which is why the state moved out of it entirely: CodeQL flagged the remaining read there, and removing the class beats defending against it. The ownership check stayed for `ULTRACODE_ANYWHERE_STATE`, which a user can point anywhere. A machine with no home to write into keeps no state instead of falling back there, since the one place a plugin can always write is the one place this should not. Every refusal answers turn 1, a full reminder, because failing towards saying too much costs tokens and failing the other way switches the plugin off in silence, which is what a `set -u` abort on an unreadable counter did. The write side opens non-blocking the way the read side always did: a fifo standing at the debug path, or where the kept answer goes, held the open until the hook timed out, on every prompt of the session | **done** `plugins/ultracode-anywhere/hooks/counters.mjs` (`ownState`, `COUNTER_NAME`, `nextTurn`, `startOver`, `sweep`, `firstTime`, `cached`, `write`) over `plugins/ultracode-anywhere/hooks/hook-io.mjs` (`readIfFile`, which opens first and asks the handle what it is, so a path checked and then opened is not a path something else can swap), pinned by `test/counters.test.mjs` and `test/hook-io.test.mjs`. Review found the one write that skipped the rule, a once-per-machine mark the session hook wrote with `mkdirSync` and `writeFileSync` directly, which landed inside a symlinked state directory on a reproduction; every write here goes through one place now, and the mark answers "say it again" rather than writing where it may not |
| A29 | A plugin whose premise is read off an installed build states the build it was read off, checks on every session that what it mirrors is still there, and ships the list a person works to re-check the rest | There is no API here to hold Claude Code to, so the failure is silent by construction: the reminder keeps arriving and the model keeps reading it whether or not the contract behind it still holds. Three of the four things checked are names and the fourth is the sentence in the Workflow tool's own description that counts a standing mode as the explicit opt-in it otherwise refuses to act without, which is the contract this plugin satisfies by restating the reminder. A proximity test on the gate itself was written and dropped on evidence: `xhigh` appears nowhere within 20,000 characters of any of the 14 `ultra_effort_enter` sites in 2.1.238, so it would have failed on the build it was calibrated against. The check reads the build rather than a launcher, since npm puts every ancestor `node_modules/.bin` on PATH and the 1,275-byte `claude` shim in one of them came first, which reported every marker missing on this repository's own suite; a file under 5 MB is not the build and is no evidence either way. Both hooks read that answer once per build rather than once per prompt or once per session: the scan streams most of a 197 MB file, about a hundred milliseconds warm against a hook timeout of 5 seconds, and it cannot change while the file it was read from has not, so it is kept beside the counters under the build's path, size and timestamp. What the check cannot do is stated rather than papered over. A first reading took the shipped build for a string table with no readable logic around it and cut the concurrent-subagent cap claim from the README on that premise; A31 records the reading that overturned it, and the claim is back with its evidence. The session-start lines are said when a session starts and again after a compaction or a clear, which empty the context, and not on a resume, whose transcript already holds them. Loud on drift by default, because going silent costs the mode to everyone whose build is fine, with `ULTRACODE_ANYWHERE_STRICT=1` for whoever would rather have it off than have it pretend. The version line waits for a run of ten patch releases as well as for a minor: the first drift here went three patches with the constant naming the first of them and no session ever saying so, and two of those builds were the same size to the byte with 176,881,324 of them different, so a patch bump is not cosmetic. One patch is still noise, since the build updates itself, and a CI runner has no Claude Code to read, so a person and the release checklist are the only other things that can notice | **done** `plugins/ultracode-anywhere/hooks/upstream.mjs` (`MARKERS`, `CALIBRATED_AGAINST`, `cliPath`, `drift`, `behind`, `PATCHES_BEFORE_STALE`), `plugins/ultracode-anywhere/hooks/counters.mjs` (`cached`), `plugins/ultracode-anywhere/hooks/session-start.mjs` (`notice`), `plugins/ultracode-anywhere/hooks/standing-ultracode.mjs` (`movedBuild`), `plugins/ultracode-anywhere/hooks/hook-io.mjs` (`invokedAs`), `plugins/ultracode-anywhere/VERIFYING.md`, pinned by `test/upstream.test.mjs` and `test/session-start.test.mjs`, which also run the check against whatever build is installed on the machine running the suite |
| A30 | The reminder opens the session and comes back as one line every tenth turn, with silence in between, and it asks for the reason before a fan-out rather than for a fan-out on every substantive task | Two costs, both paid per prompt. The first is the text: a refresher on every turn is louder than the built-in being mirrored, which says it once and keeps a short line on the maintenance cadence, and over a 30-turn session the shape here is 1454 characters against 3992 for a line every turn. The second is the fan-out the text asks for, which is the one that matters: a standing "on" with no floor buys a dozen agents for a one-file edit, and no reminder can undo that spend afterwards. The floor is a deliberate deviation from the native wording, stated as one in the README's own comparison table, because a plugin that is cheaper than the thing it mirrors should say which of the two it is. The cadence starts over after a compaction or a clear, which is what the built-in does: its generator walks the messages back to the last `ultra_effort_enter` or `ultra_effort_exit` attachment, counting user turns on the way, sends the whole text when it finds none and the sparse line once `TURNS_BETWEEN_MAINTENANCE` turns have passed, and a compaction leaves it nothing to find. A counter that kept climbing left a compacted session with the refresher alone. The wakeup skip reads the `source` field the build's own payload schema declares, with the enum it declares; 2.1.238 declared it and spelled `...!1` where it would be sent, with the schema's own note that external payloads omit it, and 2.1.251 spells it the same way and sends no `source` at all, read this time off a payload the hook was handed rather than off the builder alone; the enum has since grown to six values, of which only the four here are turns to skip; until it arrives a wakeup is a turn like any other and the README says so | **done** `plugins/ultracode-anywhere/hooks/standing-ultracode.mjs` (`full`, `short`, `contextFor`, `switchesFrom`, `WAKEUP_SOURCES`), `plugins/ultracode-anywhere/hooks/session-start.mjs` (`EMPTIES_CONTEXT`), `plugins/ultracode-anywhere/hooks/counters.mjs` (`startOver`), pinned by `test/standing-ultracode.test.mjs`, which reads the per-turn and per-session totals back out of the README, and `test/session-start.test.mjs` |
| A31 | The build check reads the gate itself, as a shape rather than a name: a function returning a flag, a call, and an effort comparison against `"xhigh"`, in any of the spellings a minifier chooses between | Four names surviving is not the claim the plugin rests on. The claim is that the reminder is emitted under an xhigh gate, and the names could all be there with the gate gone. Two attempts at that check were wrong before this one: a proximity test between `ultra_effort_enter` and `xhigh` failed on the very build it was calibrated against, since the nearest pair in 2.1.238 sits 26,351 characters apart across 14 sites; and a review round wrote off the whole idea on the premise that a compiled binary carries no readable logic, which a second reviewer refuted by recovering `function Mae(e,t,r){return r===!0&&ZL()&&zZ(e,t)==="xhigh"}` verbatim out of it. The same reading settled the concurrent-subagent cap, which the README had stopped claiming: the refusal returns early when that predicate holds, and the predicate reads a session flag no hook can write, so native lifts the cap and this does not. Minified names change between builds and the shape does not, which is why the pattern matches the shape and not `Mae`: read against 2.1.235, 2.1.237, 2.1.238, 2.1.241 and 2.1.251, the three names changed every time and the shape held. What a minifier is free to respell is accepted too, `!0` or `true`, either quote, a helper called with an argument or through `?.`, since a respelling read as a drift nags every session or, under strict, switches the plugin off on a build whose gate still holds. The markers and the gate are found in one read of the bundle: a second pass for the gate read most of a 197 MB file twice, and a sub-5 MB file is no evidence either way rather than a build that checked out | **done** `plugins/ultracode-anywhere/hooks/upstream.mjs` (`GATE`, `GATE_SHAPE`, `carries`, `drift`), pinned by `test/upstream.test.mjs`, which checks a build with the gate, one without it, three respellings of it, and whatever build is installed on the machine running the suite |
| A32 | CodeQL scans what ships: `test/**` is outside the scan | Two alerts on the plugin's reader traced a temporary-directory path into `readIfFile`, and the path came from the suite's own `mkdtempSync` fixtures, which is the safe way to make one, handed to the code under test. Moving the plugin's state out of the temporary directory left that flow sourced by tests alone. Excluding tests removes the class from the scan rather than dismissing each alert as it recurs, at the cost of the one test-side finding the scan had made, an unescaped backslash in a regex a test built, which is a class the shipped code is still scanned for. Tests are not shipped and this scan is for what is | **done** `.github/workflows/codeql.yml` (`paths-ignore`) |

| A33 | An area's kinds line is taken off the line bound before the body divides what is left, rather than entered into the body as a block that competes with claim rows | Left to compete it lost, and it lost in exactly the directories where it is worth the most. Measured on a 2,489-file front end at 0.2.9: five area files carried no kinds line at all, and they were the five largest, `src/components` at 149 files, `src/queries` at 141, `src/pages` at 75, `src/components/TaskList/tasks` at 75 and `src/pages/admin` at 41. All 122 smaller areas kept theirs, including areas of one file. The boundary was the budget and it was exact: the most claims hidden by an area that kept the line was 15, the fewest hidden by an area that lost it was 17, and no area sat between. The data was there in every case, `corpus.truncated` was false, and the line was computed and then dropped at render. What it costs a reader is the overview's own headline directive: "match sibling test shape, skip tests where siblings have none" cannot be followed in a directory whose file never said what the siblings do. It comes out of the floor as well as out of the bound, so `head + kinds + body` totals exactly what `head + body` totalled before and no file grew by this; six body lines still hold one directive block, an exception and the notice | **done** `render.mjs` (`KINDS_LINES`, `areaBlocks`, `renderArea`), pinned by `test/render.test.mjs`. A23 is amended by this row rather than replaced: its own finding, that the line should not be gated behind the room a `paths` list left, still holds and is now the weaker half of the same argument |
| A34 | A stated convention the line budget cannot print in full is still named, as its sentence alone with no counts, and the notice says how many it could name | A footer reporting how many facts a reader is missing without saying which is the worst of both: the reader knows a directive exists, has nothing to look up, and conforms to the nine sentences on the page rather than the twelve the check will grade against. Measured on the same front end: 58 of 127 areas truncated, hiding 438 claim lines, and 36 of those hid 68 stated conventions between them. `src/queries/users` is the sharpest, 15 files, 9 stated conventions printed and 3 more stated and unprinted. A sentence costs one line where its block wanted three or more, so naming is strictly denser than printing and the drop walks down from the largest fit rather than settling upward from the smallest. The naming block takes half the body and no more: unbounded it spent the whole budget on sentences, and a 30-slot area printed no counts at all, which retires the audit D7 exists to provide. Where the half is not enough the notice leads with how many of them it named, or the list reads as complete and is not. A named slot is still a dropped slot to `check`, deliberately: what the reader lost is the count, the exceptions and the audit, and the severity that means "the map told you and you are the first to break it" is a statement about evidence the file no longer carries. So the sentence is delivered and the grading stays capped, and `droppedDirectives` returns the key either way. Counted off those keys and never off the sentences, because `check` recomputes this layout from `facts.json`, which stores every slot's key and no slot's prose: reserving lines off the prose would have the writer keep nine blocks where the check counted ten, and the two would disagree about what the agent was handed | **done** `render.mjs` (`settle`, `alsoStatedLead`, `fitCount` taking a line count rather than a flag), pinned by `test/render.test.mjs`, which holds the writer and `droppedDirectives` to the same layout on a record carrying no sentences |
| A35 | A stated claim that is perfect and names no exception says, in one clause, which form its own predicate declined to count. A row carries the clause only where one short true clause names the whole exclusion and holds wherever the row does, and the clause is pinned by running the predicate rather than by reading it | The counts were right and the line read as false, which is worse than a wrong count because the reader who checks is the one who stops trusting the map. `src/components/Calendar` printed "a prop spread lands on a component, not on a host element / 46 of 46 sites across 5 of 6 files", and one of the five credited files holds `<div {...rest} />` three lines above `<ClickableArea {...rest} />`. Nothing was miscounted: a wrapper forwarding a rest binding has no list of prop names to write instead, so it is not a free choice and not a site, and the dimension has carried that reasoning in prose since it was written. The prose was never rendered anywhere. Four more instances of the same shape sat in that one map, each a perfect count beside a credited file holding what reads as its counter-example. Only under a claim that is perfect and carries no `except`, because the `except` list is what teaches a reader that a miss gets named, so a bare `N of N` is read as "there is no counter-example in these files", which is stronger than anything the tool measured. The second bar is what the row is really worth, and the first is why every clause here is pinned by a fixture that runs its row: a clause written off the `sites` prose and never run against the predicate it describes was wrong or partial far more often than not, so the fixtures are the evidence and the prose is not. Two were false outright, including the worked example above, which had dropped the condition that the exclusion applies only onto a host element: a clause reprinted in this file and in `docs/how-it-works.md` is wrong in three places at once. The ones that could not be corrected inside a line were the ones whose exclusions are genuinely a list, and a list belongs in `sites`, which nothing prints: `module_include` alone declines five separate shapes. The middle bar is the one a first pass got wrong in both directions. A decline that drops a whole file is one the reader cannot meet: the file is not credited, and `N of N sites across X of Y files` already says a file went uncounted, so a line there restates the count above it. That is why the two migration-wide Rails rows carry none while `column_null_declared` and `reference_foreign_key` do: a `create_table` block can hold four counted columns and one `t.string :name, **opts` that was dropped, all under a perfect `4 of 4` in a file the claim credits, and nothing on the line reveals it. Ten rows were dropped: the six Ruby rows for length, both naming rows for length, and the two migration-wide rows for granularity. A41 revisits the filename row: what it owes a reader is a count rather than a sentence, and a count needs no clause and no line. `absent_is_null` fails the first bar rather than the second, since its sentence names no React effect callback, and it had this repository's own `scripts/` directory, a Node CLI, advertising React. Twenty remain, each pinned by a fixture that runs the declined form and asserts no site, beside one the row does count, so a fixture cannot pass because nothing parsed. A41 adds no twenty-first. A row that grows a clause and no fixture fails the suite, because shape is all `assertApplicability` can check and prose cannot be asserted at all. The clause prints once per file rather than once per row: nine companion rows share one sentence and two of them reach the same area whenever a repository writes both `_spec.rb` and `_test.rb`, which printed it three times in one `app` area. Keyed off the registry and not stored on the record, because both readers of the layout have a key and only one has the prose | **done** `render.mjs` (`NOT_COUNTED`, `notCountedLine` and its per-file `said` set), `dimensions.mjs` (`assertApplicability` holds the one-line bound, since the field is printed into a forty-line file rather than stored), `applicabilityPredicate.notCounted` on 20 registry rows, pinned by `test/declined-fixtures.mjs`, run from `test/dimensions.test.mjs` and `test/dimensions-rails.test.mjs`, and by `test/render.test.mjs` |
| A36 | One module decides whether a script is the file node was told to run, resolving both sides before comparing, and a test refuses the spelling that compares a resolved URL against a path as it was typed | `import.meta.url` is always resolved and `process.argv[1]` is whatever the caller spelled, so an equality between them answers no for every invocation whose path holds a symlink, and the guarded script runs nothing and exits 0. The correct spelling was found twice, once for CI's manifest gate and once for the second plugin's hooks, and left standing in seven scripts: `scripts/validate.mjs` carries a comment explaining it and `scripts/check-docs.mjs`, the other gate in the same job, does not. Reproduced by mutating one doc claim in a copy: run directly it printed the error and exited 1, and the same file reached through an absolute symlinked path printed nothing and exited 0, which for a gate is worse than not running. CI itself cannot trip it, because both its invocations are relative and `process.cwd()` answers the physical path, and the vector is still not hypothetical: `os.tmpdir()` on macOS is a symlink, which is how this repository's own `check-docs` fixture reached it without trying and why that fixture already calls `realpathSync`, and a worktree of this repository sits under `/private/tmp` today. `seed-defaults.mjs` is the one where the silence would not have been the whole cost: its `main` holds no `process.exit`, so an import that was not guarded overwrites `plugins/anatomiya/lib/model-defaults.json` rather than ending the test run. It lives under `scripts/` and not `lib/`, because nothing the plugin ships needs one, `plugins/anatomiya/bin/anatomiya.mjs` runs at module scope on purpose, and the shipped set is checked now | **done** `scripts/entry.mjs` (`invokedAs`) and the thirteen scripts that call it, eight converted with it, three written against it and two that arrived later (`scripts/plugin-lock.mjs` with B35, `scripts/ab.mjs` with its guarded `main`), pinned by `test/entry.test.mjs`, which spawns a gate through a symlink against the same gate spelled directly, drives the argv-less case through `node -e`, deletes a running script's own file to reach the unresolvable path, runs both copies as files of their own so a guard that answered no for itself is caught where symlinks are unavailable, and walks six named source directories for any file that reads `process.argv[1]` at all. That last rule is about where the value may be read rather than which comparison is written, because the comparisons are not a list. What a spelling cannot do is separate the two: node absolutises and normalises `argv[1]` before a script sees it, so `.`, `..`, `//` and a relative path all collapse and only a symlink survives, which is why the discriminating case carries `needsSymlinks` and a Windows checkout without the privilege runs the weaker one. The second plugin keeps its own copy in `plugins/ultracode-anywhere/hooks/hook-io.mjs`, since a plugin may not run a file outside its own root, and the same test holds the two to one rule |
| A37 | The two plugins share no source. What they share is a behavioural contract, run against both as processes, driven off each plugin's own declaration | The seam is real and one-directional: a hook may only run a file inside its own plugin root, which `scripts/validate.mjs` refuses and the loader refuses again once installed, where a relative import out of an installed plugin resolves into the version-keyed cache parent and fails at run time with nothing having validated it. Exactly one plain-file arrangement is legal both ways, and it puts the shared module inside the smaller, less-versioned plugin, which inverts the dependency for the sake of removing a copy. So the payload read, the megabyte cap, the wall-clock bound and the one-object answer stay written twice, and the second copy is held to the first by behaviour rather than by text. The contract found two live defects when it was written. anatomiya's echo hook wrote its answer with no error listener on stdout, so a reader that went away mid-write raised EPIPE on the stream rather than from the call, which the try/catch at the boundary never saw: five runs out of five exited 1, on every turn and every tool call for the life of that session. The second plugin's `readStdin` had no wall-clock bound, and a pipe held open and empty parked it inside a blocking read until Claude Code killed it at the timeout it declares, five seconds of every prompt and fifteen of every session start. The fix for the second is why the read goes through `process.stdin` and not a stream opened on the descriptor: an `fs` read is served by the threadpool, where a blocking pipe holds a worker inside the syscall, so the timer fires and the answer comes back and the process still will not exit with no handle left to point at. The contract test was proved able to fail by putting each defect back in a copy of the tree: three of its cases caught the first and two caught the second | **done** `test/hook-contract.test.mjs`, which reads both `hooks/hooks.json` files, substitutes the loader's variable and runs every declared command as a process, `plugins/anatomiya/lib/hook.mjs` (`readPayload`, `respond`), which is now anatomiya's whole side of the wire, and `plugins/ultracode-anywhere/hooks/hook-io.mjs` (`readStdin`, `here`). The descriptor argument `readStdin` took is gone with it: it existed for tests, and a test driving a handle the plugin never sees proves nothing about the one it does |
| A38 | The shipped set is stated in `package.json` `files` and read back through `npm pack --dry-run --json --offline`, against every file the hooks and the command files reach | The plugin root was the repository root when this was written, so nothing about the tree said which files belong to the plugin and which are how the plugin is built. A40 moved it, and the rule outlived its premise: a plugin ships its directory whole, so `files` is still the one thing that says which of those files are its own, and the plugin beside it that has no list at all is checked by the walk instead. `files` was the one list that said, and nothing read it: the package is private, so no pack or publish ever happened, and it was wrong in both directions in silence. It omitted `hooks/`, which is the declaration the loader reads before anything else, and its `.claude-plugin/` entry shipped `marketplace.json` along with the manifest, which describes the marketplace and not the plugin. npm answers what the list covers rather than this re-implementing it, because the semantics are not plain globs: a directory entry is recursive, a negation subtracts, a nested `.npmignore` subtracts and is itself not shipped, a few names are added whatever the list says, and an entry naming a path that does not exist is passed over without a word, which is the one that made the gate worth having. The other half is walked rather than listed, from every starting point the loader has: the hook declaration, the mcp file, and every file under the commands, agents and skills directories, each read where the manifest puts it rather than only where the convention does. A first pass read two of the five, and widening it to all five carried an exemption with it that had been written for the two files with a check of their own, so for one round no command, skill, agent or mcp file was checked for being shipped at all. From each starting point it follows relative specifiers through `from`, a bare `import` and any call, since this repository loads its one JSON table through `createRequire(import.meta.url)("./model-defaults.json")`, where the callee is a value and no pattern can look for a name. A hook declaration is parsed as JSON before its command is read, because the quotes around the path arrive escaped and a pattern over the raw bytes took the backslash before the closing quote as part of the filename | **done** `scripts/shipped.mjs` (`shipped`, `reachableFrom`, `pluginPathsIn`, `PACK_ARGV`), pinned by `test/shipped.test.mjs`, run by `npm run validate` and by the `plugin manifests` job. The relocation this makes unnecessary was measured rather than assumed: 62 tracked files move, `scripts/validate.mjs` needs restructuring rather than repointing, CI names the binary by repository-root path seven times, eight of nine top-level scripts break, at least 48 test references and 64 documentation lines carry root-relative paths, and `runSetup` installs into the directory `lib/` sits in, so a `package.json` left behind reports a successful install of nothing |
| A39 | One tag namespace per plugin, resolved by a module the release workflow runs rather than by shell inside it. Each plugin carries its own version, its own changelog and its own coverage floor | One tag namespace and one changelog served two plugins that answer to different upstreams, and the dangerous shape was not a tag that failed. A prefixed tag never fired the workflow at all and a suffixed one died at the version compare, so both were refusals; a bare `v0.1.0` matched the second plugin's version, found no section of its own, and would have released anatomiya's `0.1.0` notes under the second plugin's name, silently. Run against the shared changelog, the workflow's own matcher answers `0.1.0` with 8,137 bytes that belong to the other plugin. The logic moved out of the workflow because a release cannot be taken back and six lines of `awk` inside a YAML file cannot be run until a tag is already pushed. The same module is what `check-docs` now calls, so a version that passes on a branch is a version that will tag. The coverage floor was one number over both: `hook-io.mjs` sat at 91.7 line and 75.8 branch, under both floors, and the total never showed it. Scoping the floor to the second plugin alone did not fix that and a review round caught it there: the same file measured 80.5 of branches under the same 87 and the scoped total still cleared, because what an aggregate hides is proportional to how much larger the rest of the scope is. The five files that plugin ships are held to a floor each now, read off an lcov record rather than off the total node prints. A round aimed at that fix measured the same silence one scope up: 33 of 149 files sat under the whole-tree floor on their own with the total clearing 95, and `check.mjs` could have lost every covered line unnoticed. `lib/` is held to a floor per file too, with `dimensions-semantic.mjs` named as an exception rather than the floor lowered to admit it, and a stale exception reported as its own problem. The floor caught `tsconfig.mjs` at 71.4% of functions on the first run it was enforced, and the cause was that every case in `test/tsconfig.test.mjs` sits behind `needsTs`: the file has tested the confinement since 0.1.11 and skips wherever TypeScript is absent. Eleven cases were added beside them that hand the two host builders the module they already take as an argument, so the confinement is measured on every runner. The rest of the tree stays on an aggregate because the `--deep` tier and the by-hand measurement harnesses are deliberately uncovered by the unit suite. The claim that the second plugin's version had gone stale was checked and was false: nothing under `ultracode-anywhere/` had changed since it shipped, so `0.1.0` was honest and what was missing was anything that would notice when it stopped being. This change is what stops it being honest, and it puts the section under `## [Unreleased]` that the next `ultracode-anywhere-v` tag releases. `docs/releasing.md` said a guard held its hook to a file it ships, and deleting that plugin's whole declaration passed every gate, because the hook check returns without a word when the file it was going to read is not there | **done** `scripts/release.mjs` (`RELEASES`, `TAG_GLOBS`, `releaseFor`, `sectionFor`, `notesFor`), `plugins/ultracode-anywhere/CHANGELOG.md`, the two tag patterns in `.github/workflows/release.yml`, `scripts/coverage.mjs` (`FLOORS`, `parseLcov`, `shortfalls`), and `installsNothing` in `scripts/validate.mjs`, which asks whether a listed plugin would install with a name, a version and no behaviour. Pinned by `test/release.test.mjs`, which holds the workflow's tag patterns, the marketplace's plugin list and the checklist table in `docs/releasing.md` to the one table in the module, and by `test/validate.test.mjs` |
| A40 | Neither plugin sits at the repository root. The root is the marketplace and the development tooling; each plugin is a directory under `plugins/`, with its own manifest, its own package and its own release | A marketplace entry whose `source` is `"./"` is copied whole into the plugin cache, which was read against primary sources rather than inferred. Measured on this machine, `~/.claude/plugins/cache/crisnahine/anatomiya/0.2.13/` held `test/`, `docs/`, `node_modules/`, a 246 KB `DECISIONS.md`, a 124 KB `CHANGELOG.md` and a complete copy of `ultracode-anywhere/`, so somebody who installed both plugins held the second one twice and every installer paid for the suite. `package.json` `files` does not govern that copy; only the layout does. Anthropic's own marketplace is the one first-party signal there is: of its 286 entries, 53 name a relative source, 38 under `./plugins/` and 15 under `./external_plugins/`, and none is `"./"`. What the move bought beyond the copy is that the gates stopped needing a special case: `validate.mjs` had three `pluginRoot === resolve(root)` tests and a root-only `required: true`, and every one of them is gone, so both plugins are read by one path and the plugin whose hooks are required is named on the marketplace instead of inferred from a position; `shipped.mjs` now answers for every plugin the marketplace lists rather than for the one it happened to sit in. The cost was measured before it was paid and it was the measurement in A38: 62 tracked files moved, file after file referenced them, and the reference is now one module, `scripts/plugins.mjs`, which both the suite and the gates read. One thing was given up: the plugin is a workspace member, so npm keeps the single lockfile at the marketplace root and the plugin directory has none, which is what Claude Code's automatic `npm ci` looks for. That install was never the documented path, `/anatomiya:setup` is, and the README said so before this change | **done** `plugins/anatomiya/` and `plugins/ultracode-anywhere/`, `scripts/plugins.mjs` (`ROOT`, `PLUGINS`, `ANATOMIYA`, `ULTRACODE`, `BINARY`, `REL`, `installed`), the `workspaces` field, and `docs/plugin-contract.md`, which carries the whole read with a source per claim and the CLI version it was true of |
| A41 | A whole-file decline still owes the reader the count where the file is a site the check enforces over, and that count goes on the counts line rather than on one of its own | A35 dropped both naming rows for length and left its whole-file reasoning standing over them: a decline that drops a whole file is one `N of N sites across X of Y files` already discloses. That holds while the dropped file is simply not a site. It does not hold for `file_naming_case`, and the row is the one place the two sides of the tool disagree by construction: the scan votes with a stem's class and the check enforces over the site, so a stem spelling none of the four is a site with no vote. Measured on a Rails map of 159 areas, `db/migrate` printed "1525 of 1525 sites across 1525 of 1532 files" while three double-underscore migrations were sites the scan never classified, and a new one there is reported MUST-FIX against that sentence with "all 1525 baseline sites conform" printed beside it. It was the only disagreement to survive a re-count of all 261 stated claims in that map, and no count in it was wrong: the failure is disclosure, not arithmetic, which is why the fix carries a number rather than correcting one. Three shapes were tried before this one. A fixed clause on its own line, the way the other twenty rows carry one, prints in every area stating the row including the 135 of 136 that skipped nothing, and says only that something might be missing, which is a disclaimer where the reader is owed a disclosure. A clause carrying its own count fixes that and still costs a line: measured across 35 repositories, 2.8% of directories hold such a name and 12.6% of Angular's do, and on vscode six of 500 areas sit at the forty-line bound with three of those stating the row, so the line pushed a stated convention out of one of them and `check` then capped that slot at FIX. A disclosure that costs a convention is a bad trade. The count on the counts line costs nothing, sits beside the population it qualifies, and needs no template, no clause and no second printing rule: a declined site never reaches the `except` list, so no perfect-claim guard applies to it either. The count is narrowed by `splitBy` alongside the population it belongs to, or it would name files the sentence above it no longer speaks about, and it is stored because the record is what the map is derivable from, the way `companionsElsewhere` is. A count the renderer cannot read as a whole number above zero prints nothing, since a record is a file on disk | **done** `reduce.mjs` (the `declined` set in `reduceArea`, narrowed with the population it belongs to), `facts.mjs` (`declined` on the record, schema 17), `render.mjs` (`declinedClause` on the counts line, leaving `notCountedLine` and the twenty fixed clauses untouched), pinned by `test/naming.test.mjs` for the count and its narrowing, `test/facts.test.mjs` for the trip to disk, and `test/render.test.mjs` for the clause, the singular, an unreadable count, and that the block height and the dropped-directive set are the same with the count as without. What it does not reach: an area whose every naming site is declined produces no slot at all, so it states nothing and counts nothing, while the check still enforces an ancestor's sentence over a new file there. There is no population of its own to qualify, and borrowing the ancestor's would put a count from one area under a sentence measured over another |
| A42 | The overview carries a third fixed sentence: `When a change is asked for, follow what this repository already does and carry it through instead of stopping at a suggestion.` | A16 sends the agent to go and look; this one says what to do with what it found, which nothing in the head said. The counts state the practice and no line told the agent to follow it. "Follow what this repository already does" is the convention half, and its wording alone adds nothing: Claude Code 2.1.241 carries "write code that reads like the surrounding code" in one branch of its prompt block, and the map already derives sentences like "Match sibling test shape" with counts behind them. What this half adds is delivery, not wording. The sentence sits at the top of the file whose body is the counts, so "what this repository already does" has a denominator under it instead of being a sentiment. Its limit: most stated directives live in the area files and the head is not in them, so what sits under this sentence is the overview's own counts, and the area files are reached by the read-before-editing line above. Alternate 3 in the note put the pointer into the words ("prefer the practice counted below") and was rejected: it reads as documentation of this tool, and it goes half wrong the moment the interesting counts are in an area file. So this half rests on position, and the proactive half is what the line says that nothing else in the stack says. "Carry it through instead of stopping at a suggestion" keeps the shape of Anthropic's `default_to_action` ("implement changes rather than only suggesting them"), with "instead of" for the house idiom the line above it already uses; the Opus 5 page's "Finish the whole task, and stop short of actions that are clearly beyond what was asked" is why it is scoped instead of carrying OpenAI's persistence clause. The trigger clause is the guard, and it is the half with a number behind it: OverEager-Gen measures this class of harness at 5.4 to 27.7% out-of-scope actions on benign tasks, and reports that stripping the consent statement "multiplies the overeager rate on every shared base model (Delta in [11.9, 17.2] pp)". The gap it aims at is measured and large (Senior SWE-bench puts Opus 5 at 62.1% solved and 34.7% solved tastefully; NITR finds 13.3% of edits pass every functional test and fail the structural oracle; SlopCodeBench finds agent code 2.3x more verbose than 473 human Python repositories), and no study measures a rules-file line closing any of it, so no effect is claimed. `docs/research/one-line-that-finishes-in-house-style.md` carries the sources | **done** `render.mjs` (`renderOverview`), pinned by `test/render.test.mjs`; constant, so A5 holds. It costs one line on a repository already at the bound. `renderLayout` is budgeted before the area listing, so a head line takes from the roster's budget first and from the areas' room after; measured on this repository and across synthetic shapes from 7 to 40 roots and 2 to 200 areas, what went every time was a named area, here `test`, which drops out of the listing and into the trailer. A fourth head line has less room than this one had: with every optional head line rendered at once, this line already takes the roster under its frame, and `renderLayout` drops that section whole rather than shrinking it further. No real scan reaches that shape, because a scan counts untracked files only when it found no tracked ones and a truncation needs many, `test/render.test.mjs` now sweeps the two apart and holds the roster over the reachable half, and `test/scan.test.mjs` pins the invariant that makes it the reachable half, so a change to `scan.mjs` that re-enters the shape fails there instead of going quiet here. A canary in `test/upstream.test.mjs` reads whatever build is installed and fails when that sentence is gone, so the premise is checked where the rest of this file's premises are, and not left to a human to remember. It skips where no build is installed, since a missing build is not evidence either way |
| A43 | By default the reminder holds one effort for the whole session, subagents and workflow stages included except where a spawn's own definition sets one, instead of naming a higher one for the stages that want depth; A47 is what lets a session name one | The text used to say to pass `opts.effort` at 'high' or 'xhigh' on the verify, judge and critic stages. That is the one place in this repository that asked for a second effort, and a stage running deeper than the session it belongs to is a variable nobody set out to introduce: the session was configured at one level and the fan-out silently spends at another. Depth is bought instead by how the work is split and independently checked, which a prompt controls without changing what the session costs. It is a fifth deliberate deviation from the native wording, listed as one in the README's comparison table beside the other four. The whole text moves from 1224 characters to 1236 and a 30-turn session from 1412 to 1424, both re-measured off the text itself by `test/standing-ultracode.test.mjs` rather than restated by hand | **done** `plugins/ultracode-anywhere/hooks/standing-ultracode.mjs` (`OPENING`, `STANDING`, `ONE_LEVEL`, `full`), `plugins/ultracode-anywhere/README.md`, `plugins/ultracode-anywhere/VERIFYING.md`, pinned by `test/standing-ultracode.test.mjs`. **Narrowed by A47**: this row is the default text and still is, but it is no longer the only text, and A47 corrected one sentence of it: "runs at that same level" was false for a spawn whose own definition sets an effort, which moves this row's 1236 and 1424 to 1266 and 1454. A47 lets a session name one level for its whole fan-out and the third paragraph then asks for it. What this row objected to was a level nobody chose, written into the text for everyone; a level the user set is chosen, so the two stand together once this row is read as being about the default rather than about every session |
| A44 | A second hook fires on `PreToolUse` for `Write`, `Edit` and `NotebookEdit`, answers for the one path that call is about, and is silent otherwise. It informs and never refuses | The claims a repository states arrive on `PostToolUse`, after the file exists, and an area's own file loads only when something in that area is read, so the directory nobody read is both the blind spot and where a convention gets broken. A session was handed the overview more than a hundred times and still put a vitest spec in a page directory whose 1003 files hold no test, then a spec in a `spec/mailers` its own commit created. Silence on the ordinary write is the design rather than a saving: an unchanged block on every result is what taught that session to skip the clause that mattered, so this speaks only where the counts have something to say, and only for a path nothing is at yet. An `Edit` names a file that exists every time and a `Write` over one is a rewrite, so there the path was chosen turns ago and repeating the block on each edit of the same spec is the banner this exists instead of. `Edit` stays in the matcher even so, because the guard that decides is the file's absence rather than the tool's name, and a build that lets `Edit` create one should not need this reopened; the spare process costs 0.08s measured on microsoft/vscode, against a `PostToolUse` matcher of `*` that already spawns one per tool call. The counts named are the source root's rather than the target directory's, which the issue's own example asked for: a directory the write is creating holds nothing, and "this directory: 0 files" is a line that says nothing. The issue asked for a second verdict on the write that is fine, `RARE, precedented`; refused, because that fires on nearly every write and is the same banner in different words. It asked to stop re-sending the unchanged overview too; also refused, since the always-loaded channel is the one that measured 100% and this hook is on top of it rather than instead of it, and what was actually wrong was that nothing spoke at the moment the path was chosen. It never refuses. Measured on 2.1.250, `PreToolUse` honours `additionalContext`, and `deny` or `ask` are the only answers that stop a path being chosen; the rule behind the notice rests on a namesake match that reads a tested directory as untested where the names differ in case, and refusing on a count that can be wrong stalls real work. Every other failure mode there lets the write through and says nothing, and only exit 2 blocks, so the hook never exits 2 and answers `{}` for anything it cannot decide, which is the guarantee A24 already makes for the other three events. The target is read per tool, because `Write` and `Edit` spell it `file_path` and `NotebookEdit` spells it `notebook_path`, and both it and the root are resolved through their links before they are compared: the root arrives resolved and `resolve` follows none, so a payload carrying `/tmp/x` against a root reading `/private/tmp/x` read as another repository's file and the hook said nothing at all. The record is read through the same bounded, typed reader the map is, since a named pipe at its path never returns and this runs before every write, but held to its own cap: the record is the whole count of a repository, measured at 9,957,450 bytes on microsoft/vscode, and the megabyte a rule file is held to would have silenced this on exactly the repositories where a directory nobody read is easiest to miss. The matcher is a list of names rather than a pattern, which is how that build reads one holding only names and pipes. A notebook cannot produce a notice today, since `.ipynb` is not one of the extensions a test file is held to; the tool stays in the matcher because which key it spells its target with is a fact about the wire rather than about what a rule does with it. What the matcher cannot reach is named rather than papered over: a model that writes with a heredoc through `Bash` never touches `Write` at all, and under `bypassPermissions` that is an ordinary path. Measured in a real session, that write got the placement right anyway off the sentence the map states, which is the half of this that is read rather than fired, and the two halves are complementary for exactly that reason. Matching `Bash` instead would mean parsing a shell command for its redirect target, which is a guess about somebody else's grammar on the hottest tool there is. Issue #119 | **done** `plugins/anatomiya/lib/hook.mjs` (`ownLayout`, `targetIn`, `isPathTaken`, `NOTICE_COMMAND`, `isOurCommand`), `plugins/anatomiya/lib/precedent.mjs` (`noticeFor`), `plugins/anatomiya/lib/commands.mjs` (`runNotice`), `plugins/anatomiya/bin/anatomiya.mjs`, `plugins/anatomiya/hooks/hooks.json`, pinned by `test/precedent.test.mjs`, `test/hook.test.mjs` and `test/hook-contract.test.mjs`, whose shared contract now carries which hooks always speak and which speak only when they have something |
| A45 | A payload `JSON.parse` refuses is read for the members that can still be had: string members, at the top level and inside `tool_input`, whole, and short enough to be a path | `JSON.parse` reads a document or nothing, so a complete payload followed by one stray byte answers the same as no payload at all, and one larger than the megabyte cap answers nothing whatever the cap keeps. Both are ordinary rather than hostile: a `Write` of a generated file carries the bulk in `tool_input.content`, a `Read` of a minified bundle carries it back in `tool_response`, and the four short fields the hooks read are sitting in the first hundred bytes of both. Measured on 0.4.2: a 240-byte envelope with 2 MB of padding after it, and a 1.43 MB single object, each cost the turn its map; the branch keeping the first megabyte could only ever answer for padding that was whitespace, which is the one kind `JSON.parse` skips | **done** `hook.mjs` (`asPayload`, and the block written twice: `NESTED_MEMBER`, `isSpace`, `skipSpace`, `fieldsIn`, `readMembers`, `put`, `endOfString`, `decodeString`, `skipValue`), `plugins/ultracode-anywhere/hooks/hook-io.mjs` (`parsePayload`, and that same block). The grammar is read rather than matched, because the cargo being stepped over is a file's own text: a reader that found `"cwd"` inside one would answer another repository's path for a live write, and nothing here looks inside a string. Every value answered is decoded by `JSON.parse` on that value's own token, so escapes, surrogate pairs and a Windows path full of backslashes stay the parser's business rather than becoming a second implementation of it. A value longer than `PATH_MOST` names no place a filesystem can hold, so it is the payload's cargo and is stepped over as a span rather than copied. Depth is counted rather than recursed: a value nested fifty thousand deep is a stack overflow in a reader that calls itself, and this runs before every tool call. The two plugins hold the same reader, since neither may run a file outside its own root, and two cases hold them together: one drives both against a list of payloads, the ordinary ones and every shape that reaches the reader of last resort, and one compares the block itself character for character, from `NESTED_MEMBER` to the end of `skipValue`. The behavioural one can only catch a drift some payload in its list happens to reach, and three did slip past it while the compared span began lower down: a `\f` added to `isSpace`, a bound moved from 4096 to 64, and `skipSpace` stepping by two. `VALUE_MOST` stays outside the compared span, since the two spell it differently on purpose, and is held to a number by a pair of payloads either side of it |
| A46 | The entry point reads `process.cwd()` only for the verbs that answer about a repository, and a hook is allowed to have none | `process.cwd()` refuses with ENOENT once the directory a session started in is unlinked, which `git worktree remove` does to a session sitting in one, and it is read before the payload, so the throw reached the guard and the guard answered the empty object. Every hook is a fresh process, so both went quiet for the rest of that session while every payload was still naming live paths. Measured on 0.4.2: the same payload, naming a live file in a mapped repository, answered 1,694 bytes with the shell's directory alive and `{}` with it unlinked. The value is cached in node and throws only cold, so a case that changes directory and removes it afterwards reads the stale path and proves nothing; the process has to start with the directory already gone | **done** `plugins/anatomiya/bin/anatomiya.mjs` (`sessionDir`), `hook.mjs` (`baseIn`, `aboutDir`, `targetIn`), `commands.mjs` (`answersFor`). The absent base is let through rather than substituted: both readers already answered without one, and `targetIn` had a written branch for it that the entry point could never reach. `aboutDir` did not, and `resolve` reads its arguments from the right and stops at the first absolute one, so an absent base behind an absolute path was never looked at while a relative path threw `ERR_INVALID_ARG_TYPE`; the two readers now take the base from one place, which is what the comment on each of them had been asking for. `doctor` and `setup` answer about this installation and take no path, so they no longer ask where the process is at all, and the verbs that do walk a tree say what happened and ask for a path rather than handing nothing to a git call. They cannot name the directory, since it is the one thing that can no longer be read |
| A47 | A session may name one effort level for its whole fan-out, in `ULTRACODE_ANYWHERE_STAGE_EFFORT`, and the third paragraph of the reminder then asks for `opts.effort` at that level on every workflow stage, and for it to be left out of a stage checking or judging another stage's work | A43 took the second effort out of the text and was right to: the text used to name 'high' or 'xhigh' for the stages wanting depth, so a session configured at one level spent at another with nobody choosing it. This is the other direction and A43's argument does not reach it. Fan-out is where the tokens go, a finder sweeping a directory does not need what a main-loop turn needs, and the level is chosen by the user rather than by the text. Nothing else a caller passes can reach it, and the text names the level rather than a direction, since `--effort` and `/effort` write nothing to `settings.json` and the hook cannot read the session's own level to say which way it points. Measured on the installed build: the spawn builder pushes a `{kind:"effort"}` permission layer only where the agent definition carries an `effort`, the resolver falls through to the parent's own state when there is none, the `workflow-subagent` definition carries neither effort nor model and nothing registers it so no `.claude/agents/*.md` can shadow it the way one can shadow `general-purpose`, and the Agent tool's schema has no `effort` parameter at all, so the text says the tool takes no effort argument rather than that it has no effort, which an agent definition file has always been able to carry. `subagentEffort`, `agentEffort`, `CLAUDE_CODE_SUBAGENT_EFFORT` and `defaultSubagentEffort` are zero hits in the bundle, and anthropics/claude-code#79135 asks for the variable and is open. So `opts.effort` is the whole of it, the model is the only one holding it, and the switch is a request rather than a setting: a session that ignores the text runs its fan-out where it was going to run anyway, which is the safe direction to fail in. The exception is the half that is not free, and it is stated as an action rather than as a value: "keeps the session's level" names something a model reading the text cannot look up, and leaving `opts.effort` out is what produces it, measured on the wire as a stage that tracked its session from medium to high. The stage that checks another stage's work is the independent check the depth argument rests on, so the text says that stage runs at the session's level unless its own definition sets one, which is what leaving `opts.effort` out actually does. It carries that carve-out because a stage naming an `agentType` resolves a registered definition and an `effort:` in that file wins where the script passes none, so a floor stated without it is one the build can break: a check whose definition carries `effort: low` runs at low under an xhigh session. Said as "never runs cheaper than the session" it was both unfalsifiable, since leaving the argument out makes the two equal, and false for that stage. The README carries what that means each way rather than leaving the reader to find it. One above the session is allowed, since a level a user set is a level somebody chose, which is exactly what A43 was about. Anything that is not one of the five levels is read as unset and said out loud at session start, rather than falling back in silence the way an unreadable cadence does: a cadence that falls back costs a refresher its place, and this one costs the saving it was turned on for. The value that reaches the text is the list's own string and never what the variable held, because a project's `settings.json` sets `env` and the reminder is read as instructions; a value carrying anything but a plain word is counted rather than quoted back in the session line, for the same reason. The whole text moves from 1266 characters to 1464 at the longest level name and a 30-turn session from 1454 to 1852, both re-measured off the text itself. Issue #129 | **done** `plugins/ultracode-anywhere/hooks/effort.mjs` (`EFFORT_LEVELS`, `stageEffortIn`, `askedFor`), `plugins/ultracode-anywhere/hooks/standing-ultracode.mjs` (`OPENING`, `STANDING`, `ONE_LEVEL`, `CHECKING_STAGE`, `DEFAULTS`, `loweredTo`, `full`, `short`, `switchesFrom`, `contextFor`), `plugins/ultracode-anywhere/hooks/session-start.mjs` (`notice`), `plugins/ultracode-anywhere/README.md`, `plugins/ultracode-anywhere/VERIFYING.md` (step 7). Its own file rather than either hook's, because both need it and neither may import the other's entry point: a hook that imported one would be one `invokedAs` change away from running it. Pinned by `test/effort.test.mjs`, whose canary reads the five level names out of whatever build is installed and skips where there is none, `test/standing-ultracode.test.mjs`, which also refuses a switch the hooks read and the README does not name, and `test/session-start.test.mjs` |
| A48 | The Agent-tool half of A47 is reported and never set. `ULTRACODE_ANYWHERE_SUBAGENT_EFFORT` names a level and buys one sentence: which of `general-purpose`, `Explore` and `Plan` no file names, which have a file the build refuses for want of a description, which name no effort, which name another level, and which were last written before the installed build. Files are keyed on frontmatter `name:` and never on the filename, which is what the build keys on. Nothing is written, generated or extracted | There is no lever to pull. Measured on 2.1.251: all twenty-two hook output schemas were read and not one names an effort, so no event reaches a spawn's; the two that can rewrite a call, `PreToolUse` and `PermissionRequest` through `updatedInput`, rewrite the tool's input, and the Agent tool's input is `description`, `prompt`, `subagent_type`, `model`, `run_in_background`, `name`, `team_name`, `mode`, `isolation` and `cwd`. An invented key does not even fail loudly: both validators drop unrecognised keys. So the only lever is the agent definition's own `effort:`, and covering a built-in means keeping a copy of its prompt, which freezes at the build it was taken from while nothing says so. Generating that copy is refused here on two counts: the prompts are functions with runtime branches rather than contiguous text, so bundle parsing cannot reach them, and the live extraction that does work costs a session per type per build and sometimes declines. Age is reported as the proxy it is, the file's mtime against the build's, and the sentence says only that the file predates the build rather than that its prompt differs, which a timestamp cannot know. Two sources the build reads are not looked in and are named in the README instead: a managed settings directory, which outranks every other, and a session's additional working directories. The two costs the issue attributed to a shadow were measured and do not hold: `Explore` already pays for `CLAUDE.md` at 45,921 tokens with `omitClaudeMd` set, and the `appendSystemPrompt` a copy of `claude` would lose belongs to the `--agent` path rather than the spawn path. `claude` stays off the list anyway, because a file named for it changes that other path too. Sourced in `docs/research/what-an-agent-spawn-inherits.md` | **done** `plugins/ultracode-anywhere/hooks/shadows.mjs` (`SHADOWABLE`, `agentDirsFor`, `shadowsFor`, `shadowLine`), `plugins/ultracode-anywhere/hooks/effort.mjs` (`subagentEffortIn`, `askedForSubagent`), `plugins/ultracode-anywhere/hooks/hook-io.mjs` (`readHead`, `configDirFor`, `homeOf`), pinned by `test/shadows.test.mjs` |

## B. Engine

| # | Decision | Why | Status |
|---|---|---|---|
| B1 | `oxc-parser` for JS/TS, `prism` for Ruby, as the syntactic tier | Measured 4,153 and 6,867 files/sec, enclosing scope free from the walk | **done** |
| B2 | `oxc` runs in a pool of warm **child processes**, never in-process, never worker threads | Measured: uncatchable SIGSEGV from inside `parseSync` at nesting depth, a worker thread does not contain it, and no static pre-screen predicts it. The pool is also *faster*: 8,463-10,563 files/sec vs 3,058 | **done** |
| B3 | Guards per file: 1 MB size, 5s timeout, 1 GB RSS polled after 250ms in flight | Measured; no V8 heap flag caps it. The size cap was 4 MB, and a 35-repository sweep found the 1 to 4 MB band holds only bundles, compiled output and perf fixtures (no hand-written source exceeds 850 KB) sitting at the timeout boundary, flipping between crashed and parsed with machine load; each flip moved the always-loaded overview (A5). A size cap is deterministic where a wall-clock timeout is not, so the cap moved to where the source ends. The timeout stays for the hang the cap cannot catch, and a parse it kills is retried once when the queue drains, alone: the batch it died in was competing for the machine, and a file charged as crashed moves the always-loaded overview. The Ruby bridge owes the same retry for the same reason: its idle window and its wall clock are timers of ours, so a child either of them killed is spawned once more for the files that never answered, while a child that died by itself, an absent interpreter or a fatal from the script is charged on the first attempt and answers the same way twice | **done** `limits.mjs`, `pool.mjs` (`attempts`), `ruby.mjs` (`attempts`) |
| B18 | The raw parser transfer is refused on Windows, whatever `rawTransferSupported()` says | It allocates a 6 GiB buffer per parsing operation to take a 4 GiB-aligned window inside it, and oxc's own note says almost none of those pages is ever touched. That is address space where the kernel overcommits and committed memory where it does not. Windows does not overcommit, this pool runs up to eight workers each parsing, and Windows is the one platform where the pool's memory guard stands down because there is no `ps` to poll. `rawTransferSupported()` asks about pointer width and the Node version and never about the platform | **done** `limits.mjs` (`rawTransferAllowed`), `parse-worker.mjs`. The flag is experimental upstream and the parser answers the same tree without it, so the cost is the measured 3.06x on the parse alone, on one platform. Not reproduced here: this machine is macOS, and the reasoning is oxc's documented allocation plus Windows commit behaviour rather than a run |
| B4 | `prism` stays in-process with an explicit `rescue SystemStackError` | Measured safe | **done** `ruby.mjs`: prism runs in Ruby, so one subprocess and no pool is what "in-process" can mean here |
| B5 | **Never index a disk buffer with a parser-reported offset.** Slice the same in-memory string handed to the parser | Measured: `oxc` reports UTF-16 code units, `prism` reports UTF-8 bytes, 5.4% of real files are non-ASCII. Silent corruption, worse than the crashes | **done** |
| B6 | Containment collapse, keyed within one file | Measured: overlapping matches are *nested*, not duplicate. A range hash catches none of it. Offsets are not comparable across files | **done** |
| B7 | `typescript@5` checker is a second tier, opt-in via `--deep`, never the default. Pin major 5 | Measured: 26x slower, whole-program (narrowing the file set drives unresolved types from 3.1% to 36.2%), and it buys 5 entries. `typescript@7` is the Go port with no JS API | **done** `semantic.mjs`, `semantic-worker.mjs`, `dimensions-semantic.mjs`, `plugins/anatomiya/bin/anatomiya.mjs` (the `--deep` argv, and its refusal on `check` and `pin`), `commands.mjs` (`runScan`, which refuses an absent checker before any work rather than after the parse). `typescript` is an optional dependency, never a runtime one, and the range stays inside major 5: a range admitting 7 turns `--deep` into a silent no-op the day it publishes. One child for the whole corpus, because the checker is whole-program. Every row declares its tier and `dimensionsFor` defaults to the syntactic one, so a caller that does not ask cannot be handed a claim nobody ran. One dimension built, not five: `law_of_demeter` is the only Tier A of them and the tier had to prove it pays before four more were written against it. The other four are named in `docs/dimension-intake.md` as planned rows. A scan option and not a check one: the checker is whole-program, so answering a branch would mean building the corpus at two revisions. It was accepted on `check`, recorded as having run, and never run, which is worse than absent because the report then said the claims were measured. The check names the type-checked claims it did not enforce instead. Not baselined either, and that is deliberate rather than missed: the checker is whole-program, so a pinned pass would mean materialising the corpus at the pinned sha and building a second program over it, at a cost already measured 26x the parse, and the pinned tree carries no `node_modules`, which is the one thing this tier needs on disk, so it would degrade there and suppress everything anyway. A semantic row on a pinned repository prints its counts under gate `semantic-unbaselined`, which says that rather than reporting a greenfield directory |
| B8 | A broken `tsconfig.json` is detected and reported as degraded mode | Measured: typed resolution drops 89.5% to 39.8% silently | **done** `tsconfig.mjs`, `semantic.mjs` (`classifySemantic`, `RESOLUTION_FLOOR`), `reduce.mjs` (gate `degraded-semantic`), `render.mjs`. Two causes kept apart: a config that could not be read keeps its own reason, and a config that read cleanly while resolution fell under 0.80 reports `low-resolution`. No property access at all is neither, and stays ok with a null rate. A degraded tier closes its own rows and touches no syntactic one. Measured on three real repositories: a client app with its dependencies installed resolved 0.895, which is the re-audit's own number; typeorm reported `config-errors`, because it extends `@tsconfig/node20` and the clone has no `node_modules`; supabase resolved 0.293 for the same reason across a monorepo. That is the tier's real requirement and it is unlike anything else here: it needs the repository's own dependencies on disk |
| B9 | The semantic tier reads repository-authored `tsconfig.json`, so the "libraries read no repository configuration" claim is false. Confine `extends`, force the root file list to the corpus, resolve our own deps from the plugin root | The v3 claim was untagged and its own evidence section falsified it | **done** `tsconfig.mjs` (`confinedParseHost`, `confinedCompilerHost`, `FORCED_OPTIONS`). An `extends` leaving the repository is refused and reported rather than followed, the file list is the corpus rather than the config's globs, and every option that writes to disk is forced off with `writeFile` stubbed as a second lock. The lib files come from the plugin's own `typescript`, never the repository's, because a repository can ship its own and reading that one runs its code in this process. Two findings from running it. TypeScript reports finding no inputs as a config error, and that fires on every well-formed config whose globs this row overrides, so treating it as breakage put every repository into degraded mode. And the confinement compared the path the compiler asked for against the root as given, while the compiler asks through `realpath`: under a symlinked path, which is every macOS temp directory, every file the compiler discovered for itself was refused, every import resolved to `any`, and the tier reported 0% resolution everywhere while looking like it worked |
| B10 | **Dimensions run in the worker; only counts cross IPC.** The tree comes back only for the check, which needs line numbers, and only for the files a diff touched | Measured: the walks are 85% of the scan's CPU (1.57ms/file against 0.27ms to parse) and running them in the parent left that 85% on one core, so pool throughput stopped improving past 4 workers on 11 cores. The reply for an 18 KB file went 278,261 bytes to 649 | **done** |
| B11 | **No cap on repository size**: not file count, not total bytes, not cumulative Ruby output. Per-file guards only | The 50,000-file cap did not trim a tail, it set `truncated`, which suppresses every directive in the map. Measured on a synthetic 100,000-file repository: 50,000 files seen and 0 of 720 claims stated, against 100,000 files and 480 of 720 now | **done** |
| B12 | The baseline reads only files that differ from the pinned commit; the rest reuse the corpus parse | Measured: the baseline stage was 6,875ms of an 8,768ms scan, re-reading through one `git cat-file` per file, on a repository where nothing had changed. Now 90ms | **done** |
| B13 | A missing parser dependency fails the scan **and the check**, and a file that answered `ok: false` is counted apart from one that crashed | Measured: with no `node_modules`, every file failed to parse, was charged as parsed, and the CLI printed a clean empty map and exited 0. `/plugin install` does not run `npm install`, so that was the first run a marketplace user got. The fix landed on the scan alone: the check went on folding all four causes into one caveat per file and reporting no findings, which the command file tells the agent means the check ran | **done** `parse.mjs`. One module drives both engines and decides the classification once, so the two callers cannot disagree about it again. Both bridges report an absent interpreter as one fact rather than as every file crashing: `ruby.mjs` marked a missing `ruby` the way a poison file looks, so the check's guard, which reads the flag, could not see it |
| B14 | The parser buffer is named after the file's real extension, never after its language | Measured: JSX is legal in `.js` and the TypeScript grammar reads `<div` as a type assertion, so 727 of react/react's 2,296 files and 3,924 of next.js's 21,358 were counted from a recovered tree, with nothing reporting it. Always asking for the JSX grammar is not the fix: `<string>x` is legal in `.ts` and a syntax error in `.tsx` | **done** `parse-worker.mjs` |
| B15 | A parse reporting syntax errors answers `ok: false`, and its count is reported on its own line | Measured: oxc recovers to an almost empty program, so react/react counted 906 `assertion_style` sites where there are 13,737; prism recovers further and answered a *conforming* site from a method with no body. Both bridges computed the count and nothing read it | **done** `parse-worker.mjs` `ruby.mjs` `parse.mjs` (`classify`) |
| B19 | A `.js`-family file oxc rejects is retried with its Flow types stripped, and the stripper is loaded the first time one is needed and never before | oxc refuses Flow by name, and react is written in it: 287 of its 2,277 files were charged as syntax the parser rejected, so every site in them counted nowhere and the areas holding them were closed as a population that moved. Measured after: 2 rejected, both react-devtools files, and the repository goes from 65 stated claims to 78. The stripper replaces types with whitespace rather than removing them, so the length in UTF-16 code units is unchanged and every offset and line number still lands where it does in the source. That is the unit oxc counts offsets in and the unit a JS string is indexed by, which is what lets the same walkers and the same reported nodes stand (B5). The byte length can move and does not need to hold: `Cafe` with an acute is five code units and six bytes, and five spaces are five of each | **done** `parse-worker.mjs` (`ensureStripper`), `langs.mjs` (`mayHoldFlow`). Scoped to the JavaScript extensions in `EXT_BY_LANG` that are not TypeScript, derived rather than listed a second time: the retry carried its own copy of the list, so an extension added to the corpus table entered the corpus and was never retried. Lazy because the stripper reaches `hermes-parser` and costs about 13 MB resident, which all eight workers would pay on every repository as a top-level import; a repository whose JavaScript all parses never loads it. The trigger cannot be Flow itself, since telling a Flow file from a broken one is what the retry exists to do. A file the stripper cannot read either, or a retry that still reports errors, stays rejected: the retry may not turn a genuine syntax error into a clean parse. A stripper that will not import at all is swallowed rather than failing the run the way an absent `oxc-parser` does (B13), and the asymmetry is the point: with no parser nothing is read and a clean empty map is a lie, while with no stripper a Flow file stays rejected and the scan says so on its own line, which is exactly what it said before this row existed |
| B20 | A tree that came back from the Flow retry does not answer the dimensions that depend on the annotations, and each such row says so on itself | The retry blanks the annotations so oxc will take the file, and three rows then answer off a tree with the thing they ask about deleted. Measured on react before the guard: `explicit_return_type` inverted from 986 of 1213 conforming to 0 of 1213, and the check printed `FIX exported functions declare their return type` next to a line that declares one, quoting the annotation in its own snippet. `type_only_import` reads the type positions the stripper removes. `import_extension` never reads a type, and is blind for a different reason: a type-only import is one of its sites and the stripper deletes the whole statement, so react's 309 of them, not one of which carries an extension, vanished and the file read as more conformant than it is | **done** `dimensions-extra.mjs` (`blindWhenStripped`), `parse-worker.mjs`, `check.mjs`, `reduce.mjs`. Named for the question a maintainer has to answer rather than for the mechanism, since only two of the three read type syntax at all. All three readers drop the row together: the scan does not count it, the check does not enforce it, and the file leaves that row's denominator, because a file nobody asked is not a file that declined. `non_null_assertion` stays, since Flow has no postfix `!` and its zero is the truth. The invariant is a test rather than a promise: one fixture is parsed twice, once with its annotations blanked, and any dimension that moves and is not marked fails it. That comparison found four bugs this row did not predict (C11), so it runs in CI and needs no corpus |
| B21 | Every fact a language owns is one declaration in `plugins/anatomiya/lib/langs.mjs`, and the parse seam routes by the declared engine; no module past the seam names a language | Traced: a third language cost about 38 edit sites in 20 files, five of them copies of the extension facts (the corpus table, the worker's private grammar regex, `scratchExt`, the corpus bare-name regex beside the glob writer's array, the Flow-eligibility table), and the seam's own interface carried `rubyGuards` and a `dimensionsFor(["ruby"])` literal, so every caller had to learn one engine's name | **done** `langs.mjs` (`LANGUAGES`, `declOf`, `language`, `grammarFor`, load asserts), `parse.mjs` (`ADAPTERS`, `guardsFor`), `corpus.mjs`, `areas.mjs`, `scan.mjs` (`guards`), pinned by `test/langs.test.mjs` and `test/parse-contract.test.mjs`. The contract suite iterates the registry, so an adapter is held to the record shape by being declared; its first run found the one override that never worked: the Ruby script's size cap was interpolated from the module constant, so a `maxBytes` handed through `rubyGuards` died at the merge. The dialect retry stays a question about the path alone, because a `.jsx` rel handed in under the `js` id is still a file the retry exists for, and narrowing it per declaration rejected files the old table retried. The two scan gates read the declaration too: the semantic tier and the imports roster ask `langHas` instead of naming languages |
| B22 | The parse worker's body is an in-process module the fork shell hosts | B14, B15, B19 and B20 are all measured incidents inside the worker, and nothing tested it: the body was reachable only through a fork, so every regression was caught end to end or not at all. The shell keeps the read, the IPC reply and the BigInt fallback; tests and the pool cross the same seam | **done** `parse-file.mjs` (`parseFile`), `parse-worker.mjs`, `test/parse-file.test.mjs`. The stripper is a defined-only override on the body, the same shape the Ruby guards take, because simulating an absent optional dependency is otherwise impossible in-process |
| B23 | Whether an engine is ready is one probe carrying one remedy per engine (`doctor`), and installing the node-hosted one is a command of its own (`setup`) | `/plugin install` copies the files and does not run `npm install`, so the first run a marketplace user gets is the one with no parser in it (B13), and nothing in the tool could put one there: the README told them to run npm by hand in a directory it did not name. Three engines were detected three different ways and their remedies were spelled at every printer that needed one, so a machine with no `ruby` on it was told to run npm, which is the one remedy that cannot work. The install belongs in the plugin's own directory rather than in the repository being scanned, since every command runs inside somebody else's tree | **done** `readiness.mjs` (`readiness`, `remedyFor`, `readinessLines`, `pluginRoot`), `commands.mjs` (`runDoctor`, `runSetup`), `plugins/anatomiya/commands/doctor.md`, `plugins/anatomiya/commands/setup.md`. A command of its own rather than something a scan does on finding a dependency missing: that would make every scan an outbound call, and F5 is that it makes none. The argv is pinned at `npm install --omit=dev --ignore-scripts --no-audit --no-fund`, run through `execFile` with `cwd` the plugin root, a 10 minute timeout and an 8 MB output bound. `--ignore-scripts` is the load-bearing flag: without it a dependency's install script runs arbitrary code in the plugin directory. npm that is not on PATH refuses with the one sentence that fixes it, since npm cannot install itself either, and `doctor` exits 0 whatever it found, because a non-zero exit would read as a probe that could not run. On Windows it refuses instead of spawning: npm ships as `npm.cmd` with no `npm.exe`, a spawn resolves an extension-less name against `.com` and `.exe` only, so the attempt answers ENOENT on a machine that has npm installed, and running a batch file needs the shell F5 forbids. The refusal hands over the same argv, after the two short-circuits, so a Windows machine with everything installed still reads `nothing to install` and a dry run still prints the line that is now the by-hand instruction. The premise in the *Why* is wrong and was wrong when it was written: `/plugin install` does run `npm ci` in a plugin's own directory, from a lockfile beside the manifest. This plugin had one until 0.3.0 moved it out of the repository root; B35 puts it back, and what is left here is the command for an install that did not run or did not finish |
| B24 | One supervisor carries the spawn, the bounded stderr, the two clocks and the kill for every child this tool parses with: the warm parse pool, the Ruby stream and the type checker. The four subprocesses that are not a parse stay outside it, each bounded by its own caller: the pool's `ps` poll, the readiness probe's interpreter, setup's npm, and every git read | Three bridges run one, the warm parse pool, the Ruby stream and the type checker, and each wrote the battery with different numbers and different gaps: three stderr accumulators at 2048, 8192 and 4096 bytes, every one able to overshoot by a chunk; three copies of `cwd: tmpdir()`; two of the pinned empty `execArgv`, whose absence once killed every parse worker before it answered and had the scan blame the parser; and one re-armed timeout where the other two carried an idle window and a wall clock. A fourth engine would have written it a fourth time | **done** `child.mjs` (`guardedChild`, `absentInterpreter`, `retryOnce`), `pool.mjs`, `ruby.mjs`, `semantic.mjs`, pinned by `test/modules.test.mjs`, which fails a bridge that guards a child of its own again. The numbers stay with the caller that measured them and only the shape is shared; the cap is exact now rather than one chunk past. Two clocks rather than one, because silence is not the only way a child fails to end: `wallMs` is armed at construction and nothing re-arms it, while `idleMs` runs only from the first `touch()`, and `touch(ms)` takes a window because the checker's first silence is its whole program build and every silence after it is a stall. `settle()` is terminal, so a touch after the run cannot leave a timer holding the event loop open. Parent-only, and it is the far side of the parse worker's walk: it imports `node:child_process`, so nothing the worker reaches may import it (F18), which is why `parse-file.mjs` still classifies its own absent module rather than sharing that one |
| B25 | A type-only import entry never decides the test runner, and still counts toward what a file's `imports` list says it references | oxc marks each specifier `isType`, true for `import type { X }` and for a per-specifier `type` modifier, and neither ever reaches the runtime, so neither can be what runs a file. Measured on next.js: 58 files under `test/` import `@playwright/test` for types alone, so most of "79 playwright" were Jest specs borrowing types. Kept on the `imports` list itself rather than dropped: a file that types against a library still names something it references, which is the question that list answers, and the roster's "most files here import" line reads it for exactly that, a different question from which module runs the file | **done** `facets.mjs` (`jsFacets`, `valueImports`), pinned by `test/facets.test.mjs` |
| B26 | The test runner needs a declared case somewhere in the file, not just a matching import | The label was set by an import edge alone. Measured: cal.diy, 13 of 72 "playwright specs" are 2 config files, 9 support and page-object files importing only types or helpers, and 2 Checkly uptime monitors that import bare `playwright` and call `chromium.launch()`, `goto()` and `screenshot()`, never `test`, `describe` or `it`; storybook's 3 "cypress" files are all `cypress.config.ts` importing `defineConfig`, while its 3 real specs were labelled something else; appsmith, all 12 "playwright specs" are non-tests while the 6 real ones are missed. `TEST_CALLS` already answered "is a case declared" at the file's own top level; the gate reuses it at any depth instead, since Ember nests `test(...)` one level inside `acceptance(...)` and that still declares a case, and a real spec that imports nothing and relies on an injected `cy`, `describe` or `it` never depended on the import in the first place | **done** `facets.mjs` (`jsFacets`, `declaresTest`), pinned by `test/facets.test.mjs` |
| B27 | A bare `test "..." do` call with a description, inside a class or module body, is minitest | `RUBY_TEST_CALLS` already held `test`, but nothing set `minitest` from a call, only `testCalls`, and `test` was deliberately kept out of `RSPEC_CALLS`. Measured on plots2: 23 of 34 mislabelled files use exactly this declarative shape (`ActiveSupport::Testing::Declarative`), no literal `def test_*`, no listed superclass, the dominant cause on that repository, larger than the missing superclass list. Scoped to a class or module body rather than any bare call: the macro is mixed into `Minitest::Test` subclasses and cannot run anywhere else, so a bare top-level `test "x" do` proves nothing. A class body alone is not enough either, because `test` is an ordinary word: an in-house rules engine naming its conditions with the same description-and-block shape read three production authorization classes as minitest specs, in their area's own line and in the repository-wide tests line. So the macro asks for the evidence `def test_*` already asks for, the superclass or the path, plus the `Test` suffix minitest gives its own classes | **done** `facets.mjs` (`rubyFacets`), pinned by `test/facets.test.mjs` |
| B28 | `MINITEST_SUPERCLASSES` is what a repository has been seen to inherit, twelve entries today, and an app-defined superclass one level removed from the list is deliberately left unresolved | The comment claimed "Rails ships six", which was a factual claim about Rails and was false: whitehall inherits `ActionView::TestCase` in 16 declarations and plots2 needs `ActionDispatch::SystemTestCase` and `ActionCable::Channel::TestCase`, none of the six. Added those two plus `ActionCable::TestCase`, `ActionCable::Connection::TestCase` and `ActionMailbox::TestCase`. The comment now says the list grows on evidence rather than defending a total, since replacing one asserted count with another invites the same mistake and a missing entry reads a minitest file as RSpec. Declined the transitive half: plots2's remaining 22 files subclass an app-defined `ApplicationSystemTestCase`, defined once in its own file and referenced from 22 others, a cross-file relationship a per-file facet function has no index for, and resolving one level within a single file would not have fixed one of those 22, since a Rails app requires the base class rather than defines it beside every subclass | **done** `facets.mjs` (`MINITEST_SUPERCLASSES`), pinned by `test/facets.test.mjs`. Transitive resolution across files: not built, see Why |
| B29 | Beaker's `test_name "..." do` macro is its own runner, `beaker` | Puppet's acceptance suite writes every scenario this way under `acceptance/tests/`; the tool recognised only the 2 of 223 files that happened to end `_test.rb`, folding the other 221 into the producer population as code owing a test it already is. Shares the block-plus-argument gate with the Rails macro and RSpec's calls, since it is the same declarative shape; kept as its own flag rather than folded into `rspec` or `minitest`, since neither word is true of a Puppet acceptance scenario | **done** `facets.mjs` (`rubyFacets`, `RUBY_TEST_CALLS`), pinned by `test/facets.test.mjs` |
| B30 | A bare RSpec-shaped or declarative-macro call must carry a description argument, not just a block | `context`, `describe`, `it`, `test` and `test_name` all take a description as their first argument in real use, and the block-plus-no-receiver check alone could not tell a case apart from a FactoryBot attribute: `context { "tags" }` sets a column literally named `context` and takes no argument at all. Measured on empire-flippers/api: 1 of 734 files under `spec/` misfires this way, inflating the RSpec count by one and putting "1 test file" on `spec/factories`; consul has the same shape once. The requirement applies to the whole `RUBY_TEST_CALLS` set alike, since none of them is ever called bare in real use either | **done** `facets.mjs` (`rubyFacets`), pinned by `test/facets.test.mjs` |
| B31 | `minitestByPath`'s directory half trusts only the top of the tree: the file's first path segment, not any segment | `dir.split("/").includes("test")` fired on any `def test_*` under a directory segment named `test` at any depth. Homebrew keeps its whole app under `Library/Homebrew`, and `Library/Homebrew/test/support/helper/test_each.rb` defines `def test_each` for a Sorbet-typed RSpec table-driven test with zero minitest APIs; that repository has no `Minitest::Test` or `ActiveSupport::TestCase` subclass anywhere. The case the rule was built for stays caught: the `app/services` and `lib` files with an incidental `def test_*` have no `test` segment at any position, so the narrower check does not reopen the empire-flippers/api and discourse false positives it was written to close | **done** `facets.mjs` (`minitestByPath`), pinned by `test/facets.test.mjs` |
| B32 | A declaration file is parsed with its own grammar: `.d.ts`, `.d.mts` and `.d.cts` route to a `d.<ext>` grammar rather than the plain one B14 already routes `.ts`, `.mts` and `.cts` to | `grammarFor` maps all three to one grammar, so a `.d.ts` reached oxc as a synthetic `f.ts`, and a bare ambient `export const x: T;` with no initializer, legal only in a declaration, was charged "Missing initializer in const declaration". Confirmed on ghost's `db/index.d.ts`, eslint's `use-at-your-own-risk.d.ts`, two babel files and next.js's `packages/next/cache.d.ts`, a real shipped public API file. Narrower than it first looked, and the refutation is worth keeping: full populations on prisma (267 files), supabase, storybook, cal.diy and canvas-lms parse identically either way, because the three extensions already share a grammar, so a `.d.ts` only mis-parses when its own content is illegal under plain TS | **done** `langs.mjs` (`grammarFor`), pinned by `test/langs.test.mjs` and end to end by `test/parse-file.test.mjs` |
| B33 | A `.js` or `.cjs` file oxc rejects for a top-level `return` is retried with each reported `return` keyword blanked in place, same length, and reparsed. `.mjs` and the TypeScript extensions are never retried this way | `parseOptions = { sourceType: "module" }` for every file charges a valid CommonJS file using a guarded top-level `return` as a syntax error. backstage: 1 file, a standard OpenTelemetry preload. next.js: 5 files, all `node --check` clean, two of them vendored copies of the real `bull` and `bullmq` packages. **`sourceType: "script"` does not fix it**, confirmed by parsing the same fixture both ways: plain Script grammar disallows a top-level `return` exactly as module grammar does, and only Node's own CommonJS function wrapper permits it. Reproducing that wrapper would nest the whole tree a level deep and break every walker that reads `program.body` as the module's own statements, so the retry blanks the keyword instead: oxc's error carries the exact byte span of each illegal `return`, checked equal to the literal text before it is touched, and any other error in the same file leaves it rejected rather than partly patched | **done** `langs.mjs` (`mayBeCommonJS`), `parse-file.mjs` (`withoutTopLevelReturns`), pinned by `test/langs.test.mjs` and `test/parse-file.test.mjs`. Retried only on failure and only where the dialect could apply, the same shape B19's Flow retry already uses, so a file that parses clean pays nothing. `.mjs` is excluded because Node always loads it as ESM; the TypeScript extensions because TypeScript rejects a top-level `return` as source before any module format is chosen. Deliberately outside `stripped`/`blindWhenStripped` (B20): this retry blanks a keyword rather than a type, so a file it recovers keeps every row Flow-stripping would have cost it |
| B34 | A row may declare `needsTypeSyntax`, and all three readers drop it for a file that cannot carry a type annotation: the parse does not run it, the fold leaves the file out of its denominator, and the check does not enforce it | `explicit_return_type`'s own `blind` field already said a plain JavaScript file has no annotation to find, and the row counted those files anyway: `export function total(rows): number` is a SyntaxError under Node, so every site in a `.js` file was a violation nobody could act on. The extension is the fact and it belongs in `plugins/anatomiya/lib/langs.mjs` beside `dialect` and `commonjs` rather than in the row, because `.js` and `.ts` share one language id here and nothing downstream can tell them apart. Dropped at the selection seam rather than inside `run`, exactly as `blindWhenStripped` is: bailing inside `run` would leave the file in `langFileCount`, so the applicability gate would read the predicate as narrow on a mixed directory, which is the failure C3 and C4 exist to stop | **done** `langs.mjs` (`typed`, `carriesTypeSyntax`, and the registry assertion that a language cannot declare type syntax for an extension it does not own), `dimensions.mjs` (`assertDeclaredFields`), `parse-file.mjs`, `reduce.mjs`, `check.mjs` |
| B35 | The plugin ships its own `package-lock.json`, and a plugin that declares dependencies without one is refused | Claude Code installs a plugin's dependencies itself. Read off 2.1.251: the loader reads the plugin root with a non-recursive `readdir` and runs `npm ci --ignore-scripts` there, at a 60 second cap, where a `package.json` and one of `bun.lock`, `bun.lockb`, `npm-shrinkwrap.json` or `package-lock.json` sit together; `yarn.lock` and `pnpm-lock.yaml` are refused by name because their resolution-time hooks run around `--ignore-scripts`; a root with the manifest alone is passed over with nothing logged. 0.3.0 moved the plugin out of the repository root and left the marketplace's lockfile behind, so every version from 0.3.0 to 0.4.2 installed with no parser in it. The evidence was on the machine and read the other way: of 26 cached versions, the 20 before 0.3.0 have `node_modules` within a second of `lib`, because the install was still firing, and only the 6 after it are minutes later, which is a separate `setup` run each time | **done** `docs/research/what-a-hook-reads-and-what-an-install-runs.md` records the doc and the build read, `plugins/anatomiya/package-lock.json`, `scripts/validate.mjs` (`dependencyInstall`, `lockfileDrift`, `INSTALLED_FROM`), `scripts/plugin-lock.mjs` (`seedFor`), `.github/workflows/ci.yml`. Two lockfiles for one dependency set drift the moment npm resolves a range to something newer, and then the suite is green against a parser nobody running the plugin has: on the first build of this one they already disagreed, `flow-remove-types` 2.329.0 against the root's 2.328.0. So it is seeded from the root's own resolutions rather than resolved afresh, `validate` refuses any package the two resolve differently, and `--check` rebuilds it offline, since a gate that reaches a registry fails on the network rather than on the repository. A CI leg copies the tracked files, runs the loader's own command on them and asks `doctor`, because nothing else here would notice: every other job installs from the marketplace root's lockfile, one directory up and invisible to the loader. On Windows the seed check says why it did not run and passes, the way the shipped-set check does and for the same reason: both spawn npm, which is a batch file there with no `.exe` for a spawn to find, and the Linux job is where either one gates. `setup` stays, for the install that did not run or did not finish, and `doctor` leads with one line where the directory holds nothing rather than naming three engines that are absent for one reason: the rows below it then keep what was wrong with each, which differs, and drop the remedy, which does not, or the one sentence appears four times and the report reads as four faults again. Nothing at all, and deliberately not an install that ran and stopped short: the loader kills one at sixty seconds and what that leaves is some of the packages, where the rows are the better answer because they say which. Asked of the whole way up rather than of the directory beside the manifest, because that is where node looks: this marketplace declares its plugins as workspaces, so npm hoists every package to the root and the plugin's own directory never holds one, and a contributor whose engine failed to load was told nothing was installed and pointed at a place that will never hold it. A resolution npm could not hoist is re-keyed to the plugin's own root rather than dropped with its workspace, and the drift gate compares against that one rather than the copy at the top, which belongs to whichever consumer won the hoist |

## C. The counted-claim model

| # | Decision | Why | Status |
|---|---|---|---|
| C1 | Every dimension carries `applicability`, `candidates`, `conforming`. Ratio over **candidates**, never over file count | Measured: file-counting flips 10 of 39 verdicts, in both directions. It hides real conventions *and* manufactures false ones | **done** |
| C2 | A dimension with no writable applicability predicate does not ship | 8 of 8 were writable; 3 of them under-count applicability, which is the dangerous direction. Re-measured over expressjs/express, sidekiq/sidekiq, vuejs/core and mastodon/mastodon, across the 37 of 40 rows that produced a slot in any of them, since which rows those are depends on which repositories are asked: 6 precise rows sit under a 0.25 median applicability share and each names a construct that is simply rare, so the share ranks rows to open and decides nothing. The under-count the row is about was found by the witness rather than by the share, and by widening it: `service_result_shape` refused every receiver, so `def self.call`, the commonest Ruby service entry form, was not a site at all and the ratio was stated over whichever subset writes instance entry points. One witness per sentence would not have caught it either, and neither would a sixfold narrowing of `optional_chaining`, the row this contract already names as the measured false-convention case. A sentence naming a set is only as true as the members somebody wrote down | **done** `dimensions.mjs` (`assertApplicability`), `pairing.mjs`, `dimensions-ruby.mjs`, `test/applicability.test.mjs`, `scripts/audit-applicability.mjs`. Writable was already true. What nothing held was that the predicate a row ships agrees with the sentence anybody would write for it: `applicability` is whatever `run` emitted, so a predicate seeing a tenth of its own construct gives 1.0 over four files and reads as a strong convention. Each row states the files it can speak about and, where it is partial, what it cannot see, tied to `precision` so the marker and the reason cannot disagree. The witness is per form rather than per row: every shape the code treats differently gets a source, because one source proves the sentence names something and nothing more. A closed table of names is one shape, so it is covered from the other side, each member driven through the predicate against a list written out in the test: dropping `next-intl` from the module list changes no shape and makes every file in such a repository inapplicable, and reading the expectation off the table would agree with it by construction |
| C3 | Render `applicability` beside the area file count on every line | A wrongly narrow predicate gives ratio 1.0 over a small set and reads as a strong convention. This is the only thing a human can audit it with | **done** `render.mjs`, `facts.mjs` (schema 6). The rendered line carried the number a reader audits by eye; the machine record did not. `langFileCount`, the files a dimension could speak about rather than every file in the area, shaped the gate and was then dropped before the record was written, so the share that separates a narrow predicate from a rare construct could not be read back at all. The record carries this pass's count beside this pass's `applicability`, which is the pair that divides; the gate reads the baseline's where there is one, and storing that instead would put a numerator and a denominator from two populations on one line. Auditing one line at a time is what a reader does; auditing every row that produced a slot, across four repositories, needs it on disk |
| C4 | Applicability floor: the stricter of two, with the share capped at three roots, `applicability >= max(R, min(ceil(0.25 * F), 3R))` where `R = ceil(sqrt(F))` and `F` is the files the dimension can speak about | v3 promised this floor and gave it no number, no gate, no storage, no test. Each floor alone is wrong: the root asks for more than a quarter below sixteen files, where a quarter of a small directory is one or two files, and the share is what holds above it. Measured: the root alone asked 11 files of 120, and a 120-file area where 11 files used `?.` and 109 read absent values without it stated the claim over all 120 | **done** `reduce.mjs` (`GATES.minApplicabilityShare`, gate name `applicability`). `F` is the files the dimension could speak about, and a file nothing was read from is not one of them: `applicability` counts files that produced a site, so a denominator over every file of the language divided one population by another. Measured over a 35-repository corpus, on the repositories that hold syntax this tool does not take: react is written in Flow, 287 of its 2,277 files are rejected, 55 of its 122 areas hold at least one and one area holds nothing else, and its stated claims go from 36 to 65 once the denominator counts only what was examined. Webpack 80 to 92, next.js 162 to 172. None of those claims was suppressed because a predicate was narrow. An obligation is counted the same way, since `applyPairings` skips a record that is not ok and an unread producer answers nothing. The count is taken off the records handed in rather than off the area's file list, which is what makes `applicability <= langFileCount` hold in both passes: the pinned pass reduces records keyed by the pinned paths against an area holding today's, so reading `area.files` dropped every file renamed since the pin, and twelve renames with no content change took a slot from suppressed to stated. That is the laundering the pinned population exists to refuse, arriving through the denominator The share is capped because it was measured at 120 files and never tested at 1,531: it grows linearly with the area while the risk it guards does not, so on a single 1,531-file `db/migrate` it asked for 383 files and made any construct rarer than a quarter of the directory unstateable however perfect. `table_primary_key_declared` there is 152 of 154, ratio 0.987, bound 0.954, and it is the failure the cap was written for. It was not the only one: the uncapped floor closed two slots in that map, the other being `zone_aware_time` at 63 of 63 over 22 of 117 files in one spec directory, which is a small area rather than a large one and is the case the root half of the floor is for. Three roots is the smallest whole factor that leaves the measured band alone: the first area size where the cap changes the answer is 157, above every area the share was measured on, and at two roots it is 73 and `floor(120)` falls from 30 to 22, which moves the answer on the very area this row was written from. Still monotone in the area size, checked to 200,000. The floor is one exported function, `applicabilityFloor`, because two tests re-implemented it locally and would have stayed green through a broken change. `scripts/audit-applicability.mjs`'s own `NARROW = 0.25` is a ranking threshold for which rows are worth opening, not this gate, and the two must not be unified. |
| C5 | Dimensions carry `precision: precise \| partial`. A `partial` dimension can never reach top severity | 3 of 8 measured predicates are partial | **done** `dimensions.mjs` (`assertPrecision`), `check.mjs` (`severityFor`), `pairing.mjs`. The cap was enforced; carrying the field was not. `severityFor` reads `precision !== "precise"`, so a row that forgets it or spells it `"Precise"` is capped by the same comparison a deliberate `partial` is, silently, and reads in the map as a claim nobody marked. Checked at load rather than in a test, because the registry is assembled from six files and the obligations were the sixth: they are not in `ALL_DIMENSIONS`, and every per-file precision test had missed them |
| C6 | A dimension may state its inverse only where it carries a hand-written `counterClaim`. Refusal is an explicit `null`, never an absent key. Both sides run the same gate battery, and every gate but the three numerators is polarity-free | 144 dimension slots across ten repositories hold a strong inverse and state nothing. A machine negation names nothing for the agent to write, and writing the sentence out loud is where an inverse that is really a defect becomes visible. 13 of 32 dimensions are eligible; a counter needs the same measured cross-repository spread the claim does | **done** `reduce.mjs` (`judge`), `facts.mjs` (`statedSide`, the record the polarity fields are written into, and the schema they arrived in), `check.mjs` |
| C7 | A file-to-file obligation is a dimension whose site is the file, counted over the corpus rather than over a tree. Every such row carries `companionsElsewhere`: producers whose companion is missing where the predicate looks, which C9 now learns rather than declares and whose namesake exists elsewhere. A repository is asked the question only for a companion suffix it actually uses | Every other dimension asks a question inside one file, and a review comment is as often about a missing companion as about syntax. Two things a first cut got wrong and the measurement caught. A ratio of zero has two meanings, so the audit count is what separates them: on alphagov/whitehall the `app/models` area scores 0 of 160 and 117 of those models have a test one directory deeper, which without the count reads as a repository that does not test its models. And producers exist whatever a repository tests with, so counting both the RSpec and the minitest row put a line that can only read zero into every Rails map | **done** `pairing.mjs`, `reduce.mjs`, `scan.mjs`, `check.mjs`, `render.mjs`, `facts.mjs` (schema 6). The namesake count was rendered and not stored, so the map printed a number the record it is derived from could not reproduce. Stored beside the two counts behind "and N more", which had the same shape, and the version moved for them: two records both calling themselves 5 with different fields is the coincidence C10 replaced |
| C8 | A dimension whose claim belongs to a framework carries `framework`, and is offered only where the corpus shows that framework | Measured: `zone_aware_time` has no counter-claim, so off-Rails it can only ever print zero, and did: 123 sites on Homebrew, 197 on puppet, 97 on fastlane, 96 on chef, plus a NIT delivered onto a plain-Ruby branch. The signal is `app/models/`, `db/migrate/` or `config/application.rb` in the corpus, which separates 14 Rails from 5 plain across 19 repositories; read from the corpus, because rubocop's only `app/models` files are three fixtures the corpus drops. Only the two dimensions that cannot see their own context are marked. The check asked its own question and never asked which frameworks are here, so the measured NIT on a plain-Ruby branch outlived the fix on the scan | **done** `dimensions.mjs` `corpus.mjs` `check.mjs`. The registry no longer refuses a framework row on a language the oxc worker parses: the reducer and the check are the arbiters, both selecting through `dimensionsFor` with the frameworks the corpus shows, so the worker's unfiltered hits are read by no slot and no finding. `adoptedCapabilities` is the one reader that takes those hits without selecting, and it is framework-blind, so a row may not carry `framework` and `capability` at once: otherwise off-framework hits would vote three files' worth of adoption for the capability rows the whole repository is then offered |
| C9 | An obligation learns its companion root from the corpus, with the declared pair as the prior | Measured: alphagov/whitehall keeps model tests under `test/unit/app/models` and scored 0 of 160 with 117 namesakes one prefix away, now 117 of 160; discourse answers its controllers in `spec/requests`, which no hardcoded pair reaches. Learned on the path tail rather than the basename, because basename matching was ambiguous for 388 of one repository's services. A tie learns nothing and the declared pair stands, or an obligation is decided by a filename | **done** `pairing.mjs` |
| C10 | The facts record carries a schema version, and a reader refuses a version above its own rather than reading the fields it does not know | The version was written by `write.mjs` and read by nobody: the check took whatever shape was on disk. Fields have moved twice already, at 2 for the polarity keys and at 3 when the single `glob` became `globs`, and a plugin update is not atomic with the map a repository has committed. Reading a newer record positionally enforces a convention nobody stated, which is the one failure the whole counted-claim model exists to avoid. An older version stays readable, because the compat rules for 1 and 2 are written down and tested | **done** `facts.mjs` (`schemaProblem`, `readFacts`, `FACTS_SCHEMA`), `scripts/audit-applicability.mjs`. The rule is one function now rather than a shape each reader re-tests, because a second reader arrived: the audit read a record field by field and reported a file that was not a map at all as a clean empty table. Which of the two refusals it is comes back tagged rather than as a message, or rewording the sentence would silently move the branch |
| C11 | A dimension reads the value inside its type wrappers | `null as any` is null, and the walkers matched on the node type of the expression they were handed, which is a `TSAsExpression`. `return null as any` was not counted as a return of null: react writes it 24 times and vscode 20, every one a conforming site nobody counted. `x ?? ([] as Foo[])` is a default taken with `??` and was not counted as one either, because the right side was not a literal by node type. `satisfies`, `!` and `<T>x` wrap the same way, and oxc preserves parentheses, so `(null as any)` is two wrappers deep | **done** `walk.mjs` (`value`), read by `dimensions.mjs` and `dimensions-extra.mjs`. Peeled only where a value's shape is read. It lived beside one dimension file first, and `error_shape` in the other kept matching the raw argument, so `return { ok: true } as Result` stopped being a conforming site while every throw beside it still counted. The leaf every dimension file already reaches is where it belongs. `non_null_assertion` reads the node before anything peels it, because there the wrapper is the site rather than something in front of it. Found by comparing one file's counts against the same file with its annotations blanked (B20) rather than by reading the walkers: the two answers differed, and the cast was why |
| C12 | An ambient declaration is not module state, and neither is a binding inside a namespace | `declare const x: number` binds nothing at run time; it describes something declared elsewhere. It was counted as a module-level const, and `var` inside `declare module 'x'` was counted as module-level too, though it is scoped to the block. Both moved the ratio on a claim about mutable module state | **done** `dimensions.mjs` (`module_state_const`). oxc marks the first with `declare` on the declaration and the second is any `TSModuleDeclaration` ancestor. The ambient case also counted two ways for one file, since the stripper removes the declaration outright, which is how it was found |
| C13 | A learned-class dimension states the plurality class of its own sites; a tie learns nothing and produces no slot; a class that moved since the pin closes the slot as `learned-moved` until a human re-pins | Naming is the measured divergence class no fixed sentence can carry: the same row must state kebab-case in one repository and PascalCase in another. The plurality is decided per pass, so the pinned counts answer a different sentence than today's the moment the class moves, and letting either side state across that move would enforce a sentence nobody measured. The check enforces the stored class, never a hit's own placeholder flag, and a brand-new file breaking the area's filename class is a finding, because that site is the name itself and no tree walk sees it | **done** `dimensions-naming.mjs`, `reduce.mjs` (`learnClass`), `check.mjs`, `facts.mjs` (schema 10, `learned`) |
| C14 | A capability-routing row is offered only where at least three examined files already route through a wrapper; the filename vocabulary is the check's fallback where no map exists | The most cited agent symptom is framework-default code instead of the repository's wrapper, and the wrapper is repository-specific by construction. The vocabulary alone over-offers (a repository holding a `config.ts` nobody imports has a name, not a habit) and also under-offers: the Ruby logger conforms on `Rails.logger` receivers no filename carries, so requiring the vocabulary silenced the row on the repositories it is for. Adoption alone decides, and a line that can only read zero never ships (C8). Three adopting files is the same bar the concentration gate uses for evidence being worth counting. Keys are split per engine (`route_logging` beside `logger_over_puts`) because one key is one predicate, and the two engines read different trees; adoption is counted per repository across both engines, deliberately, because a wrapper habit is the team's rather than a language's | **done** `dimensions-capability.mjs`, `dimensions-ruby.mjs`, `corpus.mjs` (`capabilitiesIn`), `dimensions.mjs` (`adoptedCapabilities`), `scan.mjs`, `check.mjs` |
| C16 | A framework is a profile in `plugins/anatomiya/lib/frameworks.mjs`, and `frameworksIn` folds over the profiles as a set | The fold returned a `Set` and every consumer treated it as one, but the body broke out of the loop after the first hit behind a `lang === "ruby"` gate, so a corpus holding two frameworks could only ever be one of them, and the check's probe carried its own copy of the gate as a language literal. One profile carries its name, its language and its measured signal; the fold asks every profile until it answers, the same shape `capabilitiesIn` already has; the check asks `couldSignal` instead of naming Ruby; and the closed name set the `framework` field is asserted against is the profiles', so the two cannot drift. Deliberately not built until a second framework exists: a full profile owning its rows, its obligations and its runtime packages, because one adapter means a hypothetical seam and the nine Rails obligations are offered by companion-suffix use, not by framework, which this row does not change | **done** `frameworks.mjs` (`FRAMEWORKS`, `couldSignal`), `corpus.mjs` (`frameworksIn`), `dimensions.mjs`, `check.mjs`, pinned by `test/frameworks.test.mjs`. The second framework this row waits for may now be a JavaScript one: a framework row loads on any language, whichever engine parses it |
| C15 | Every registry row carries `kind`, one of three, and the whole load battery runs over all three lists, pairings included | A row was one of six shapes told apart by which optional fields exist, so every reader re-derived the union: H14's one boolean cost four modules, H12 shipped learned rows the map stated and the check silently did not enforce, and `PAIRINGS` passed through three of the five asserts, so a pairing spelling its tier `"Syntactic"` would have loaded and been dropped by every reader without a word. The kind is how a row is counted, a tree walk, a corpus fold or a companion match; a tree row is stamped at load rather than spelled per row, and the newest fields (`groupedSites`, `noneClaim`, `learnedFromSource`, `capability`, `framework`, `blindWhenStripped`) are held to shape the way the first five always were. The check finds its corpus rows by kind, never by a key literal, so a second corpus row is enforced the day it is declared. Two things deliberately not unified: the reducer's grouped fold and the check's fold share `groupKey` and the one-vote-per-body rule and sit on their own sides of the counts/findings split, and the facts record keeps inferring a slot's kind from which counts are present until a reader needs more, which is a schema bump away | **done** `dimensions.mjs` (`KINDS`, `stampKind`, `assertDeclaredFields`, `assertRegistryRows`), `pairing.mjs`, `check.mjs` (`CORPUS_ROWS`, `filenameFinding`), pinned by `test/dimensions.test.mjs` |
| C17 | The three row lists are one registry, and a reader that wants more than one asks for it rather than spelling the union | A row is declared in `ALL_DIMENSIONS`, in `PAIRINGS` or in `NAMING_CORPUS`, and ten readers across `lib/`, `scripts/` and `test/` each spelled a different subset of the three, so one that missed a list read a shorter registry without a word and a fourth list would have to be added to all of them. C15 put the load battery over all three lists and it still ran from inside `dimensions.mjs`, which is the module the parse worker imports | **done** `registry.mjs` (`REGISTRY`, `REGISTRY_KEYS`, `rowsOfKind`, `rowsForLangs`, `rowByKey`, `assertUniqueKeys`), read by `reduce.mjs`, `check.mjs`, `model-defaults.mjs` and the scripts. The battery runs here, once, over the union, and `stampKind` writes the kind in place, so a row read through this module is stamped whichever list declared it. One question a per-list battery cannot ask is asked here: one key names one row, or two rows print the same three numbers twice and a lookup reaches whichever it found first. A misspelled kind throws rather than answering with no rows, which is a reader switched off in silence. The parse worker must not reach this module and the import graph is walked to hold it: the worker runs the tree rows off `dimensionsFor`, has no program to run an obligation or a filename row against, and eight forked children pay for whatever it imports (F18's reason). A second walk holds every `lib/` module that branches on a row's `kind` to importing the module that stamps it |
| C18 | An obligation learns its producer root the same way C9 learns the companion root, scoped to one package, and a companion may vote only from inside that same package | Measured on decidim, a 28-gem Rails engine monorepo: `PAIRINGS` hardcodes literal top-level producer paths, and `companionOf` and `companionRoot` both gate on `rel.startsWith(`${from}/`)`. Only the companion side was ever learned. Every gem nests its `app/` one level down, so the producer set was empty for 7 of 9 pairings repo-wide, "ships with a" appeared in none of 339 generated files, and a brand-new spec-less model passed `check` with 0 MUST-FIX, 0 FIX, 0 NIT. The one obligation this tool enforces rather than merely states went silent on exactly the repository shape it exists to catch, and nothing reported that it had not applied | **done** `pairing.mjs` (`companionRoot` takes a prefix, `packagedPairings`), pinned by `test/pairing.test.mjs`. A package is a prefix under which `from` genuinely occurs in the corpus, found by scanning the corpus for the declared producer path appearing below the root rather than at it, with no vote: a prefix either holds an `app/models` or it does not. C9's vote still runs, one step later and unchanged, to find that package's own `to` root. The empty prefix is the flat repository and is exactly C9's original computation, pinned byte-identical against a flat corpus so nothing about that shape moves. A companion may vote for a package's root only when it sits under that same prefix, so decidim-admin's spec of the same basename can never win the vote for a decidim-core model. The root is a package too, the one holding everything not inside another: restricting only the named prefixes left it seeing every spec in the repository, and a nested package's spec then satisfied a root-level model with none of its own. A package is named by a producer under it and never by a companion, or a repository mirroring its source tree under the test root reads that mirror as a package holding every spec it answers, which is whitehall's `test/unit/app/models` exactly. Falling back to the declared pair mirrored under the package prefix when nothing lines up inside it keeps the same honest zero C9 established, now per package rather than per repository |
| C19 | The exported-name naming row is judged per declaration kind: `exported_symbol_case` (a function, or a variable not bound to a class expression), `exported_class_case` (a class, or a variable bound to one), and `exported_type_case` (an interface, a type alias, or an enum). Each learns and states its own casing | Verified by running the real CLI, since reading the code alone does not show which population a claim ends up speaking for. On typeorm, in an area whose export population is 100% classes, a planted `export function isValidPost(post: Post): boolean`, camelCase and correct by every convention and matching that repository's own recorded precedent `filterByCteCapabilities`, was flagged `FIX ".../entity/Post.ts":19 exported names are PascalCase`; a planted `export function IsValidPost(...)`, wrong by every convention, passed with 0 findings. The only defect measured across 35 repositories that made `check` wrong in both directions on one directory. Confirmed from the other side on mastodon, where the conflated row stated "exported names are camelCase, 509 of 518" and listed idiomatic PascalCase type exports as violations though every runtime export really was camelCase; the tool already got this right in that repository's `api_types`, where types dominate, so the defect was the shared bucket rather than the casing rule. `model-defaults.json`'s own calibration recorded the same conflation and could not resolve it: 12 samples, `classCounts: {PascalCase: 22, camelCase: 10}`, no majority either side of even | **done** `dimensions-naming.mjs` (`exportedSites`, `exportedPopulation`), `model-defaults.json`, pinned by `test/naming.test.mjs`, `test/applicability.test.mjs` and `test/fixtures/counter-pins.mjs`. A class keeps a row of its own rather than joining the type row, though both are conventionally PascalCase: it is the one PascalCase declaration that survives a Flow retry with its name intact, so folding it in would make `exported_type_case`'s `blindWhenStripped` false for the sites it borrowed. `exported_symbol_case` keeps its key and its claim text, narrowed to the population that was always camelCase, so the picker and the A/B plumbing that read that key by name are untouched. A default export is read when it names what it declares, since one class per file exported that way is the ordinary shape for a component: skipping the statement whole left a repository of five default-exported PascalCase classes stating, through its one named-export outlier, the opposite of what 83% of its classes do. An anonymous one binds no name and is still no site. A specifier (`export { foo as bar }`) is not resolved to the declaration it renames and answers none of the three: a real narrowing from the row it replaces, not a gap papered over. The three defaults are reset to an unmeasured seed rather than carrying the old counts forward, because a default measured over a conflated population is evidence for neither half; re-measuring needs `scripts/measure-defaults.mjs`, which spends model calls and is run by hand |
| C20 | An export statement counts as a naming site only where it is a direct top-level statement, never one nested inside an ambient module or namespace declaration | Extends C12's principle, an ambient declaration is not module state, to a site C12 did not cover. `declare module "legacy" { export var legacyGlobal: number }` is scaffolding describing an external dependency's shape, not a binding this file creates, and the Flow retry deletes the whole block. The three split naming rows first walked the whole tree, matching the row they were split from, which counted `legacyGlobal` as one site before a retry and zero after: caught by `test/stripped-consistency.test.mjs`, the same battery C12 cites, and reproduced first as a unit test naming the exact shape before the fix | **done** `dimensions-naming.mjs` (`exportedSites` reads `program.body` directly rather than walking the tree), pinned by `test/naming.test.mjs`. A real ES `export` is always a top-level statement outside this one ambient exception, so the narrower read changes nothing on ordinary code |
| C21 | A predicate does not count a construct whose conforming form does not exist. Where the language decides it the exclusion is in the predicate; where the area's own learned class decides it the exclusion is in the fold, because only the fold knows the class | Eleven rows, each executed rather than reasoned about, each asking for code that does not compile or does not run. TypeScript: an overload set has no arrow form, so `function_style` made the only available syntax the first violation in a 280-of-280 area; `const container: HTMLDivElement` with no initialiser is a SyntaxError and assigning to a const is a TypeError, so `module_state_const`'s two MUST-FIX on the ordinary React Testing Library setup asked for a file that does not compile, and measured over a 2,486-file repository 0 of its `let` bindings were initialised-and-never-reassigned while 9 of the 17 the row called violations could not be fixed as asked; an interface inside `declare global` carries the identity of the thing it augments, so `IWindow` compiles and the merge silently stops, which was 24 of that row's 40 non-conforming names; `x?` is TS1109 so a bare `x!` has no optional form, and `o?.r = 3` is TS2779 so neither does one in a write position, a `new` callee or a tagged template's tag; `a \|\| b ?? {}` is TS5076 so a mixed chain cannot be flipped one operator at a time; a plain `.js` file cannot carry a return type at all; a JSX element name is a value read, so `import type` on a component is TS1361; and React refuses `return null` from an effect in its own words. Ruby: `[]=` cannot take keywords at all, `EachValidator#validate` calls `validate_each` positionally, and Sidekiq reaches `perform` through a splat of a JSON array where a splatted Hash is never keywords, which was 76 of that row's 297 violations; ActiveRecord raises `Cannot add a foreign key to a polymorphic relation`; a Sidekiq job that returns instead of raising is acked as successful, never retried and never in the dead set; and Rails' `change` auto-inverts only a closed set of schema commands, so a migration the schema-only row already flags cannot answer the reversibility row, which was 88 of its 121 violations. A spread onto a host element is the same shape from the other end: something has to reach the DOM, and a rest element is by definition the props nobody named, which was 11 of that row's 16 violations at 69% and the fastest way to teach somebody to ignore the tool. A directive comment is the same argument about text rather than syntax: `eslint-disable`, `@ts-expect-error` and a triple-slash reference are not documentation on either side of a doc-comment claim, and obeying the finding deletes a directive and breaks the build | **done** `dimensions.mjs` (`module_state_const`, `optional_chaining`), `dimensions-extra.mjs` (`function_style`, `non_null_assertion`, `nullish_default`, `absent_is_null`, `type_only_import`, `doc_comment_style`), `dimensions-jsx.mjs` (`spread_on_component`), `dimensions-naming.mjs` (`interface_prefix`, `function_naming_case`), `dimensions-ruby.mjs` (`keyword_params`, `service_result_shape`, `zone_aware_time`), `dimensions-rails.mjs` (`migration_reversible`, `column_null_declared`, `reference_foreign_key`; the polymorphic exclusion is narrowed by C34, which reads the option Rails reads and lifts it for the list form that declares a key), `walk.mjs` (`optionalChain`, the one grammar rule both `?.` rows ask). Two of the eleven were left alone on measurement rather than fixed, and the reasons are in C28. One consequence is worth knowing before somebody reads the audit: excluding components narrows `function_naming_case`'s population, and its median applicability share on a components-heavy front end reads 0.429 against main's 0.889. The exclusion alone takes it to 0.125, which is under the 0.25 the audit flags at, and C25's narrowing lifts it back over the line by cutting the denominator with the numerator, so the shipped build is not flagged and the audit's own footer does not name the row. It is narrow on purpose, because in a components directory a function's name is JSX's to decide, and the row still states in 17 areas of that repository, 13 of them through the prior D8 gives it. Two of the sixteen needed a second pass. `absent_is_null` guarded only the `undefined` arms, so a `return null` in an effect stayed a site of a row whose own predicate says an effect callback is not one; both arms go now, which is what the reason the predicate gives already argued, and no corpus repository holds such a site. And a component was recognised only by a bare `return <jsx/>`, so one rendering through a ternary or an `&&` stayed in the naming vote: in a directory of 40 components beside 40 helpers that flipped the learned class to camelCase and the check then asked for a lowercase component name, which is a host element and is the impossibility this row exists to stop. Four corpus repositories hold 62, 35, 33 and 33 module-level PascalCase functions that read that way. The exclusion asks every value the function hands out now, through a ternary, an `&&` or a yield, rather than only a bare return, and stops at a nested function, so a `renderRows` returning `items.map((i) => <li/>)` is still the helper it is named as. Asking the whole body instead was measured and refused: a function that builds an element and returns a string made from it hands out no JSX, and ten such functions across two repositories left the population with nothing announcing it, which is the under-count direction the applicability contract exists to catch. `exported_symbol_case` reads the same rule, because excluding a component on one row and not its twin left the same declaration asked for a lowercase name by the other sentence |
| C22 | `class_base` excludes a class whose superclass is one of Ruby's own exception classes, from a closed table plus the `Errno::` prefix, and the fold excludes a site whose own qualified name is the class the area learned | Conforming makes an error class a non-`Exception`, and `raise` then answers `TypeError: exception class/object expected`. Six areas of one repository learned `ActiveInteraction::Base` with a perfect baseline, so the next error class anyone added to any of them was a MUST-FIX, and every Rails service directory grows error classes. The self-base half is the same absurdity printed on the page: `app/models` states "classes here inherit ApplicationRecord" and lists `application_record.rb` among its own exceptions, and `class X < X` is a NameError. Together they were 37 of that row's 75 violations. Only the language's own exception classes, so a repository that gives its errors a base of their own keeps every subclass and the row still states there; the superclass decides it and never the name, because `class ClassifyTransactionError < ActiveInteraction::Base` is a real class. The learned-class half matches `self === learned` or `self` ending in `::learned`, because a subclass inside `Api::V1` writes the bare `BaseController`; matching the last segment of both would drop `class Base < ActiveInteraction::Base`, which is also a real class | **done** `dimensions-ruby.mjs` (`isRubyError`, `qualifiedName`), `walk.mjs` (the hit carries its own name across the worker boundary), `reduce.mjs` (`isLearnedItself`), `check.mjs` (`conformingOf`, because the check re-runs the predicate and would otherwise still fire) |
| C23 | The omission is a site: a filename spelling none of the four naming classes, and a top-level class naming no superclass at all | The one-sided shape H16 fixed for `module_include`, twice more. `classifyBasename` answers null for two different things and only one of them is not a site: a name matching every class cannot disagree with any, and a name matching none disagrees with all of them, so `TMP_PROBE_UPPER.rb` and `_tmp_probe.rb` passed a stated snake_case claim while `TmpProbe.rb`, `tmpProbe.rb` and `tmp-probe.rb` were all caught, and the same shape held on the client map. The strongest reading of "classes here inherit ApplicationRecord" was again the one the check could not enforce, and the realistic violation is again the omission: an agent drops a PORO into `app/models` rather than inheriting the wrong base. Measured over seven Ruby repositories the superclass omission costs two stated slots and gains two, and both losses are honest: discourse's `plugins/discourse-ai/lib/agents/tools` goes 54 of 54 to 54 of 56 and its `plugins/discourse-workflows/lib/discourse_workflows/nodes` 58 of 58 to 58 of 63, because those directories hold bare classes the old shape could not see. The largest population it moves states on neither side: forem's `app/models` goes 119 of 132 to 119 of 140 and prints "no convention" before and after. The report asked for `class TmpOdd < Struct.new(:a)` to count too and the measurement refuses it: a superclass naming no constant is still a superclass somebody chose, and `ActiveRecord::Migration[7.2]` is an index call, so counting it took mastodon's `db/migrate` from 41 of 41 to 41 of 576 and lost the claim in two directories that hold nothing but migrations. H16's carve-outs map over: a class inside a class is that class's helper, and a reopening declares its superclass in the part that carries it | **done** `dimensions-naming.mjs` (`namesASite`, declared on the row as `isSite` and required of every corpus row by `assertDeclaredFields`), `dimensions-ruby.mjs` (`class_base`), `check.mjs` (`filenameFinding`) |
| C24 | An omission site is reported only where the map stated the claim, on both of the site types C23 introduced | H16's new site type exists to say "you should have written X", which is a directive. On a row the map holds at `20 of 31 sites, no convention` the check was nudging an agent to mix `ActiveModel::Dirty` into a plain PORO on the strength of a 20-of-31 ratio, and the map's whole contract is that a count failing the gate prints as a count and never as a directive. The NIT tier softens it, not the direction. A body that includes the wrong module is still counted where the map said nothing: "you wrote a different module from the one this area writes" is the count speaking, and "add this module" is the directive the gates refused | **done** `check.mjs` (`isOmission`, `filenameFinding`, and `sidesFor` carrying which keys stated). The filename half needed saying separately: a name spelling no class is the omission and needs the stated claim, while a name spelling a *different* class is the count speaking and is reported either way, which is what it always did |
| C25 | A learned naming row narrows its population to one kind of file, the kind holding more of its sites, counted by whether the file holds JSX | A React directory holds `UserCard.tsx` beside `formatDate.ts`, the area learns PascalCase off the components, and every correctly camelCase helper becomes a violation of a convention nobody holds. On one measured pull request a single `.ts` helper collected 5 of the 9 findings; replaying 982 merged commits, 29 of 55 false findings were this pooling, and four of the 29 were React hooks where obeying breaks the rules of hooks. Two slots was the obvious answer and the measurement refuses it: across three repositories and three rows, splitting produced two stating halves in 0 of 9 cases and rescued 0 slots, because the evidence gate needs about 35 perfect sites and halving the sample kills both sides. Narrowing instead keeps 15 of 17 mixed stated rows and drops half the non-conforming sites in them, which are the findings that stop being emitted; only 17 of 733 slots across those repositories are mixed at all. The kind is counted rather than declared and the facet already rides on every parse record: the extension is wrong on 5.4% to 23.9% of `.tsx` files, "used as a JSX element somewhere" needs a repository-wide index nothing builds, and a hook file holds no JSX so it already lands on the right side. Verified end to end on the file the report named: checking a real 253-file branch of that repository against a pinned map, `syncStatusTooltip.ts` goes from 9 findings to 4, and the five that go are exactly the ones the report counted, `file_naming_case`, `function_naming_case` twice and `exported_symbol_case` twice. The four that remain are `doc_comment_style` and `explicit_return_type`, both of which a `.ts` helper genuinely answers | **done** `dimensions-naming.mjs` (`splitByJsx`, on the three rows measured), `reduce.mjs` (`majorityKind`, ties to the module side so two scans agree), `facts.mjs` (schema 16: `learnedKind` travels with the class, and `narrowed` says whether the sentence names it, because the check builds its own text from the registry template and was quoting the unqualified sentence in the finding, the JSON and the annotations at once), `check.mjs` (`violations` and `filenameFinding` enforce over the same population the map measured, off one kind for both revisions the way `sides` and `learned` already are, because answering it per revision skipped the whole base side of a file that gained JSX on the branch and every pre-existing violation in it read as newly introduced; and decline a file whose kind this run could not read: an absent facet spelled `module`, so an unread component drew a MUST-FIX one line under the caveat saying the file went unchecked). Two amendments the narrowing needed. The sentence names the kind wherever the narrowing left something out: the area file loads on the files the population excluded, so "files here are named camelCase" was delivered to 12 components beside 45 helpers, and a component named camelCase is a host element, which is the very class this row cites. An area holding one kind states the plain sentence, because there the exclusion excluded nothing. `dimensions-naming.mjs` carries a sentence per kind and `dimensions.mjs` refuses a splitting row that declares fewer than two |
| C26 | The module that implements a routing claim is not one of its own sites, decided by the file's own stem: every word of it in the capability's vocabulary, where an import needs one | Something has to reach the platform. The repository's own logger calls `console`, its own client calls `fetch` or `Net::HTTP`, its own config module reads `process.env`, and the always-loaded map read `network calls go through the repository's own client, not fetch directly ... except src/queries/request.ts`, which is the client: the map told an agent that the client breaks the client rule. Admitted in a `blind` field and recorded nowhere. Not a site rather than a conforming one, because the file implements the rule instead of following it and a conforming hit would also make it one of the three adopters C14 asks for. The strict word rule is measured: it keeps all 22 Ruby clients and the one JavaScript client 301 files import, and drops the 92 files that merely mention the vocabulary; one word would have excused a real `Net::HTTP.get` in an ordinary service and exposed 20 log-named files, five of them `db/migrate` log-table migrations where `puts` is normal | **done** `dimensions-capability.mjs` (`implementsCapability`), `dimensions-ruby.mjs`, and the path plumbed through the same side channel comments and source already use: `parse-file.mjs`, `ruby.mjs` and `check.mjs`, which runs the rows itself and would otherwise keep firing on a file the scan stopped counting |
| C27 | An abstract base is not a companion producer, and a pairing row may not declare a counter-claim at all | `controller_spec` counts 62 of 65 at `app/controllers/api/v1`, bound 0.8729, and the three non-conforming files are base controllers that are never routed to and can never have a spec. Exclude them and it is 62 of 62, bound 0.942, and the claim states; a reviewer left "No spec for this controller. All 56 siblings have one" on a live pull request and the map had the data and could not say it. The rule lives in `companionOf`, the one function both the scan and the check go through, because a scan-only exclusion makes the one commit in 483 that edits a base controller a finding against a file the map never counted. The name rather than an inheritance graph, because `check` reports on files the branch created and no graph built at scan time holds a file that does not exist yet; the noun comes off the row's own producer root, so `base_price_controller.rb` stays a site. The counter half is the other end of the same asymmetry: `pairingViolations` computes one direction, so a pairing row with a counter would print a sentence in the map and enforce nothing. `worker_spec` is the case that raised it, 0 of 125 and 0 in all 22 worker areas, a convention the repository holds at 100% that the map cannot state; re-weighed rather than re-opened, because "a worker ships without a spec" as a directive tells an agent to write one test fewer, and each area already carries the fact twice, on the kinds line and on the suppressed count line, the first of which the line budget drops last | **done** `pairing.mjs` (`isAbstractBase`), `dimensions.mjs` (`assertDeclaredFields`) |
| C28 | Four predicates were measured and deliberately left as they are, with what was measured written into the row rather than into a comment | `optional_chaining` recognises an optional by the receiver's name, and that is the only signal this tier has: a measured 2,485-file repository writes `?.` 2,627 times across 672 files while the row sees 538 sites in 202 of them and calls 21 conforming. It is not narrow, it measures a different population, and no widening of the six names closes it; answering it properly needs the checker, which knows whether a receiver's type includes null. `record_lookup` counting `find_by!` as a violation is correct and the sentence was wrong: the axis is what a miss does, and `find_by!` is `where(...).take!` in ActiveRecord's own source, so counting it as conforming would put it in the numerator of both sides of a two-sided row at once. `absent_is_null` still counts a `: void` function although `return null` there is TS2322, because the annotation is exactly what the Flow retry blanks and reading it would make the row answer differently on a stripped tree than on the same file unstripped, so a stripped base beside an unstripped head would cancel real findings; one measured site is not worth that. `hook_call_style`'s counter is kept, and it is the distinction the whole exercise produced: a counter whose other side does not compile is a defect, and one that merely pushes against the ecosystem default is a house style, where the escape is one import line and the finding names the sentence | **done** `dimensions.mjs`, `dimensions-ruby.mjs`, `dimensions-extra.mjs`, `dimensions-jsx.mjs`, and `docs/dimension-intake.md`, whose `why` column now carries the ones not visible from the code |
| C29 | Of eight proposed rows one ships, and every refusal is recorded with the measurement behind it | The proposal's own "clears 0.90" column reads a point ratio while the gate reads a Wilson lower bound, which is wrong for four of the seven, and its counts are repository-wide while the gates run per area. Measured across seven repositories each: `hook_per_module` spreads 0.7778 react, 0.8235 storybook, 0.9224 supabase, 0.9259 next.js, 0.9367 cal.diy, 0.9429 backstage, 0.9800 empire-flippers/client, a 0.2022 spread with two repositories under the gate, so it ships. The first-ranked proposal does not: expectations naming their argument rather than `anything` reads 0.9003 to 0.9883 across seven Ruby repositories, a spread of 0.088 with nothing under the gate, which is the `module_state_const` shape CONTRIBUTING's bar was written for. A comment carrying no ticket reference is 0.999 on the one repository measured, same refusal. A mutation's `onError` at 0.8850 and a date wrapper at 0.9033 both fail the bound, and the second cannot clear it at any realistic area size. An `eslint-disable` naming a live rule needs a repository-config channel no row can reach and is the linter's own job. The data-fetch routing row is left planned rather than dropped: 92 of 96 is a bound of 0.8977, three conforming sites short, and it has not been measured per area. The eighth is planned for a different reason and is the one worth coming back to: `schema_matches_migrations`, that `db/schema.rb` changes only what this branch's own migrations produce, is a branch invariant rather than a ratio over sites, so it needs a fourth row kind beside tree, corpus and pairing, and a reader in the check. Three of its four measured hits sit on one pull request whose head would have shipped a schema dropping two columns that exist in production | **done** `dimensions-extra.mjs` (`hook_per_module`), `docs/dimension-intake.md`, which is where the refusals live so nobody proposes them again, the seven above plus the two the proposal had already measured and set aside, an inline `<svg>` belonging in an asset at 45 of 51 = 0.882 and a Cypress spec waiting on an alias at 40 of 89 = 0.449 |
| C30 | A superclass written bare inside a namespace is the constant it resolves to, and the two spellings count as one base | Ruby resolves a bare constant against the enclosing nesting before the top level, so `module Api::V1; class QboController < BaseController` names `Api::V1::BaseController`. Compared as written, empire-flippers/api's four relative spellings counted as a second base and took a 59-of-64 convention to 55 of 64: the strongest fact in its largest controller area, inverted, telling an agent to write `< ApplicationController` in a directory where 59 of 64 do not. Resolution is only ever from the site's own namespace prefixes, longest first, which is what `Module.nesting` offers, and it runs in both directions because either side may be the bare one: a repository whose plurality spelling is relative learns the bare name and its scoped sites have to count too. Narrower than the suffix match `isLearnedItself` refused and does not touch it: that one accepted any name merely ending in the learned one, with no nesting to justify it, and it asks about the site's own name rather than about the base it chose. A scoped name has already said which class it means, so `Api::V1::ChromeExtension::BaseController` stays a different base. The count moves; the gate does not, and 59 of 64 still reads "no convention" because the Wilson lower bound is 0.83 against the 0.90 bar. Fixing a miscount is not the same as restating a claim, and widening the gate is a separate decision with its own evidence | **done** `reduce.mjs` (`sameConstant`, read by the ungrouped fold and by `groupSites`, so `module_include` resolves a relative mixin the same way), `dimensions-ruby.mjs` (`nestingOf`, which is `Module.nesting` and an array because a compact path opens one scope and not one per segment), `walk.mjs` (the hit shape, which drops a field it does not name: the scopes were emitted, asserted in unit tests that call the dimension directly, and thrown away on every real scan until it named them), pinned by `test/naming.test.mjs` and `test/ruby.test.mjs` |
| C31 | An obligation learns what its companions are called as well as where they are, per learned root, with the declared suffix as the prior | C9 learns the root and left the spelling hardcoded. empire-flippers/api writes the plurality of `spec/models` as `<name>_model_spec.rb`, 52 of 166 against 46 bare, so the row read 46 of 166 over the roster and 44 of 129 over the area, and the map told an agent this repository does not spec its models at half the real rate. It reads 95 and 93 now. A candidate spelling is whatever a corpus file adds after a producer's whole tail, never one holding a separator, since `spec/models/address/foo_spec.rb` is a file in a directory named after the producer rather than a file named after it. Kept at a fifth of the root's producers and at least three, which is a measured bar rather than a chosen one. Run over the 35-repository corpus, five spellings clear the floor of three: openfoodnetwork's `_rake_spec` at 5 of 15 and empire-flippers/api's `_model_spec` at 52 of 166, both real, and three readings of openproject's `_integration_spec`, the largest 28 of 479. So a fifth admits the two and the nearest thing it refuses sits under a sixteenth, and that one is a different file's spec caught by its own name, `user.rb` beside `user_membership_spec.rb`. A spelling has to begin at a separator, `_`, `.` or `-`: `m0.rb` beside `m0book_spec.rb` is `m0book`'s spec, and on a small root the floor and the share coincide so the noise bar alone cannot tell a spelling from a longer name. The corpus is walked once and each companion asked which producer it extends, rather than each producer asked which companions extend it; the second shape is a scan of the whole corpus per producer and measured 4.7 seconds a row on a 20,000-file repository against 0.9 before the learning existed. A producer conforms when any learned spelling is in the tree; the declared one is still what a missing companion is reported as, because that is the spelling the row is written with | **done** `pairing.mjs` (`companionSuffixes`, `companionsOf`, carried through `packagedPairings` so a package learns its own), pinned by `test/pairing.test.mjs` |
| C32 | A foreign key the same migration declares apart from the column, as a statement or on the table block, is that reference's foreign key | The row read the inline option alone, so a reference the migration does constrain read as a violation: 43 of 76 where 72 of 76 is the count. Two forms were missing and the larger one is the one nobody had looked for: empire-flippers/api writes 102 `t.foreign_key` calls inside the `create_table` block that declares the column, `t.references :shopify_account` answered by `t.foreign_key :shopify_accounts` a dozen lines below it, against 12 inline options and 167 standalone `add_foreign_key` statements. Matched on the table the key is added to and on the column it covers, which is `column:` where the migration names one and the plural of the reference otherwise, because matching on the table alone would credit every reference in a migration that constrains one of them. Three pluralisation rules rather than an inflector: an irregular or uncountable table is caught by the equality beside them, and anything neither reaches reads as declaring no key, which under-counts conformance and so suppresses a claim rather than stating one nobody holds. Measured before it was built: only 8 of the repository's migrations hold both a reference and an `add_foreign_key`, so those 167 are overwhelmingly retrofits onto columns earlier migrations added, and reading them as the repository's idiom for reference columns does not survive the count. The idiom is the block form, which no grep in the report reached. A key declared only on the way down is not one: `def down`, and the `dir.down` half of a `reversible` block, add nothing going forward, and the receiver has to be the parameter `reversible` yields or an unrelated `Rollout.down` block would suppress a real key. The denominator needed no reconciling either: the repository writes 58 `t.references` and 22 `add_reference`, and 4 of the 80 are polymorphic, which is the exclusion the predicate already states, leaving exactly the 76 the line prints | **done** `dimensions-rails.mjs` (`foreignKeys`, `pluralOf`, `declaredApart`, `columnBlockTable`), pinned by `test/dimensions-rails.test.mjs`. Narrowed by C33: a statement this tool cannot match answers for nothing, and blocks the reference it might cover |
| C33 | A fact a predicate could not read is evidence for nothing, in either direction: the site holding it is declined rather than judged, and a statement that cannot be matched neither credits nor charges what it might cover | One shape convicted and its twin declined, on the same fact. `options` dropped an entry whose key is not a literal and returned the rest, so `t.string :name, key => false` read as a column declaring nothing while `t.string :name, **opts` declined the site; a `null: false`, an `id:` or a `foreign_key: true` written behind a constant, a local or an interpolated symbol was charged as absent, and `check` grades against that. Declining under-counts the population, which `N of N sites across X of Y files` already reports; convicting invents a violation the reader cannot fix. The same fact reached C32's evidence lookup three ways, each found by executing the rows rather than reading them: a statement whose table could not be read was dropped, so the reference it may cover read as a column with no key; one whose `column:` could not be read matched by the plural of the reference name and credited a reference it may never have covered; and a reference on a table that could not be read was charged, since no key can be matched to it. A key is matched on the table it is added to and the column it covers, so a statement missing either is kept as evidence for nothing and blocks what it might be the key for, and the block is lifted where the options declare the key inline, which is evidence on the site itself. Each half is ruled out on its own, which is what keeps the block from spreading: a statement on `editor_id` is not a reference on `author_id` whatever table it was added to, and a statement added to another readable table is nobody's key here, so only the fact this tool could not read blocks anything. Not a blanket decline: measured over 35 repositories, 7,902 migration files hold 8,268 column sites, 1,732 `create_table` sites, 1,758 reference calls and 511 foreign-key statements, and blocking a reference on an unreadable table against every key in its class lost canvas-lms a site its own comment says is a deliberate violation, `create_table :"aua_logs_#{index}"` holding a `t.references :asset_user_access` no statement in that migration covers. Asking whether a key covers the column keeps it. 2 of the 511 statements are unreadable, both in one openproject migration whose references declare their keys inline, so none of the four counts moves | **done** `dimensions-rails.mjs` (`options`, `foreignKeys`, `covers`, `declaredApart`, `unsettled`), pinned by `test/dimensions-rails.test.mjs` and `test/declined-fixtures.mjs`. `table_primary_key_declared` declines the same way and carries no printed clause, which is A35's granularity bar and not an oversight: its `sites` prose already says a readable options list |
| C34 | An option is the value Rails reads for it: the key it looks up, the truthiness it tests, and only where the source decides that value | C33 closed the key half and three more sat behind it, each found by executing the rows against the corpus rather than reading them. Rails looks an option up by symbol, so `"null" => false` is a hash entry and not the `null:` option, and reading it as one credits a column that declares nothing; a string key is skipped now rather than voiding the list, since it is readable and what it reads as is not the option. Ruby's truthiness is what Rails tests, not the literal `true`: `polymorphic: { limit: 255 }` is the type column's own options and `polymorphic: %i[account course]` is the list of types, and 26 of the corpus's 129 polymorphic references are written one of those two ways, all in canvas-lms, every one of them charged for the foreign key ActiveRecord refuses. And a value the source does not decide decides nothing: `null: nullable` may be declaring `null: false`, and `foreign_key: fk_options` may be declaring nothing, which whitehall writes once and this credited. The exclusion is lifted for the list form alone, where the options declare the key beside it: canvas patches `TableDefinition#references` to expand `polymorphic: %i[account course]` into one real reference per type with `foreign_key:` passed to each, and its own guard is `unless polymorphic.is_a?(Array)`, so a hash and a bare `true` fall through to stock ActiveRecord and raise. 10 of the 26 are the list form with a key, and excluding them denies a fact that repository's source demonstrates, while lifting on any truthy value would credit a migration that cannot run. The same reading settles `foreign_key:`, which Rails tests for truthiness: `nil` adds no key and read as one it credited a column that has none. `null:` is the one option still compared against the literal `false`, because `add_column_options!` compares it that way. Measured over 35 repositories: reference sites 1,655 to 1,638, of which 16 are the polymorphic exclusion and 1 the unread `foreign_key:`; conforming 1,186 to 1,185; the column and `create_table` rows do not move. Keys are matched within one direction as well, since a column added only on the way down does not exist going forward and neither does a key, which C32 already says of the key alone: a rollback reference is still a site, judged against the rollback's own keys, and 8 of the corpus's 1,758 reference calls are written in one, none of whose verdicts moves | **done** `dimensions-rails.mjs` (`options`, `isTruthy`, `undecided`, `isRollback`, `declaredApart`, `unsettled`), pinned by `test/dimensions-rails.test.mjs` and `test/declined-fixtures.mjs` |
| C35 | A declared type name that reads as a prefix and as an acronym alike - two capitals, or three or more leading ones - votes for neither prefix nor none, and is a site of neither prefix row | `prefixClass` wanted a lowercase third character, so `IEFLogon` fell through to `none` and was charged with violating the `I` prefix it already carries. The only way to comply with a stated `I` claim was to write `IIEFLogon`. Measured on a 2,489-file front end: `src/components` recorded `interface_prefix` at 157 of 164 where the true figure is 157 of 159, five of the seven charged sites carrying the prefix, and it named `EFLogon/index.tsx` and `EFLogon/types.ts` as the area's exceptions when neither holds a violation, so anything added to those two files was waved through as already excepted. In an area stating the row at N of N the same shape reaches MUST-FIX: `IApiResponse` conforms and `IAPIResponse` is a finding, the only difference being that the acronym is capitalised. The row is recorded in 116 of that repository's 127 areas and stated at N of N in 87, so this is the normal case. The design comment defended declining to credit `IOStream` with an `I`, which is right, and never noticed the two shapes are the same shape to this regex. What it costs is a denominator: an area near the evidence floor that loses two names can stop stating, and under a stated `I` claim `ABCFoo` now passes rather than being charged. That is the trade C33 already takes, and it is the right direction here because the unreadable shape is the realistic conforming one rather than the realistic violating one, which is what separates this from C21's `classifyWord` narrowing | **done** `dimensions-naming.mjs` (`prefixClass`, `interface_prefix`, `type_alias_prefix`), pinned by `test/naming.test.mjs` and by `test/applicability.test.mjs`, where the two new inapplicable witnesses hold the predicate to its own sentence |

## D. Gates

| # | Decision | Why | Status |
|---|---|---|---|
| D1 | `conforming / candidates >= 0.90` | The measured gate elsewhere; v1 loosened it to 0.80 with no argument | **done** |
| D2 | `candidates >= 6` | | **done**, and subsumed. D1's Wilson bound is the only count floor the gates read: at 0.90 a perfect record needs 35 sites, so a separate minimum of 6 can never bind. It is `reduce.mjs`'s `evidence` check, and no `GATES` entry spells this number |
| D3 | **The sites are worth at least 3 files by inverse-Simpson count, and the ratio still reaches 0.90 with the largest file dropped** | v3's `files_conforming >= 3` does not block its own motivating case: 200 candidates in one file plus one each in 13 others gives 14 files, ratio 1.0, and every gate passes. The share of candidates this row first proposed cannot answer it either: at two files the largest share is at least 0.5 by arithmetic, and at fifty files no share ever fires however lopsided the spread is. The effective-file count answers concentration and the leave-one-out ratio is what sees a large file holding the rest of the area over 0.90 | **done** `reduce.mjs` (`GATES.minEffectiveFiles`, gate name `concentration`) |
| D4 | `distinct authors >= 2`, counted over files carrying conforming matches, **only when the area holds more than one directory** | Measured twice independently: the unconditional directory gate blocks 124 of 170 slots because area discovery finds leaf directories and a leaf directory has one directory | **done** |
| D5 | Authors from **one** `git log -M --name-status --format=%ae` pass, unioning rename chains, `-M100%` on a partial clone (F14). Never `git blame` | Measured: 271x faster (0.84s vs 103s), agrees 99.6-100%, and one formatter commit collapses per-site blame to a single author | **done** `authors.mjs` |
| D6 | All gates read the **baseline** population; "current also holds" prints but never gates | v3 defined the triple and never said which population the gate reads | **done** `reduce.mjs` (`verdictFor`, `blockedFor`), which is where the ordering lives: which condition a whole area is under is still the baseline's to decide (`areaBlock`), but the order it is asked in, and the rule that a block closes both sides, are in one place and reachable without a repository |
| D7 | Counts print whether or not a directive fires, and `gate` records which one suppressed it | Makes a wrong threshold cost one sentence | **done**, and bounded by A6. A6 gave the area file a hard line budget, and a count is what gives way when it runs out: a directive is what the file exists to deliver, and an area holding more slots than fit has to drop one of the two. `gate` is recorded either way, in `facts.json`, which is where a threshold is actually audited from; the printed line is the convenience. The truncation notice says how many of each kind went, so a reader is never left guessing which, and it counts the stated ones off the same array `droppedDirectives` reads. Two derivations of that disagreed: a slot the model writes by default is stated too and rides with the counts, so a dropped tail of those printed "all of them counts" while the check enforced them at FIX. On one measured repository 49 of the 74 areas carrying the notice said something other than what the check would, 34 of them by reporting that nothing enforceable had gone |
| D8 | The `evidence` gate passes on the area's own Wilson bound, **or** on the rest of the repository's bound for the same dimension and learned class, when this area's own upper bound reaches the rate it is borrowing. Leave-one-out, pooled per learned class, built only from slots no other condition has closed, and read off the same population every other gate reads | Of 90 mechanical review comments across 170 merged pull requests on two mapped repositories, the map would have pre-empted none, and a gate was the reason rather than a predicate. The bound needs about 35 perfect sites to reach 0.90 and the measured front end's median area holds 11 files, so an area could be perfectly consistent and never speak: `interface_prefix` holds 2,125 of 2,152 repo-wide at 0.987, 116 of its 118 areas clear the ratio gate, and six state it. A reviewer left "we typically prefix our interfaces with an `I`" on a file whose area read `no convention. 9 of 9 sites (evidence)`, bound 0.7008. A 9-of-9 directory is not weak evidence of a weak claim, it is a consistent sample of a strong one. Two measured cases the design had to survive and does: `handler_is_named` 3 of 7 in a repository that is 0.757 the other way stays silent on both sides, because `ratio` fires first and the prior lends neither side anything; and `record_lookup` 26 of 26 in a directory of a repository that is 0.539 stays silent, held only by the prior failing at 0.4937, which is the case the design has to earn. The consistency clause is what earns its place: without it a 900-of-1000 area at exactly the ratio bar, whose own upper bound is 0.917, would inherit a 0.9875 repository's confidence, which is the direction that manufactures conventions. A repository sitting at exactly 0.90 lends nothing at any size. The comparison that clause is written as needed a bound that is actually a bound, which D10 supplies. Measured after the fact on a 2,486-file front end, and the volume is worth knowing before anyone reads a map: stated slots go from 72 to 735, of which 799 of the 947 states are borrowed and none of them borrows against a repository rate under 0.90 on the side it states. The always-loaded cost falls rather than rises, 240,636 bytes to 225,424, because a stated directive replaces a longer counts line, and every file still holds the 40-line bound. The thinnest slot that can borrow is 3 sites over 3 files, which is `concentration`'s existing floor rather than a new one; no number was invented for this. What the reader gains and loses on that repository, measured either side: MUST-FIX-capable slots go from 86 to 82, because a borrowed claim is capped at FIX, and 95 stated slots lose their line to the 40-line budget and are capped by H24 instead, against 19 before. Printed directives across all 127 areas go from 64 to 714, and exactly one area prints fewer than it did. So the map says a great deal more and the check says it more quietly, which is the trade this row is | **done** `reduce.mjs` (`wilsonUpper`, `applyGates`, and `blockOf` split out of `verdictFor` so a closed slot can be excluded before the gates run), `scan.mjs` (two passes over the areas, the pool a sum so two scans of unchanged source still agree), `facts.mjs` (schema 14: `priorBound` and `borrowed` on each side, so a stated claim can be audited from disk for which of the two carried it), `check.mjs` (a borrowed claim is capped at FIX, because MUST-FIX is a statement about this area's own history and that is exactly what was borrowed). The new coupling is worth saying out loud: an area's map now depends on the rest of the repository, so scanning a subtree answers differently from scanning the whole of it |
| D9 | The side a **suppressed** slot prints needs a majority the sample can support, not a plurality: the counter's own Wilson lower bound over a half, and a side holding no site at all never prints | `handler_is_named` is 799 of 1,054 = 0.758 repo-wide, and in one 7-file area it is 3 of 7 named, so the plurality flipped and that area printed "an event handler prop is given an inline arrow, not a named function" while other areas of the same repository printed the forward claim. One dimension, one run, two directly opposed sentences. Worse than reported: `check` then reported a file doing the thing 76% of the repository does as a violation of the inverse, reproduced end to end. The guess that D8 would close it is wrong, and the reason is worth keeping: the slot fails `ratio` on both sides, which is checked before `evidence`, so the pooled prior never runs, and the sentence is chosen in `statedSide`, which reads raw counts and has no gate input at all. The bar is algebraically "the counter's lower bound beats the claim's upper bound", since the Wilson interval is symmetric. Measured against the case C6 exists for: 61 sites at 59 to 2 still flip, 10 at 9 to 1 flip, 10 at 8 to 2 do not, and 7 at 4 to 3 do not. The one behaviour lost is a 5-of-9 area that used to print the counter and now prints the claim with 4 of 9, which is a directory with no habit saying so. Amended after the fact, and the amendment carries more weight than the rule: the bound is penalised hardest at small n, so a unanimous counter of three failed it too and the map printed the claim with 0 of 3, a sentence the directory follows zero times, which `check` then reported against files doing exactly what every other file there does. Measured on the same 2,486-file front end: of 441 suppressed two-sided rows, 81 printed a side with no site behind it, every one of them unanimous, and one parent and its own child printed directly opposed sentences off the same run, `src/components/forms` at 15 of 15 through `React.` and `src/components/forms/utils` at 0 of 2 by the bare name. That is the defect D9 exists to close, recreated at small n. One site each way is still mixed and the plurality rule still decides it, and so is one site against none: without a floor a single site flipped a directory against a claim the rest of the repository holds at 0.947 and against its own parent area's directive, which is this row's own defect at n = 1. Of the 81 unanimous counters on that repository 29 are a single site, and the count printed beside the sentence is what says how thin the other 52 are | **done** `facts.mjs` (`statedSide`) |

| D10 | `wilsonUpper` is held to the two bounds its arithmetic owes and its division does not give: never above one, never below the rate it was handed | Whether an area states a convention, and therefore whether `check` can ever grade against it, was decided at a perfect ratio by which side of 1 a float happened to land on. D8's borrow compares `wilsonUpper(k, n)` against the rest of the repository's rate, and that rate is exactly 1 wherever the rest of the repository holds a claim without exception, so the test reduces to `wilsonUpper(n, n) >= 1`. Unclamped it returned 0.9999999999999998 at 118 of the first 500 sample sizes and 1.0000000000000002 at others, and nothing in the code decided which. The damage is visible in the shape of the counts: on a 2,489-file front end 172 rows printed a perfect ratio that was still denied, and bucketing them by sample size against the float, 37 of the 172 sat at n = 12, 20, 21 and 31, every one of them a size the ratio fell short at, while n = 16, 17, 19, 23, 24, 25, 26, 29, 30, 32 and 33 produced none between them. The gate was not measuring evidence at those sizes, it was measuring rounding, and it was not even monotone in n at a fixed ratio, which is the clearest statement of the defect. Fixed in the function rather than at the call site, because both bounds are true of a Wilson upper bound and neither was being held: the lower clamp is what carries the perfect case, and it subsumes asking separately whether the sample already meets the rate it is borrowing, since at or above that rate the bound is too, by construction rather than by rounding. This repository holds 38 perfect rows and 3 of them sat at a denied size | **done** `reduce.mjs` (`wilsonUpper`), pinned by `test/gates.test.mjs`, which asserts a perfect sample borrows a perfect prior at every size from 3 to 500 and that the gate is monotone in n |
| D11 | A clone holding only a window of history cannot lower the author bar. The bar holds at `GATES.authorEvidence`, and both the overview and the terminal say the history was truncated | `git log` succeeds on a shallow clone and answers truthfully about a window nobody chose, and nothing above it asked whether the window was the whole history. Measured on one repository at three depths, same HEAD, same corpus, same 2,199 slots: the full clone states 465 claims with 20 held by the author gate and 13 authors at most; at `--depth=503` it states 383 with 105 held, losing 82 conventions and gaining none; at `--depth=1`, which is what `actions/checkout` does by default, every one of the 2,199 slots reads one author, the bar derived from that window collapses to one, the gate fires on nothing, and 484 claims state, 19 more than the honest 465 and every one of them on the single-author evidence D4 exists to refuse. The map then printed `1 author (the repository's only)` 644 times and told the reader the repository has one author, over a repository with fifteen. Nothing on any surface said the clone was shallow: 103 of 127 area files differ silently between the full and the truncated build. Refusing to scan is the wrong answer, since CI checkouts are shallow by default and a map is still worth having there; what a depth-1 checkout may state is what one commit can evidence, which is nothing, so every claim prints as a count. `historyRead` stays true, because a shallow history was read and conflating the two would print the wrong reason on every slot | **done** `authors.mjs` (`shallowHistory`, carried on the returned map), `reduce.mjs` (`authorsRequired`, `applyGates`, `verdictFor`), `scan.mjs`, `facts.mjs` (schema 18, which is also where the record starts carrying `authors` at all: the gates read it and nothing on disk held it), `render.mjs` (`truncatedHistorySentence`, and the one-author sentence withheld), `summary.mjs`. The probe follows F15's soft read, so a call that cannot answer reads as not shallow and costs a caveat rather than a wrong answer. The overview carries the claim and not the size of the window: A5 owes that file byte-stability between scans of unchanged source, and both numbers move on their own, since on a fixed-depth checkout the boundary slides forward with every commit that lands upstream. `historyWindow` carries them and only the terminal asks for it, which is also where the gate count goes, since the overview does not hold one |

## E. Baseline and drift

| # | Decision | Why | Status |
|---|---|---|---|
| E1 | Store the baseline **file list** per area at pin time; compute baseline over that list, not the current glob | Moving violating files into a denied or new directory otherwise re-selects only the conforming ones at the baseline sha, ratio 1.00, every guard held | **done** `baseline.mjs`, `reduce.mjs` (`verdictFor` orders the population block ahead of the gates) |
| E2 | Baseline conformance from `git show <sha>:<path>`, never the working tree | Otherwise an agent editing baseline files moves the baseline numbers | **done** `baseline.mjs`, reading through `git.mjs` (`showBlob`) |
| E3 | Unreachable baseline sha: verify with `git cat-file -e`, then drop to counts-only until a human re-pins. Never fall back to stored counts | Squash-merge-to-main is the common workflow, not the edge | **done** `baseline.mjs`, `reduce.mjs` |
| E4 | An area whose candidates all postdate the baseline emits no directive | Greenfield directories are where agents write most | **done** `reduce.mjs` (`blockedFor`, ordered ahead of the gates by `verdictFor`), `baseline.mjs` (`areaBlock`) |
| E5 | Re-pin is a separate command that prints the population delta it is about to accept. The plugin never suggests it | v3 had it suggested at the moment of maximum laundering | **done** `commands.mjs` (`runPin`), `summary.mjs` (`pinSummary`, `pinLines`), `plugins/anatomiya/commands/pin.md` |
| E6 | Drift is files changed in mapped areas from the commit the pin and the base ref share, to the base ref, and whose bytes there differ from the pin's, never `..HEAD` | Over HEAD the branch's own changes count as drift, so severity falls as the change under review grows. The range between two commits is symmetric, which leaves the same hole from the other end: `anatomiya pin` puts the pin at HEAD, so pinning on a branch read the branch's own commits as a map that had moved. Measured on a 12-file repository whose branch touched 6 mapped files: pin, scan, check reported "severity capped at FIX: 6 of 12 mapped files changed since the pin" against a pin that was HEAD, so a branch silenced its own findings by being large. Drift is the base's own progress past what the pin already holds, and where the pin is a descendant of the base that is nothing. Running from the shared commit alone opens the mirror of the same hole: after a squash merge the shared commit sits behind work the pin already holds, and a base whose tree is byte-identical to the pin reported 40 of 130 mapped files and capped again. A path is drift when the base moved it since the shared commit and its bytes there differ from the pin's, which is both ranges at once. One case is left standing and is worth naming: in a shallow clone `git merge-base` exits 1, the range falls back to the symmetric one, and a branch's own commits read as drift. Reporting no drift there is worse, because an unmeasurable drift caps every finding at FIX permanently | **done** `git.mjs` (`resolveBaseRef`, the one resolver both phases read), `baseline.mjs` (`mergeBase` before the range) |
| E7 | Carry a rename map from `git diff --find-renames` into the baseline lookup | A path hash cannot tell a rename from a delete-plus-add, and at the baseline sha the new path does not exist | **done** `baseline.mjs` |
| E8 | A pinned file this tool cannot read closes the area, unless it cannot be read today either, which makes it a blind spot rather than something the pin lost | Any unread pinned file suppressed the whole area, and a permanent blind spot is not a population that moved. Measured across 35 repositories: 13 lose slots to it, and react is written in Flow, which oxc does not take, so 287 of its files never parse at the pin or today and 507 of its 986 slots were closed for it. The current pass already leaves such a file out of its counts, so the two passes disagreed about what an unreadable file means. Closing it takes react from 36 stated claims to 65, webpack 80 to 92, next.js 162 to 172, and population-change to zero on all three | **done** `baseline.mjs` (`countAtPin`). The guard stays for the case it was written for: a file that has become readable since the pin still closes the area, because the baseline is then missing sites today can see. A file readable at the pin needs no exception at all, since the pinned population is what the gates read and it was counted in full. The obvious objection is laundering, and all three routes were tried on a repository pinned at 54 of 84: breaking the violating file at HEAD leaves the ratio at 0.64 and the gate on `ratio`, because the baseline reads the blob at the pinned commit where it still parses (E2); deleting it reads 1.00 today and is closed by `population-change` (E1); and a file unreadable at both revisions was never counted at either, so it has no sites to hide |
| E9 | The head side of a check is read from the working tree wherever the tree differs from the commit, and a file only in the tree is examined like any other file the branch added. The base side stays on `git cat-file` | An agent writes, checks, fixes, then commits, so the natural loop runs the check exactly where it used to answer about content it had not read: a violating file planted uncommitted reported `0 MUST-FIX, 0 FIX, 0 NIT` with the miss demoted to a caveat, and the same file committed was caught (#48). A headline that says clean and one line below it that unsays it is a headline an agent acts on. E2 is untouched and is the reason only one side moved: the baseline population is still read at a commit, so no edit of the agent under review can move the bar it is judged against. The cost is that a run is no longer reproducible from git alone, so the report says how many files it read that way | **done** `check.mjs` (`pendingPaths`, `withPendingEdits`, `treeSource`), `plugins/anatomiya/commands/check.md`. Whether a pending path has a base version is asked of the merge base rather than of the index letter: `git rm --cached` and a delete-then-restore both report an addition for a path whose committed version is right there, and read as new every site in the file was charged to this branch. What that cannot answer is a file moved to a name it never had and not yet committed, because `git status` rename-detects nothing for an untracked path; the move is judged correctly the moment it is committed, where `--find-renames` sees it. The note it replaces said what the check could not see, and now says it only where the tree is not read: with no merge base nothing can be called newly introduced, so the pending files are named rather than judged |
| E10 | One reader hands both the check and the baseline the content at a revision | Each answered the question itself and the two disagreed about what an unreadable path means: the baseline wrote a temporary tree and handed back paths, the check read one blob at a time into strings and let the parser write them out again, serially. Measured on a 40-file branch: 80 of the 88 git processes the check spawned were `cat-file` waiting on each other. Re-measured on this repository, which examines 59 files and reads 99 blobs: 107 processes of which 99 are `cat-file blob`, the same before and after, and the run goes from about 2.1s to about 1.25s, roughly 40% off a check | **done** `revision.mjs` (`readAtRevision(root, sha, files, {concurrency, withSource, timeout})`), `check.mjs`, `baseline.mjs`. Through disk because the parser runs out of process and takes a path (B5), and `withSource` for the caller that also needs the bytes in hand, since the check quotes snippets and resolves line numbers against them. A path that will not come back is reported rather than thrown on: one missing file is one file to both callers rather than the whole set. The clock travels with the caller, or the check's blob reads would silently move from its own 30s clock to the scan's 120s one. The size cap does not: `showBlob` gives up at exactly the size the parser skips at, so a blob refused there is one the parse would have refused anyway, and no caller ever widened it. E2 is untouched: the base side is built from the pre-rename path and read at the merge base, so no working-tree path reaches it |

## F. Safety

| # | Decision | Why | Status |
|---|---|---|---|
| F1 | Corpus is `git ls-files -z`, NUL-split, deny-list applied | Git permits newlines in paths; a newline split turns one hostile filename into two entries | **done** `corpus.mjs` |
| F2 | Lexical containment, then realpath both sides, fail closed, read the resolved path | v3 dropped this to one sentence about symlinked directories, which exempts symlinked files | **done** `corpus.mjs` (the read), `rules.mjs` (`resolveInside`), `write.mjs`, `facts.mjs`, `check.mjs` (the write). It held for the corpus and for nothing else, which is the half that only reads. Measured: a tracked `.claude -> ../victim`, git mode 120000 so it survives a clone, had the scan write the map and `facts.json` into a directory the repository does not own, name that directory's files in the always-loaded overview, and remove one of its `anatomiya-*.md` files on the next scan. `join` normalises `..` and resolves no link, so lexical containment is not containment. Every component is resolved now, a dangling link is refused rather than created through, and the two directories are checked before anything is written, since one link at `.claude` escapes with both. The scan fails closed; the check reports, because refusing a branch over a link is the blocking behaviour this design rejects |
| F17 | A file in `.claude/rules/` is read by its head and only when it is a regular file, and every listing of them is bounded | Measured: the ownership test is a regex anchored at byte zero, and it was reading whole files to run it. A tracked symlink to a 400 MB blob took a scan's peak resident size to 1.2 GB; pointed at `/dev/zero` the read never returned at all, and a symlink is available on every Unix box. A directory named `anatomiya-overview.md`, which is a fixed name a repository can spell, threw `EISDIR` out of the atomic rename and failed every scan with an errno. The check listed every name it found, into the context of the agent reading the report | **done** `rules.mjs` (`readHead`, `HEAD_BYTES`, `occupied`, `listSome`), `check.mjs`, `render.mjs`, `summary.mjs` (`LISTED.report`). The cap is sized by our own frontmatter rather than by a guess at how long one should be: an area's `paths` list is one line per pattern, and a cover needing 170 of them is 14 KB on canvas-lms, so a first cut at 8 KB put that file's closing fence past the head and this tool's own output came back as a file it had not written, never removable and named as somebody else's in the always-loaded overview. A megabyte is far past any cover generated across 35 repositories and far short of a file worth holding in memory. The head is opened first and typed on the handle it is read from, non-blocking so a fifo cannot hold the open, because a stat on the path and then an open of the same path is two lookups and a symlink swapped between them reads something the type test never saw; a shape the platform refuses to open at all, a socket under whichever errno that platform refuses with, or a directory wherever one will not open, is typed on the path with nothing read after it, and still occupies the name. A file the tool cannot open is its own answer rather than an empty one: read as empty it failed the frontmatter test and came back as somebody else's, which the always-loaded overview then said out loud about the tool's own output, and which no later scan could undo because a file called foreign never re-enters the removable set. A directory that will not list is reported for the same reason the symlinked one is |
| F3 | One encoder, allowlist not denylist: reject non-printable codepoints, NFKC normalise, reject mixed-script paths, reject `\|`, `---`, comment delimiters, backtick runs; cap **before** quoting, on grapheme clusters; emit paths JSON-quoted | Bidi controls and zero-width joiners are category Cf and pass an ASCII control filter; `JSON.stringify` does not escape them either | **done** |
| F4 | Every repository-controlled value goes through F3: paths, area names, author names, commit subjects, branch names, matched source text | | **done** `encode.mjs`, and one pass per record rather than one per line at the two writers a machine reads: `check-report.mjs` (`encodeReport`), `summary.mjs` (`encodeScan`, `encodePin`). The rendered lines had always encoded, so a record was the one surface a crafted filename reached whole: `JSON.stringify` escapes neither a bidi override nor a zero-width joiner, and the pin's `added` list is printed by its JSON writer and by nothing else, so no encoded counterpart existed for it anywhere |
| F13 | History is read off the stream, and history git could not read is told apart from history that is empty, by exit code rather than by message | Measured: buffering put an oversize log in the same silent branch as a repository with no commits. Every file came back with no author, every dimension failed the author gate, and the map stated nothing with nothing to say why | **done** `authors.mjs` |
| F15 | A git read the check could not perform is reported, never absorbed as an empty answer | Measured: with the base commit's tree object missing, `git diff` exits 128 and every caller in `check.mjs` read its output without looking at the exit code. The result was zero changed files, zero findings and no caveat, which renders identically to a branch that broke nothing. Same shape as F13 for history and B13 for the parser, and the same rule closes it: tell "could not read" apart from "read, and there was nothing there", by exit code | **done** `check.mjs`. Every read looks at its exit code before its output: the diff, the added-line ranges, the pending-edits listing, and base resolution through `resolveBaseRef` and `mergeBase`, which report their outcome rather than an empty string. One read stays deliberately soft: an unanswerable `--is-shallow-repository` reads as not shallow, which costs a fallback rather than a wrong answer |
| F16 | No module in `lib/` imports its way back to itself | A cycle loads under ESM hoisting, so nothing fails at run time and the first symptom is a module reading a half-initialised binding from a partner still evaluating. `ruby.mjs` carried a comment refusing an import for exactly this reason and the comment was deleted in the same change that took the import, closing `ruby.mjs -> dimensions.mjs -> dimensions-ruby.mjs -> ruby.mjs`. The Ruby AST helpers living in the parser bridge are what make the registry unreachable from it, so shared code both bridges need goes in a module that imports nothing of ours | **done** `walk.mjs` (`collectHits`), pinned by `test/modules.test.mjs` |
| F5 | Any subprocess: `execFile`, arguments after `--`, reject paths starting with `-`, cwd outside the repository, no network, timeout, streamed stdout byte cap | Measured: `--` neutralises the whole argv class; a tracked file named `--instruction-file-path=.git/config` exfiltrated a secret without it | **done** `git.mjs` (`FLAGS`, `refuse`, `gitEnv`), `pool.mjs`, `ruby.mjs`. Two runners, one buffered and one streamed (F6), and both carry the timeout and a byte bound. Every git call goes through one of them: `corpus.mjs` and `authors.mjs` were the last two running their own `spawn`. One thing reaches the network and it is not the scan: on a shallow clone the check fetches the single base commit, because `merge-base` cannot answer without it and the alternative is reporting a branch against nothing. `ls-remote` and `fetch --depth=1` are the whole of it, both on that one path, and F14 separately forbids the lazy-fetch a partial clone would otherwise trigger. The scan makes no outbound call at any point, and the `gh` measurement helper that did was published in `lib/`, imported by nothing, and is gone. `--` is still not universal, because `rev-parse`, `cat-file`, `merge-base`, `config` and `status` do not take it, so the rule is stated from the other side instead: an argument that reads as an option must be one this tool wrote, checked against a closed list of the flags it actually passes. A ref, a sha or a path that begins with a dash now refuses the call rather than resting on whichever predicate was supposed to have caught it. The two subprocesses that are not git carry the battery too: `ps` runs with a timeout, a kill signal and a byte bound, since `execFileSync` blocks the parent's event loop and a `ps` that never returns is the memory guard becoming the hang; `ruby.mjs` times the wall clock behind the idle window, since a child answering one file every fourteen seconds never trips a liveness check. Both parser bridges now run outside the repository, and git carries an environment that refuses a credential prompt. One command installs anything and no other command runs it: `setup` reaches a package registry with `npm install --omit=dev --ignore-scripts --no-audit --no-fund`, `cwd` the plugin root, so a scan, a check and a pin install nothing and the shallow-clone fetch above stays the only outbound call any of them makes (B23) |
| F6 | Never buffer subprocess output; `spawn` plus streamed parsing | Measured: `execFile` throws `RangeError: Invalid string length` inside Node's own exit handler, `maxBuffer` does not protect, V8 caps any string at 0x1fffffe8 bytes | **done** `git.mjs` (`gitStreamed`, `pathSet`, `nameStatusReader`), `check.mjs`, `ruby.mjs`. The two callers that hand-rolled a stream, `corpus.mjs` and `authors.mjs`, share one field split; each kept its own reading of what the fields mean. The streamed side also bounds one record, which neither hand-rolled reader did: output carrying no delimiter grew one buffer to the same string limit streaming exists to avoid. Four reads that grow with the repository were still buffered, not one: `filesAt`'s `ls-tree -r`, `changedSinceWorktree`, `diffRange`, and the check's own `changedFiles`, where a branch off a distant base lists every path there is. The `--name-status -z` grammar now has a streaming reader beside the buffered parse, both of them one reading of what a rename's three fields mean. Which runner a function uses is pinned by `modules.test.mjs`, because both answer the same values on a repository small enough to test with |
| F7 | A partly-read corpus sets `truncated` and **suppresses every directive** | Counting over an arbitrary subset and rendering it like a complete scan is worse than reporting nothing | **done** `ruby.mjs`, `scan.mjs`, `baseline.mjs`. No repository size can set it (B10); the Ruby per-line guard can, and that path is tested end to end. Both corpus reads set it: the baseline materialises blobs from the pinned commit and parses them a second time, and that pass hits the same guard, so its flag travels back out of `measure` rather than being dropped at the seam |
| F14 | On a partial clone the author pass uses `-M100%` and refuses to lazy-fetch | Measured: `-M` scores similarity, which needs blob content a `--filter=blob:none` clone does not hold, so git fetched from the promisor one round trip at a time. 33 of 35 measured clones could not answer at all and reported history unread; warming them cost 62 seconds to 57 minutes each and 4 GB of network. A full clone is untouched and keeps the inexact detection D5 was measured on | **done** `authors.mjs` |
| F18 | No module the parse worker reaches imports `node:child_process` | Every JS parse child loads the registry, and the two Ruby dimension files took their walkers from `ruby.mjs`, the bridge that spawns Ruby, so all eight forked workers loaded the spawn machinery and the inline prism script to read a table of Ruby rows. `ruby-walk.mjs` was extracted to be the importable leaf and two of its three callers bypassed it through the bridge's re-export | **done** `dimensions-ruby.mjs`, `dimensions-rails.mjs`, `ruby.mjs` (the re-export is gone), pinned by `test/modules.test.mjs` beside the cycle rule it extends |
| F19 | An explicit `--base` that resolves nowhere is refused, and `HEAD` is refused before anything is fetched | The command file's own contract is that a non-zero exit means the check could not run, show its output and stop. `--base no/such/ref` was answered with 685 added-lines findings over 2,150 files at exit 0, which an agent is told to trust as a finished review of a branch that changed nothing, and the same degradation happens on an ordinary full clone where the fetch fallback never runs. `--base HEAD` resolves locally in every repository that has a commit, so reporting it as a base a shallow clone could not fetch names a cause that is not the reason and a fix that would not work; it is refused for what it is, this branch's own tip, and refused before the shallow fallback, which fetches by branch name and would otherwise use a remote branch literally named `HEAD` and defeat E6. A ref this tool guessed is a different question from one somebody typed: the candidate list not resolving is a repository that keeps its trunk somewhere else, which is what the added-lines degradation is for, and it is unchanged. A repository with no commits at all has no ref to mistype against and still answers that nothing was examined | **done** `check.mjs` (`resolveBase`, `refusal`) |
| F20 | The sites a branch introduced are decided in one leaf, `introduced.mjs`, that reaches no git, no child and no disk, and the check is its only importer | The identity rule, node type plus normalised slice and never the line, was pinned by comments in four other test files, one citing `check.mjs:437` for a line that had moved into `resolveBase`, because the block had no interface of its own: the only way to run it was a repository committed twice. Moving it gives the rule a namesake test that asserts two golden digests, the one-polarity rule and both modes from one parsed program, and the check's output is byte for byte what it was on a fixture holding a rename, a line shift and an edit inside an existing site | **done** `introduced.mjs` (`newlyIntroduced`, `siteIdentity`, `bodyIdentity`), `check.mjs` (one call in `collect`), `test/introduced.test.mjs`, `test/modules.test.mjs` (the reach and the one importer) |
| F21 | A binding declared inside a block is read only where that block is in reach, held over every source file by resolving each read the way the engine does | `scripts/ab.mjs` declared two bindings inside the `try` around its trials and read them after it; `node --check` passes that, nothing imported the file, and the throw came once every trial was paid for. The guard parses every file with oxc, opens a scope per block, `for` head, `switch`, `catch`, function and named class expression, hoists `var` and the declarations the way the engine does, seeds the module with what node provides as globals, leaves `typeof` alone, and reports a read no scope in reach answers that some block in the file declares. It went red naming the two lines before the fix, and its own driver case holds the resolution rules | **done** `test/modules.test.mjs` (`readOutsideTheirBlock`, driven directly by its own case) |

## G. Scope

| # | Decision | Why | Status |
|---|---|---|---|
| G1 | Dimensions come from the glossary re-audit's 87 Tier A entries, starting with the 8 proven writable | | partial: 31 registry rows and 9 obligations, and the next tranche is named. G2, G3 and G4 have landed, so the three rules that blocked it are now a committed table rather than a document elsewhere: `docs/dimension-intake.md` carries nine planned rows with the entries each absorbs and the rename each needs. What remains blocked is the glossary itself, which is not in this repository, so the rows beyond those nine cannot be enumerated here. Building the nine is deliberately separate work: the measured A/B on a 5,489-file Rails repository found no effect on generated code, and the reading was that the registry is full of general language idiom a frontier model already writes, so fifty more rows of the same kind would spend the effort where the measurement says the value is not. G5 is the measurement that would change that |
| G2 | **Glossary rows are not dimensions.** Nine collapses found; the largest is Postel's Law + Fail fast + Input validation = one `boundary_input_validation` | | **done** `docs/dimension-intake.md`, `check-docs.mjs` (`readIntake`), `test/intake.test.mjs`. The collapse was a finding in a document outside this repository, which is a finding nobody can act on twice. It is a committed table now, one row per key with the entries it absorbs, and no entry may be absorbed by two keys. Writing it found one collapse the re-audit did not: the glossary's "Test coverage" entry is answered by the pairing obligations that already ship, so a `spec_adjacency` row would have been the tenth duplicate. It also found a second, smaller one, that `keyword_params` answers the boolean-trap entry and the positional-overload entry with the same signature walk |
| G3 | Rename entries whose glossary name would misread as a verdict: `boundary_input_validation` not "Postel's Law", quarantine-rate not "Flaky test" | A rendered `1.0` next to "Postel's Law" reads as a philosophical endorsement rather than a measurement | **done** `dimensions.mjs` (`PRINCIPLE_NAMES`, `assertClaimIsNotAVerdict`), `pairing.mjs`, `docs/dimension-intake.md`. The rename is recorded per row, and the rule that produced it is a load-time throw rather than a convention: `claim` is the sentence an agent reads, so a row naming a principle refuses to load at all. Both sentences are held to it, since a counter-claim is rendered too. The list is closed and holds the names that have actually been proposed as labels, which keeps it a rule about labels rather than a style guide |
| G4 | Entries with no denominator are dropped, however visible. OWASP Top 10 is a findings list, not a counted claim | | **done** `docs/dimension-intake.md`, `test/intake.test.mjs`. Dropped rows stay in the table with their reason rather than being deleted, because an entry deleted for having no denominator is an entry somebody proposes again next quarter. Seventeen are listed and they fall into three kinds: forge or CI API state, a runtime artifact, and a scalar from somebody else's model. The invariant the tests hold is the one that matters, that nothing dropped may also ship |
| G5 | Success is measured by a with-map / without-map diff on real tasks, never by review-comment counts | Measured: comment data only sees what got *through* review. Preventable comments are 8.5% of the corpus, the cheapest ones, and 93.5% of PRs are unaffected. The pre-review effect is unmeasured, not disproven | **done** `scripts/ab.mjs`, `scripts/ab/*`, `docs/measurements/`. One command: two worktrees off one commit, the map installed into one of them, and the trials alternating so a rate limit partway through hits both arms equally. Two things the first hand-run could not answer are built in. It ranks areas by headroom and refuses under 0.05, because that run scored 10 of 10 in both arms on a claim stated at 140 of 145 and measured a ceiling rather than a map. And it scores through `parseAll` and `reduceArea` rather than by grep, so the harness's number and the map's number are the same number by construction. Injection is verified before any trial: arm A must name the area and arm B must answer NONE, and either failing aborts. First run recorded in `docs/measurements/2026-08-16-vscode.md`: 10 trials per arm on vscode against a claim with headroom 0.077, and both arms wrote conforming code every time. That is a null result about the task rather than about the map, which the result file now says in its own words rather than leaving to the reader, since the reading is the thing the first run got wrong |
| G6 | The corpus takes every extension the two parsers already read, plus Ruby's extensionless filenames | Measured: `.mts`, `.cts`, `Rakefile`, `Gemfile`, `config.ru`, `.gemspec` and `.jbuilder` are 803 files in a 35-repository corpus, neither counted nor reported as skipped. `.rbi` stays out: a Sorbet signature describes types rather than anything anyone wrote, so a claim counted over one speaks for no code | **done** `corpus.mjs` |
| G7 | The excluded directories cover the other names deliberately unidiomatic code goes by | Measured: angular keeps 2,010 files of golden compiler output under `test_cases`, which was 55 of its 339 areas and every one of its 476 unparseable files. `test_cases`, `testdata`, `test-data`, `golden`, `goldens`, `__mocks__` and `mocks` are 2,218 collected files across 35 repositories, and `.yarn` is 14 more, every one a machine-generated bundle whose 2 MB releases sit at the parse timeout boundary and flip between crashed and rejected across runs, which moves the always-loaded overview (A5). `examples` is deliberately left in: 8,967 paths match it and much of that is maintained code | **done** `corpus.mjs` |
| G8 | The fixture exclusion also matches a word boundary, not only a whole segment: `cases` bare or camelCase-compounded into `...Cases`, `fixture(s)` bare or underscore-compounded, `golden(s)` bare or joined to `test`, `snapshot(s)` bare | Measured on four repositories. webpack spells its fixture-case directories `cases`, `configCases`, `statsCases`, `watchCases`, `hotCases`, `benchmarkCases`, `memoryLimitCases` and `typesCases`: 347 of 393 areas and 3 of 14 stated directives came from them, and two of those three contradict each other on one dimension, one stating that relative imports carry the file extension at 50 of 50 and the other that they are written without it at 113 of 116. angular's `golden-test` is a hyphenated compound `/goldens?/` never reached; prisma's `_fixture` is singular and underscore-prefixed, 309 source files, and diaspora's `jasmine_fixtures` is the same compound the other way round; next.js spells `tests/fixture` and `tests/snapshot` singular with no underscore at all, 75 of 500 areas | **done** `corpus.mjs` (`EXCLUDE_DIR`), pinned by `test/corpus.test.mjs`. The boundary is a literal `-` or `_`, or webpack's own capital-letter join, never a bare substring: `use-cases` and `showcases` are real directory names carrying neither. Still misses chef's `spec/data` (8 of 70 areas, holding directories named `borken`) and next.js's `tests/execution`, both real words too common to exclude bare, and no compounding is added for `golden` or `snapshot` past `-` and `_`, since only the hyphenated and bare leaks were measured |
| G9 | The `build` exclusion does not fire when the segment is reached by descending through `src` | turbopack-cli's `src/build/mod.rs` is a hand-written Rust module named for the verb, not compiled output, and the bare-word match dropped it with no repository where Rust is read to notice the loss. A repository's own `src/` is where its hand-written source lives by construction, and no bundler writes its output one directory inside it | **done** `corpus.mjs` (`isBuildOutput`), pinned by `test/corpus.test.mjs`. Whole-segment match only, as before: `rebuild` and `build-tools` still do not fire. Output at a package root or one directory into a monorepo package is unaffected, since neither sits under `src`. Not extended to `lib`, `app`, `packages` or `source`: only `src/build` was measured, and reaching further risks the overreach G8 was written to avoid |
| G10 | A file is dropped from the corpus when its own head carries a generated-file marker in its first ten lines (`@generated`, `Code generated by`, or `DO NOT EDIT` beside a word about generation), or a root `.gitattributes` declares its path `linguist-generated`, whichever directory it sits in | The fixture gates catch code that looks unusual: old syntax, a foreign copyright, one or two authors. Generated and vendored code looks ordinary, because it was ordinary somewhere else, and it carries the real git-blame authors of whoever ran the generator or committed the copy. Proven three ways with plant-and-catch: babel's compiled runtime helpers stated 2 of 20 directives from code nobody typed, and stripping an extension from one produced a real FIX; prisma's `contract.d.ts` files, every one stamped `// GENERATED FILE - DO NOT EDIT`, stated two more from a 24-file area that is 100% generator output, and dropping `type` from one import produced a real FIX; next.js vendors about 678 files of byte-for-byte `@babel/runtime`, stated as its own conventions with four authors | **done** `corpus.mjs` (`isGeneratedFile`, `generatedAttrRules`, `isAttrGenerated`, `classify`), pinned by `test/corpus.test.mjs`. The read is bounded to the first ten lines of the first 4 KB. Bytes alone protect against a single-line minified file the way the parser's own size cap does, and lines are what put the stamp where a generator writes it: `DO NOT EDIT` on its own is an operational warning, and a hand-written file carrying one was dropped with no trace anywhere in the map, since `collect` drops before anything counts. It now needs a word about generation beside it, and this tool's own `corpus.mjs` had cleared the byte cap by 1,756 of them, which is not a property anyone was maintaining. What is dropped is named in "Not covered", because a reader who knows the directory is there would otherwise see a map that has never heard of it. The `.gitattributes` reader takes the root file only and three pattern shapes, not gitignore's full grammar; anything else is skipped rather than guessed at. The file is dropped rather than the directory, so a directory whose files overwhelmingly carry the marker falls out of a stated directive by losing its evidence rather than by a second rule about directories, and a hand-written file beside ninety generated ones still counts. Not closed: generated code carrying neither a marker nor a declaration, which is babel's own runtime-helper shape, confirmed only by reading its Makefile |
| G11 | Every measurement trial runs at one named engine, `claude-opus-5[1m]` at `medium`, stated on the command line and recorded in what the run produces. A batch of another model or another effort is a different question and never merges into a held one | The harness said every setting was fixed rather than inherited and named only the model. Effort was inherited, and 2.1.250 reads `CLAUDE_CODE_EFFORT_LEVEL` ahead of the session's own effort, refusing a settings bridge with "CLAUDE_CODE_EFFORT_LEVEL overrides effort for this session" in its own words, so an exported level decided every trial while nothing recorded it. More variables move the model, the thinking budget or the context window the same way, so the child environment is cleaned of every one of them by name and of the `ANTHROPIC_DEFAULT_<alias>_MODEL` and `ANTHROPIC_CUSTOM_MODEL_OPTION` families, and keeps everything naming a credential, an endpoint or a region. Read off the build by name rather than matched by shape, on evidence: a shape wide enough to catch `MAX_THINKING_TOKENS` also catches `ANTHROPIC_SMALL_FAST_MODEL_AWS_REGION`, and one narrow enough to spare the region lets a trial run with thinking off while provenance records `medium`. The context group is in it because the pinned model is the 1M one: the build strips `[1m]` and asks for the window with the `context-1m-2025-08-07` beta header, which `CLAUDE_CODE_DISABLE_1M_CONTEXT` takes away. `medium` is a level `--effort` takes on this build (`sg=["low","medium","high","xhigh","max"]`) and Opus 5 pins no launch effort, unlike opus-4-7, opus-4-8 and fable-5. The flag is not the last word, though: `settings.env` sits above it, so a settings file naming `CLAUDE_CODE_EFFORT_LEVEL` decides the trial, and an arm is a worktree of the repository being measured. `--setting-sources ''` looks like the answer and is not, measured on 2.1.250: an arm holding a `paths` rule answered with the map's file count with the sources loaded and NONE without, so excluding them removes the map the experiment installs. Both settings files are taken out of each arm instead, which leaves the rules directory where it is, and a run refuses to start when a settings `env` entry names one of them. Only `env`: measured on 2.1.250, `--effort` beats `ultracode: true`, `modelSettings` and `effortLevel`, so refusing on those would fail a machine merely configured for ordinary work and refuse a run the flags were deciding correctly. The machine this was written on carries `effortLevel: "medium"` with `modelSettings["claude-opus-5"].effortLevel: "xhigh"`, which is what the old harness ran at, since it passed no effort at all; the stated flag is what takes that back. Managed policy and an organisation's own effort cap sit below neither and cannot be reached from here, and the harness says so rather than claiming a level it cannot guarantee. The merge guard reads both halves: two batches summed into one tally claiming 60 samples of a run that never happened. An entry measured before the effort was recorded carries none, and that absence matches no batch that names one, since reading it as `medium` would date those runs to a setting nobody can check; `--force` replaces such an entry with one that says. A refusal prints which half it disagreed on, because the trials are paid for by the time it fires, grouped by the engine in the way rather than one line per key. The model string moved too, from `claude-opus-5` to `claude-opus-5[1m]`, so all 24 measured rows in the shipped table refuse on both halves and stand at the older engine's answer until somebody re-measures them with `--force`, which replaces rather than adds. That cost is the price of not blending two engines into one tally, and `docs/how-it-works.md` says it where a reader of the table will find it. So the flags are treated as a request and the answer as the record: every trial asks for `--output-format json` and reads `modelUsage` back, which names the model that served it and the window it got, and provenance carries those rather than the flags. Nothing else can confirm the 1M half, since the `[1m]` suffix never reaches the wire and becomes the `context-1m-2025-08-07` beta header. Trials that report two engines refuse to be written up as one, and a batch that reported none records the request and says the observation was missing. `CLAUDE_CODE_NO_MODEL_FALLBACK` is set rather than scrubbed, on the build's own words, "model substitution is disabled": a trial now runs the pinned model or fails, instead of answering from a substitute under the pinned name. A hand-written list against one build is a guess in five years, so a test reads the installed build for every name shaped like a model, an effort or a thinking budget and fails on one nobody has ruled on; the three kept on purpose are listed with their reason. The evidence for all of this is `docs/research/one-model-one-effort.md`, read off build 2.1.250 | **done** `scripts/ab/engine.mjs` (`ENGINE`, `EFFORT_LEVELS`, `OVERRIDES`, `PINS`, `engineFor`, `overridesEngine`, `engineEnv`, `engineRan`, `conflictingSettings`), `scripts/ab/run.mjs` (`CLAUDE_DEFAULTS`, `runTrial`, `answerIn`), `scripts/ab/arms.mjs` (`dropSettings`), `scripts/ab/args.mjs` (`parseArgs`), `scripts/ab/render.mjs` (`render`), `scripts/measure-defaults.mjs` (`parseArgs`, `accumulate`, `refusedLines`, provenance), pinned by `test/ab.test.mjs`, which also reads the installed build for the pinned id and the effort vocabulary and skips where there is none, so it runs on a maintainer's machine and never on a CI runner, which installs no Claude Code, and by `test/measure-defaults.test.mjs` |

| G12 | `CONTEXT.md` is gated on shape and on one closed set, never on meaning. `check-docs.mjs` holds that every entry is whole, a definition under its name, an `_Avoid_` line closing it and no name defined twice, and that `Unexamined` names every member of `PARSE_OUTCOMES`, which the tally in `parse.mjs` now reads from that same list | Nothing read the glossary at all, and eleven entries had drifted by the time anyone set it beside the code. The worst of them reached a reader: `Conforming` said "a candidate matching the positive pattern" while `facts.mjs` returns `conforming: counterSites` on a counter-stated slot, so an area file printed "5589 of 5589 sites" for a number the glossary's own word put at 0. Meaning is not mechanically checkable, and a gate pretending to check it would be worse than none, since it would answer "the glossary matches the code" over a file whose every sentence had rotted. Shape is checkable, and it is what a hand-edited file of forty entries actually loses. The closed set is the one place the prose and the code hold the same list, so it is the one coupling a machine can keep. Both halves are proved to fire by breaking a copy of the repository each way and asserting the run exits 1 naming the entry, because a gate nobody has watched fail is a gate nobody knows works | **done** `scripts/check-docs.mjs` (`readGlossary`, `READS`, the glossary claims), `plugins/anatomiya/lib/parse.mjs` (`PARSE_OUTCOMES`), pinned by `test/check-docs.test.mjs` |

## H. Layout

| # | Decision | Why | Status |
|---|---|---|---|
| H1 | The roster is delivered through the always-loaded overview and through nothing else | The overview carries no `paths` key, so it is in the system prompt before any Read or Write. That is the one channel that reaches a write path nobody Reads in, and #34 measured that path: on a 5,517-file Rails API the exploration phase ran as four subagents and no area file attached in any of them, the one dissected having made 54 `cat`, `grep` and `head` calls and no Read at all, and the four directories that feature's code landed in never attached one either. The other way to reach it is a hook, which "What is deliberately not built" refuses as the channel on complexity and on being flagged as prompt injection, and the chameleon inventory measured a hook channel at 10 to 40% adherence against 100% for the always-loaded one. So the fact that decides where a new file goes is paid for on every turn instead. A24 later added a hook beside this one rather than in place of it, for the one thing this channel does not carry: recency, and a timestamp saying when the map was read. This row is unchanged by that, and would not survive the hook becoming the only delivery | **done** `layout.mjs`, `layout-scan.mjs`, `scan.mjs`, `render.mjs` (`renderLayout`). #34 stays open; this is the measured answer to it for now |
| H2 | No vocabulary of kinds. A line is labelled with a directory name and a count is nouned with an extension | "components", "services" and "helpers" are a taxonomy this tool would have to ship and then defend on every repository, and the repository has already written the name it uses on the directory itself. So the tests line reads `0 of 504 .tsx files have a namesake test` rather than `504 components`, and a root prints as the path it is. It also removes the whole class of failure where a repository's own word for something disagrees with ours | **done** `render.mjs` (`rootLine`, `testsLine`), `layout.mjs` |
| H3 | Six shell names are descended past: `src`, `lib`, `app`, `apps`, `packages`, `source`. Every other directory name is printed | Those six say nothing about what is inside them, so a line naming one carries no fact a reader can use. Six words rather than a list that grows, because the rule is "this name is empty" and a longer list starts deciding what a name means, which is H2 arriving through the back door. A Ruby gem's `lib/<gem>` is descended twice, once as a shell and once as a wrapper, and lands on the name the gem actually uses. `apps` sits beside `packages` for the reason `packages` is there at all: a monorepo splits independently named things under a plural wrapper, and a repository laid out as `apps/*` plus `packages/*` names the same shape on both sides. Measured on supabase, where `packages` already descended into each package while `apps` rolled seven sites, a product dashboard, a marketing site, two showcase sites, a docs site and a course app, into one bullet, and the always-loaded namesake rate printed a blend of 4.0% over the dashboard's own real 6.9%, diluted by about 1,558 files from sites nobody was going to write per-file tests for | **done** `layout.mjs` (`SHELL_NAMES`) |
| H4 | Root selection scales with the corpus: a floor of `max(3, ceil(0.01 * N))` cumulative files, descent through any directory one child holds 80% of, and at most 7 lines. The three numbers are tuned by measurement; that they scale is the decision | A fixed floor right for a 200-file repository makes every directory of a 100,000-file one a root, which is the failure area discovery already answered with a scaled floor. 80% is what separates a wrapper from a parent. 7 lines is what the section can spend without crowding the listing it sits above, and the whole section is 15 lines of a 40-line file. A directory under the floor folds into the nearest root above it or into `and N more directories holding M files`, and the direct files of a descended directory are their own candidate, because rubocop prints `lib/rubocop (files at this level): 45 .rb` beside `lib/rubocop/cop`. A descent that earns no line at all keeps the directory instead, because webpack's `lib` is 652 files with 117 at that level and no child clearing its 144-file floor, and the map named `test` and `examples` and never webpack's source | **done** `layout.mjs` (`minRootFiles`, `layoutRoots`, `WRAPPER_SHARE`, `ROOT_BUDGET`) |
| H5 | The roster describes the tree as it is; the five learned rows read the baseline like every other row (D6) | The roster is counts and never a directive, so there is nothing in it to launder: a tests line that moves when an agent adds a test file is a true count that flips no claim, exactly as the area listing's file counts already are. A learned row states a sentence, so it reads the pinned population. The one thing the roster refuses is a subset: a truncated corpus prints `layout: not counted, the scan was truncated` and no roots, because counts over an arbitrary slice of the tree rendered as a description of the tree is what F7 exists for | **done** `layout-scan.mjs` (`counted`, `notCounted`), `reduce.mjs` |
| H6 | C7 is unchanged. The pairing obligations keep their null counter-claim and the roster carries the absence as a count instead | `0 of 40 have a spec` is a fact about a directory; a directive telling an agent to write specs where nobody writes them is a sentence nobody measured. The count prints whenever the repository holds any test file at all and the root's top printed extension is one this tool parses, so a root whose largest is images or data is not asked. That is the line the api side needed: workers ship no spec, services do, and nothing in the map said so, because an obligation treats a missing companion as an absence rather than as a habit. The namesake is matched on the path tail the way C9 learns a companion root, and the root the namesakes share is named by a count of votes rather than by the first match. The tail is asked twice, whole and then with the seven tree names dropped from both sides, because a repository that splits a source tree from a spec tree writes the same path on both halves and only the word for the tree differs: openproject's `modules` read 0 of 2623 with `budgets/spec/models` sitting beside `budgets/app/models`, and a mirrored match votes for the tree the two paths part on. A top vote under half the matched files names no root at all, since one `__tests__` per component directory answers every file and names no one place. A root that is or sits under a test tree at any depth, `test`, `tests`, `spec`, `cypress`, `e2e` or `__tests__` matched against every path segment rather than only the first, is not asked at all: its non-test files are what the tests run on, and webpack's `test` read `1 of 7858 has a namesake test under test` over the fixture modules its 2,607 tests exercise. The first-segment reading was right for a single-project repository and silent on every monorepo, where the first segment is the package name: fastlane's `gym/spec` stated `1 of 1 has a namesake test` over a single zero-byte `spec_helper.rb` credited through an unrelated gem's same-named spec, and decidim's `decidim-dev/lib/decidim/dev/test` scanned 46 files of shared RSpec tooling as application code with 7 false credits. The webpack citation is unchanged, since `test` is still caught at its first segment under the any-segment reading | **done** `companions.mjs` (`namesakeCompanions`), `layout.mjs` (`rootFacts`, `underTestTree`), `render.mjs` (`rootLine`, `testsLine`) |
| H7 | Every tracked file counts in the roster, source or not; a file in no language this tool parses is never a test file | A directory holding 40 `.md` files is a fact about where things live, so the roster's corpus is the whole `git ls-files` pass under the same deny list rather than the source extensions, and a non-source file is named by its extension and nothing else. The test side runs the other way, and it was measured: twenty screenshots under `cypress/` are not twenty specs, and counting them made the denominator this whole section exists to be read 24 over 4 | **done** `layout-scan.mjs` (`roster`), `layout.mjs` (`isTestFile`) |
| H8 | The common-imports line skips relative specifiers and the runtime packages a framework area cannot be written without, matched by package name so every subpath of one is runtime too | "This React area imports React" is a line the reader already has, and a sibling import is a fact about one file rather than a habit the next file should copy. Matched on the package, so `next/link` and `react-dom/client` are the framework while a package that merely starts with the same word, `next-auth`, is not. A closed table for the same reason C8's framework list is one, and it grows the same way | **done** `siblings.mjs` (`RUNTIME_MODULES`, `commonImports`) |
| H9 | The reuse line resolves a specifier on the path tail, an ambiguous tail resolving to nothing, and counts only importers outside the area | `~/utils/user` and `@/utils/user` both name `src/utils/user.ts` because the tail `utils/user` does, which is the shape C9 already learns a companion root with; a tail two files answer resolves to neither, or one file is credited with another's readers. No `tsconfig` is read, because the aliases a repository points at its own root with are a short closed list and a wrong resolution here is a wrong number rather than a missing one. Counting the area's own files made this repository's `lib` line report twelve readers of a name eleven of its own modules pull in: a directory importing itself is how it is written, not who depends on it | **done** `siblings.mjs` (`specifierToFile`, `mostImported`) |
| H10 | The section and the area `kinds` line shrink into whatever the rest of the file leaves, and both give way to a directive | A6 bounds what a reader reads, and the head, the tail and the two listings that grow are paid first. The section gives way in the order it is read backwards: root lines fold into the count that was already there, then that count, then the two sentences, then the tests line, and under four lines it prints nothing, because a root line names one directory while the tests line is the denominator for all of them. In an area file the `kinds` line prints under the heading and is dropped with the roster lines after a suppressed count and before a stated directive, since a directive is what the file exists to deliver and a description is what makes the next file fit beside the ones already there | **done** `render.mjs` (`renderLayout`, `renderArea`, `LAYOUT_FRAME`, `fit`) |
| H11 | Five learned rows carry a name out of the repository instead of a class from a closed set: `extends_base` and `class_base` for a superclass, `module_include` for a mixin, `interface_prefix` and `type_alias_prefix` for a declared type name's prefix letter. A source-learned class is encoded where the sentence is filled, and a row that can learn an absence writes the absence out in full | What a new file in a directory is expected to sit on is a review comment nothing here counted, and it is repository-specific by construction, which is what C13 already built the learning for. Two rules the template alone cannot carry. The class is text an attacker writes and it lands in a file the agent loads, so it goes through the encoder once at the fill rather than at every render site, where one site would eventually be missed. And `none` is an absence rather than a value: filled in it renders "named with a `none` prefix", so a prefix row spells the whole sentence instead. `none` is also the model default for both prefix rows, so a repository that prefixes nothing prints counts and a prefixed one states. Keyed per engine and per construct, because one key is one predicate and a repository can prefix its interfaces and not its type aliases | **done** `dimensions-naming.mjs` (`extends_base`, `interface_prefix`, `type_alias_prefix`, `claimFor`, `prefixClass`), `dimensions-ruby.mjs` (`class_base`, `module_include`) |
| H12 | Whether a learned class may be enforced is asked of the row, not of the four naming classes | C13's check compared the stored class against the closed list of naming classes, which is the whole vocabulary of the rows that existed then. A superclass name and a prefix letter are neither, so all five new rows would have stated a sentence in the map and enforced nothing on the branch, silently, which is the one asymmetry between the two surfaces this design cannot have. `enforceableClass` asks per row: a source-learned class holds when the encoder leaves something of it, a prefix row's vocabulary is one capital letter or `none`, and everything else still votes for one of the four | **done** `check.mjs` (`enforceableClass`) |
| H13 | Two sentences under the roster and no others: match sibling test shape, and match directory granularity. The first prints when the tests line printed, the second when at least one root printed a helper facet, and neither carries a number of its own | These two because they are the two findings the review this design started from actually made: an agent added a vitest file under `__tests__` where the repository writes Cypress, and extracted a one-function module beside a component whose siblings inline theirs. Both are about which kind of file to create and where, which is the class of finding no shipped row counts, and each has a counted ground directly above it: the tests line is the denominator that said 4 vitest against 102 Cypress on the repository the review was on, and the helper facet is the sibling-module and inline-helper pair. A sentence with no such ground is a rule this tool did not measure, which is what keeps the list at two. They read as sentences rather than as claims because the numbers are already on the lines above, so repeating one here would state a count twice and let the two drift | **done** `principles.mjs`, `layout-scan.mjs` (`principleKeys`), `render.mjs` (`renderLayout`) |
| H14 | A learned row may declare `groupedSites`, and `module_include` is the one that does. The worker still emits one hit per included constant, and every hit of one class or module body carries that body's group, so the fold votes once per body and class, counts one candidate per body, conforms a body that includes the learned module whatever else it includes, and lists one exception per body. The check fires one finding per body, at its first include. A tie is still a tie (C13) | A Rails worker includes `Sidekiq::Worker` and one more module, so with one site per constant the row could not pass 0.5 whatever the directory did, and it failed closed on exactly the directories it was written for. Measured on empire-flippers/api: `app/workers/workers` read 125 of 234, which is 0.534 and stopped by the ratio gate; grouped it reads 125 of 125 and states, and `app/workers/cronjobs` goes from 70 of 140 to 70 of 70. The gates, the baseline and the render are untouched because they read candidates, conforming and exceptions, which is where the grouping is settled. `class_base` names one superclass per class and stays per site. The grouped finding is fingerprinted from the constants the body mixes in, so adding a module to a body that already violated moves that fingerprint and the branch is charged for the violation: accepted, because the branch did edit the violating body and the severity is still capped by the baseline rules. H16 added the body that mixes in nothing, which has no constants to be told apart by and answers with its own name instead, and which is reported at the class rather than at a first include it does not have | **done** `dimensions-ruby.mjs` (`module_include`), `reduce.mjs` (`groupKey`, `groupSites`, `learnClass`), `check.mjs` (`violations`), `walk.mjs` (`collectHits`) |
| H15 | Every spelling of a test file's name lives in `plugins/anatomiya/lib/test-shape.mjs`, and the differences between the spellings are stated beside each other | Four modules each carried their own: the roster's basename regexes, the namesake suffix list, minitest's path rule, and the runner labels, with a fifth label copy in the measure harness. All suffix-shaped, so a new spelling meant four edits with nothing failing at three, and a prefix convention was not expressible anywhere. The spellings deliberately differ per question, is-test against namesake against mirror-tree, and consolidating without flattening is the point: the hyphen forms count as tests and answer no namesake, which is now a stated fact with a pin rather than a scattered accident | **done** `test-shape.mjs`, `layout.mjs`, `companions.mjs`, `facets.mjs` (`minitestByPath`), `render-layout.mjs`, `scripts/measure-layout.mjs`, pinned by `test/test-shape.test.mjs`, which also holds every pairing row's `companionSuffix` to a suffix the vocabulary knows |
| H16 | `module_include` counts the body that declares nothing. A class body is a site when it includes something, and also when it includes nothing, names no superclass and is not nested inside another class. A module including nothing, a subclass, and a class inside a class are not sites | The row already said "the body is one site" in its own predicate and counted only the bodies that had an include, so the omitted include was invisible: a planted worker with `include Comparable` was caught and one with no include at all passed clean on the same run (#46). The strongest reading of a stated claim, every class here carries this include, was the one reading the check could not enforce, and the omission is the violation an agent actually commits. The three exclusions are each a body with somewhere else to have got the mixin, and each was a wrong finding on the corpus before it was excluded: counting `module Users` around a worker read forem `app/workers` at 88 of 176 and suppressed a claim true of every worker in it; the other nine of that 97 were `< BustCacheBaseWorker`, which carries the mixin for them, the mirror of the rule `class_base` states; and discourse `Chat::Channel::Policy::MessageCreation` holds a bare `class Strategy`, a helper rather than a peer of the services the claim is about. Measured over twelve Ruby repositories, eleven do not move at all and empire-flippers/api is identical row for row, including the `app/workers/workers/listings` 38 of 38 the report was filed against. The one claim that moves is discourse `plugins/chat/app/services/chat`, from 46 of 46 to 46 of 47: `Chat::MessageDestroyer` is a real class there that does not include `Service::Base`, and one counterexample takes the Wilson bound to 0.889, under the 0.90 bar. That is the evidence gate reading a true population rather than a filtered one | **done** `dimensions-ruby.mjs` (`module_include.run`), pinned by `naming.test.mjs` at the row and `check.test.mjs` at the finding. The fingerprint a grouped body is told apart by (H14) reads the constants it mixes in, and a body mixing in nothing has none: every include-less body in one file was the same site, so a new one absorbed an older one's finding and the report named a class the branch never touched. It answers with its own qualified name instead, since `A::Worker` and `B::Worker` in one file are two bodies and the short name told them apart no better than the empty string did. Review added two more bodies that declared a mixin by another route: one that `prepend`s or `extend`s a constant, and a reopening of a class that declares one elsewhere in the file. Both reported a forgotten include against a body that had not forgotten one. The applicability witnesses carry all three exclusions, since the predicate and the walk disagreeing is what this row was |
| H17 | `extOf`, `stemOf` and `withoutExtension` read one shared split, and that split recognises `.d.ts`, `.d.mts` and `.d.cts` as their own extension rather than the last dot alone | Measured: cal.diy's `packages/types` reported `30 .ts` where 29 are `.d.ts`; appsmith's 3,661 is 3,640 plus 21; prisma at 26 of 199 areas, one saying `24 .ts` where 16 are declarations. The old stem left a stray `.d` on every declaration file too, so `image.d.ts` read `image.d`. The three answers share one `splitBase` because this module's own header says a rule matching on the basename and a rule matching on the directory have to agree where one ends, and one copy drifting is a file counted twice: `withoutExtension` was left counting dots and on `index.d.ts` returned `index.d`, which no longer ends in `/index`, so `siblings.mjs` stopped resolving a package whose entry is a declaration file through its own directory | **done** `paths.mjs` (`splitBase`, `extOf`, `stemOf`, `withoutExtension`), pinned by `test/paths.test.mjs`, which asserts all three agree on the same paths |
| H18 | An empty tail falls through to the same `mirrors` test a nested tail already uses, rather than waiving the tail match and taking the first same-stem file in path-sorted order from anywhere in the repository. Both sides at the top of the tree is the one further shape it accepts | The `whole` check read `tail === "" \|\| t.dir === tail \|\| t.dir.endsWith(...)`, and the first disjunct made every file sitting directly in the root being evaluated match unconditionally, in any directory, in any language. openproject built an oracle from the same mirrors rule this module already applies correctly to nested files and measured over its real 500 areas: 3,849 producers with an empty tail, 1,536 matched, 868 correct, **668 false, 43.5% of all matches, across 167 of 500 areas**; one engine, backlogs, holds the repository's only real `engine_spec.rb`, and 24 siblings were each credited through it for sharing the stem. decidim: 58 of 156 Ruby areas inflated, 134 files credited with no same-engine candidate, `decidim-elections/lib/decidim/elections` stating 6 of 8 for a gem with no `spec/lib` at all. fastlane: file after file credited across gem boundaries. chef: 488 root-level files matched this way, all 5 hand-verified wrong, one shadowing the genuinely on-point spec because the loop broke on the first alphabetical hit. puppet: a decoy planted at `spec/unit/totally_unrelated/network_spec.rb`, describing nothing to do with `Puppet::Network`, moved both counts by exactly one and moved them back on revert. plots2 credits a 17-line WebSocket consumer with a server-side Ruby spec, so the defect was language-agnostic as well as namespace-agnostic. huginn generated a self-contradiction from one run, the overview reading 0 of 18 with a non-empty tail while the area file read 2 of 6 over the same files | **done** `companions.mjs` (`namesakeCompanions`), pinned by `test/companions.test.mjs`. The nested path is untouched by construction, the same branch as before: openproject measured zero cross-engine leakage among its 2,623 nested producers. The flat-repository half was found by scanning one fixed tree with both builds and diffing the maps rather than by a test: mirroring the root's own directory has nothing to mirror with when that directory is nobody's tree word, and this repository's own overview went from a true `8 of 15` to `0 of 15`. Both sides at the top of the tree is the narrowest rule that keeps it: what the two share is the top and not the shape below it, so a test root that files by type (`test/unit`, `test/integration`, whose own word is nobody's tree name) answers too, and it does not reach a package, where `apps/www` answered by a repository-wide `test/`, or a top-level script answered by one package's own tests, is the shape a monorepo keeps beside the package instead. That branch asks one question the nested path never had to: the directories part first there and here they do not, so a JS script is not answered by a Ruby spec of the same stem. What it still does not close is a source root whose own name is a tree word, where `lib` and a `test/app` both strip to nothing and mirror each other: pre-existing, unchanged from before this row, and the accepted cost of the width the typed test root needs. With it the two maps are byte-identical |
| H19 | An area's `kinds:` line prints its own leftover past the two named extensions, the same `and N other` clause a root line prints | The omission read as a guard against comparing an area's leftover to a root line's, which is a real risk when the two sit near each other describing the same directory from two different populations. It is not the risk inside one area's own file: `rootFacts` is handed the area's own files, so the heading's count and this line's `other` are the same population counted twice, and hiding the leftover did not avoid a comparison, it broke the arithmetic the heading and the line owe each other. eslint's `packages/eslint-config-eslint` printed `5 .js, 4 .ts` under a heading of 10 files; canvas-lms confirmed it at scale, 107 of 497 areas hiding a nonzero leftover; storybook's worst case is an 8-file area, one file per extension by design, printing `1 .cjs, 1 .cts` and hiding 75% of itself; supabase's worst erases a whole file type, saying `8 .js, 7 .ts` while the 9 hidden files are every `.jsx` and `.tsx` in the area | **done** `render-layout.mjs` (`kindsLine`, `extText`, now shared with `rootLine`) |
| H20 | An area's `kinds:` line names a test group's runner the way a root line does, per group, rather than summing every group into one bare count labelled `test file` | The two lines already read the same `tests` field `testGroups` built, so summing it on one and naming it on the other stated one fact two ways. plots2's `test/unit/helpers` stated `11 test files` two lines above stating that all 11 inherit `ActionView::TestCase`, never once saying minitest, while the repository-wide overview two sections up correctly said `75 minitest specs` over the same files. No area file in that repository named a runner for any of its 8 test-holding directories. The per-group clause is shared with the root line's own rendering rather than duplicated, so the two spellings cannot drift again | **done** `render-layout.mjs` (`kindsLine`, `testsParts`, shared with `rootLine`) |
| H21 | `rootFacts` matches a root's producers against whichever of the two printed extensions is a language this tool parses, not always the first by count | The denominator has to be a number the line already printed, which is true of either shown extension and not only the larger: a marketing site's own screenshots outnumber its components, and matching only the most common extension read every producer there as zero rather than naming the real ones. Coupled to `apps` joining the shell names in H3: breaking supabase's `apps/www` (`.png` at 2,179 against `.tsx` at 753) and `apps/docs` (dominated by `.mdx`) out from behind the old blended bullet would, without this, have made their roster lines report zero producers instead of the truth, which is worse than the blend they replaced. Fixed together, so a newly visible site is honest rather than silent | **done** `layout.mjs` (`rootFacts`) |
| H22 | A file named by Component Story Format's own convention, `*.stories.<ext>`, is its own kind, present above zero as `record.stories`, and is excluded from the producer and sibling-module populations rather than sorted into either by its incidental syntax | A story is neither source to imitate nor a test: storybook's `.stories.tsx` carries literal JSX and joined the ordinary component population, while its `.stories.ts` carried none and joined `helperFacet`'s sibling modules, so the always-loaded overview's own top-three stem list read `2559 sibling modules named index/types/input.stories`, filing that repository's most common fixture name beside private helper functions. About a fifth of its component-shaped population, never disclosed as what it is. `isStoryFile` matches the filename alone, since the two syntactic flavours are one convention and the reader's question is the same for both. The count is absent rather than zero, the way `companions` and `helpers` already are, since most repositories hold none | **done** `layout.mjs` (`isStoryFile`, `rootFacts`, `helperFacet`), `render-layout.mjs` (`kindsLine`, `rootLine`, `storiesPart`) |
| H23 | The layout acceptance recount reads the same predicates it checks and keeps its own summation | `scripts/measure-layout.mjs` reimplements the roster's summation by hand on purpose, since calling `rootFacts` would compare that function to itself; its header states the split, that the classification predicates are imported so a test file is a test file by one definition, and that the recount owns the summation, the root subsets and the reading back of the printed line. H21 and H22 both landed inside `rootFacts` and `helperFacet`, so the recount's own copies of the producer filter and the sibling-module filter disagreed with the shipped renderer on any repository hitting either case, and the corpus gate would have reported a false drift on supabase or storybook | **done** `scripts/measure-layout.mjs` (`recountRoot`, and a stories clause consumed between the extension clause and the tests clauses because `rootLine` now prints one there). Verified end to end rather than by unit test: a scratch repository holding 8 components, 4 `.stories.tsx` and 2 `.stories.ts` renders `12 .tsx (JSX), 4 .ts; 6 story files; 2 sibling modules named index/types` and the recount agrees. The `.stories.ts` half is the one that matters, since those carry no JSX and would otherwise have joined the sibling modules and put a story stem in the printed list |
| H24 | A file whose area holds no slot for a dimension inherits the nearest enclosing area that states one, capped at FIX; a directive the 40-line budget dropped is capped the same way; and so is a claim the owning area's own globs never deliver to this path | A dimension that finds zero sites in an area produces no slot at all, so the first `Net::HTTP` call an area ever sees, the one that decides whether its HTTP goes through the repository's own client, could never be flagged at any severity. Covered-but-first-of-kind was blinder than uncovered, which at least gets the model-default NIT. The ancestor's sentence was measured over a population this directory sits inside, which is the smallest honest thing to fall back to, and the polarity and the learned class travel with the slot or an inherited finding is judged on the claim side while the ancestor's map handed the agent the inverse. Capped at FIX because the ancestor's area file is not delivered to the child's files: MUST-FIX means the map told this author and they are the first to break it, and here the map did not tell. The budget half is the same sentence from the other end: `check` reads `facts.json` rather than the rendered file, `fit` is greedy and drops from the end, and on a measured repository seven stated slots lost their line, three of them with a perfect baseline, so a branch reached MUST-FIX on a sentence that appears nowhere in the file the agent reads. The third half is the same sentence from a third end: ownership is the directory prefix and delivery is the glob, and A10 makes the glob the narrower of the two on purpose, so an area listing only its own files owns every new subdirectory under it and hands its sentences to none of them. On one measured repository 11 of its 156 areas carry no recursive glob rooted at their own path, and a file planted in a new subdirectory of one of them drew MUST-FIX on a claim its author was never handed; it reads FIX now, with the sentence the ancestor case already uses. It caps rather than decides, because replacing the verdict raised a slot the gates had suppressed from NIT to FIX on those same paths, under a reason claiming it was counted in an area whose file prints it as "no convention". It reaches all three producers, because a tree row and a filename row capped beside an obligation left at MUST-FIX is one file drawing two severities off the same undelivered area file. And delivery is read whole rather than by directory: a row's langs are wider than one area's extension brace, `file_naming_case` spanning js, jsx and ruby, so a `.jsx` file in a directory of `.js` read as delivered and drew MUST-FIX on a sentence that area's `paths` list cannot spell. Held against the reference matcher the corpus tests already carry rather than against itself. Recomputed from the same layout the writer uses rather than stored, because a second derivation of the same thing is a drift waiting for a field to move, and the claim text is not needed: a directive block's height is its counts line, its exceptions and its blank | **done** `check.mjs` (`ancestorsIndex`, `ancestorSlot`, `severityFor`, and `filenameFinding`, because a corpus row's slot is inherited on the same rule), `render.mjs` (`fitCount` and `areaBlocks` split out of `fit` and `renderArea`, and `droppedDirectives` over them, which reports a dropped counts line for a stated slot as well as a dropped directive: the model-default half is twice the size, 15 slots against 7) |
| H25 | A candidate the tail refuses is still credited when it imports the producer outright, and an import-only match votes for the directory the test sits in | H18 left a false negative it could not close from path shape alone: this repository's second plugin keeps its sources at `ultracode-anywhere/hooks` and every test flat under `test`, so the tail `hooks` mirrors nothing on the other side and five genuinely tested files read `0 of 5`. The shape is indistinguishable from the decoy H18 pins at `test/companions.test.mjs`, `apps/www/Page0.tsx` against `test/Page0.test.tsx`: same depth, same flat test root, same language, one-to-one stems. No rule over paths can separate them, so the evidence has to come from the file rather than from where it sits, and `import ... from "../ultracode-anywhere/hooks/counters.mjs"` is the producer named in the one place a guess cannot be wrong. Additive in what it counts, and only there: the tail is asked first and no match H18 measured is taken away, while the root a file votes for can move, which is the half that needed the most review. The root is a separate question and the edge never wins it against shared structure, because an edge proves a file is tested and says nothing about where a root keeps its tests. A covered candidate still votes whatever structure it shares; what it may not do is claim the vote when it shares none. Reviewed and reproduced before shipping: claiming the vote let a foreign integration test that sorted earlier take `packages/core` from `packages/core/spec` to `apps/admin/test`, a true count under a false place. A structural root now wins wherever any candidate names one, and the test's own directory is used only where none did. Reviewed again against those fixes, which found three more: the empty string `wholeRoot` answers for the repository itself is not a place, and counting it as a vote let it outrank one that is; a compiled specifier stripped to its stem answered every file sharing it; and `./x/.` named a directory the trailing-slash guard did not catch. A third round found one more, again from a fix: making the empty string falsy stopped it ending the walk, so a colocated test, which is what `wholeRoot` also reduces to nothing, let a second suite that merely imports the file name the place. Structure that answers and names nothing now says so, and the edge does not name a place in its stead. The same round found a specifier with no extension reaching every file that shared the stem, a stylesheet and a declaration file included, held off real maps only by the single-extension population one caller builds. Measured old build against new over 12 repositories, `puppet`, `huginn`, `plots2`, `decidim`, `fastlane`, `consul`, `rubocop`, `commander.js`, `lodash`, `express`, `got` and `chalk`, which include H18's own counter-examples: 455 namesake claim lines, byte-identical on 11 of the 12, and every H18 counter-example among them. `got` is the one that moves, and it moves from a false statement to a true one: `source/core` went 0 of 20 to 3 of 20, `source/core/utils` 0 of 12 to 2 of 12, and all three are hand-verified, `test/parse-link-header.ts`, `test/dns-cache.ts` and `test/weakable-map.ts` each importing the file they are named after through its compiled `.js` spelling. No producer anywhere in the 12 is credited falsely. On this repository it credits exactly 5, the five files the row was wrong about. The stem still has to match, so the sentence stays the one it always was; a repository whose specs never name what they cover, which is every Rails tree here, gains nothing and is told nothing new | **done** `companions.mjs` (`importedPath`, `coversOf`, the `covered` branch in `namesakeCompanions`), pinned by `test/companions.test.mjs`. The extension is kept when the specifier carries one and the producer is asked for both spellings, because stripping it here let `./defaults.json` answer `defaults.mjs` and credit a module with a test that only reads the table beside it; caught by a test written before the branch shipped. A specifier climbing above the root, and a bare package name, resolve to nothing rather than to a file that happens to share the name. A trailing slash is a directory and resolves to nothing too, or `../src/foo/` would answer the unrelated `src/foo.js` beside it. A loader suffix is cut, since `./worker.ts?worker` names the file before the question mark. The four compiled JavaScript extensions are also matched against the TypeScript file they are written for, because NodeNext requires `../src/parser.js` for `src/parser.ts` and refusing it missed the dominant modern spelling; no other extension is stripped, which is what keeps the `.json` table from answering for the module. The resolution is textual and never touches the filesystem, so a directory import through its own `index` is not followed and reads as no evidence, which is the conservative half of the trade. `siblings.mjs` resolves a specifier against the corpus and is deliberately not called: it needs the whole file set, and a caller with none would answer no rather than fail, which is how an overview and an area file come to disagree; it also resolves a bare specifier through a tail index, which here would let a dependency name a file sharing its tail. What `facets.mjs` records is what is read: a static ESM `import` and a top-level `require("...")` bound to a variable, whatever it is declared with, so a dynamic `import()` names nothing, and a type-only import counts the same as a value one. A specifier written for a compiled file names the TypeScript spellings it is emitted from and no others, so one `./foo.js` cannot answer `foo.json` or `foo.css` beside it |
| H26 | The namesake question is answered off the whole directory where the tail below the root leaves nothing, so one file reads the same whichever root above it is asked | A tail that is only tree words is the most ordinary layout there is: at `packages/foo` the tail of `packages/foo/src/parser.ts` is `src`, `withoutTree` empties it, and the guard against a bare basename then refused every candidate. The package read 0 of 2 where `packages` above it and `packages/foo/src` below it both read 2 of 2, which is the huginn `2 of 6` against `0 of 18` class the companion tests already pin: one file, three roots, three answers, and the middle one wrong. Falling back to the whole directory's tree-less form rather than to the basename keeps the discrimination that guard was there for. Not the whole path in every case, which was tried and measured and is why this row is narrow: comparing the full tree-less path everywhere collapsed chef's `lib/chef` against `spec/unit` and puppet's `lib/puppet` against the same, taking 428 of 797 and 590 of 1009 to zero, along with every one of fastlane's six gems. Neither spec tree repeats the package segment its source tree carries, so nothing below the root is left to match on. A spec tree is free to drop the package segment the source tree carries, and the tail below the root is what sees that | **done** `companions.mjs`, pinned by `test/companions.test.mjs` |
| H27 | One test file answers one source file, decided over the whole corpus before any root is counted | Asked per root, with only that root's sources in hand, the question cannot see that another directory already owns the answer. `app/services/stripe/reports/generate.rb` has no spec at all and was credited with `spec/services/shopify/reports/generate_spec.rb`, which describes `Shopify::Reports::Generate`, because the tail below the area root is `reports` on both sides. The kinds line then said 15 where the pairing claim, which mirrors the whole path, said 13, in one generated file about the same seventeen files. The closest structural match wins, counted in shared trailing segments, and the import edge breaks a tie between equals. Where the two disagree nothing is decided: both orders were tried and each has a counter-example the other gets right, the corpus holds neither shape, and leaving it unowned costs at worst the false positive this pass exists to remove rather than the false negative that would retire a real test. A tie no edge settles is unowned too, which is the posture C9 already takes. Two further rules the report asked for are refused and recorded rather than left silent. A companion outside the spec root is already impossible on the obligation side, where the path is built from the learned root, and on the roster side there is no spec root to be outside of: the namesake count is asked of whatever a repository keeps its tests in, which is the whole of H18, and holding it to a `spec/` tree would empty it on every repository that files them anywhere else. And the two readings stay two computations. They agree on every measured case now that the cause is fixed, and one of them counts a namesake by shape while the other counts an obligation against a learned root and suffix; folding them would make each answer the other's question. H23's argument against a second derivation is a real cost here and is paid knowingly, so the corpus harness recounts both. Unowned is also what every candidate is when no source list is handed in, so a caller with no corpus asks exactly the question it used to. Measured over the corpus, against every change in this train together: 167 of 232 namesake lines unchanged, 20 gains, 25 drops and 20 where only the denominator moved. The drops hand-checked are this class, an Ember adapter named `api-key.js` credited with a server model's `api-key.test.js` from another package. Decided within one language, the way the spelling learner already decides: keyed on the stem alone, a module that merely shares a name and sits deeper took a spec off the Ruby file it was structurally written for | **done** `companions.mjs` (`assignOwners`, `sharedTail`, the owner guard in `namesakeCompanions`), `layout.mjs` (`layoutIndexes` passes the corpus's sources), pinned by `test/companions.test.mjs` |
| H28 | A test directory's own second spelling for the name it covers is learned, on the same bar C22 sets | The roster half of the same defect: `namesakeStem` strips the suffix that says a file is a test and nothing else, so `address_model_spec.rb` answers `address_model` and never `address`, and the overview read `46 of 166 have a namesake test` for a repository that specs 95 of them. Learned per test directory, because that is the population the share is a share of and it is where a spelling is actually held to; the corpus as a whole is not a place anybody names a file in. Held to the same language as the file it would answer, or `index.js` is credited with `index_spec.rb`. It reaches further than the row that prompted it and the corpus says the reach is right: webpack's `lib/util` read 0 of 75 and reads 48 of 75 now, its files answered by `<name>.unittest.js`, and `lib/dependencies` and `lib` at this level move the same way. It also answers, from evidence rather than by listing, the question `NAMESAKE_SUFFIXES` left open about the hyphen form: a directory writing `member-test.js` teaches `-test` where enough of its files agree, which is what puts Ember's suites back on the line. A spelling begins at a separator and is applied only to a candidate of the language it was learned in, and a candidate reached under two names gets a record per name rather than one shared object, or whichever name was settled last overwrote the other's owner and the file whose own name the spec spells read untested | **done** `companions.mjs` (`learnStemExtras` and the re-keying in `namesakeIndex`), `test-shape.mjs` (`startsAtSeparator` and the measured bar, which the obligation reads too so the two cannot drift), pinned by `test/companions.test.mjs` |
| H29 | A file the parse read and found no statement in is not a test file, whatever it is named | empire-flippers/api commented one spec out top to bottom, header and all, and it still made the service beside it read as covered; the runner collects nothing from it. The empty program is the whole test and not the absence of a case, which was tried first and measured: reading "no test call at the top level" as "not a test" costs vscode 1,864 of its 2,366, because `test` there is nested inside `suite`, and 100 of this client's 102 Cypress specs. Over the corpus the empty-program rule moves 23 files across 14 repositories, at most 0.6% of any one, and every one hand-checked is a licence header, a comment pointing at another file, or a bare triple-slash reference. A file the parse never reached keeps its name's answer: unexamined is not empty, and that is the silence every other dimension keeps about an unread file. The other half of the report needed no code and is recorded so it is not reopened: `RSpec.context` and `RSpec.feature` were already spec entry points, in the call table `facets.mjs` has carried since 0.1.13, and the receiver test already accepted `RSpec`. The claim that they were unrecognised was read off a doc comment rather than off the behaviour. Nor is an empty file a producer: left in the source list a commented-out spec could take ownership of a real one. What this deliberately does not do is require an example, which is what the report asked for: of the corpus's 21,418 `_spec.rb` files that declare a group outside a whole-line comment, 1,064 hold no bare `it`, `specify`, `scenario` or `example`, and the ones read by hand are real suites pulling their examples in through `it_behaves_like`, so the rule as asked would drop about a twentieth of the corpus's specs | **done** `facets.mjs` (the `empty` facet on both parsers, absent unless true so it costs nothing over IPC for a file that holds anything), `layout.mjs` (`isTestFile`, and the producer and ownership populations both), pinned by `test/facets.test.mjs` and `test/layout.test.mjs` |
| H30 | The tests line says how many of a runner's files are under the directory it names, and the namesake clause names the directory its denominator was counted over | Two readings of one line, both false and both in the file that loads every turn. `majorityDir` names the directory four fifths of a runner's files sit in, deliberately, because a strict prefix collapses on the one file 28 of 35 repositories keep outside the tree; the renderer then printed the whole count beside that name, so empire-flippers/api read `1333 RSpec specs under spec` two lines under its own `- spec: 1332 RSpec specs`, and a reader could only take them for one claim. It now reads `1332 of 1333`, and only where the two differ. The second clause took its denominator from the largest source root and printed it bare, so `1046 of 1575 .rb files have a namesake test` read repository-wide when 1575 was `app/services` alone. Both halves are wording rather than arithmetic: the counts either side of them were already right, and the namesake figure moves from 1046 to 1045 only because H29 stops counting a commented-out spec. The third complaint against this line, that the folded-directory count reconciles with nothing, does not survive the arithmetic: the roster counts every tracked file, 8,986 of them, the seven named roots hold 5,426, and the difference is exactly 3,560 | **done** `layout.mjs` (`testsLine` carries `under`, and the record carries `dir` beside `path` so the clause names a directory rather than the `(files at this level)` label), `render-layout.mjs` (`namesakeClause` takes the population's root, and the tests line prints the pair), pinned by `test/render.test.mjs`; `scripts/measure-layout.mjs` recounts both clauses |
| H31 | Where only the import edge answers, a suite inside the root being counted beats one outside it | Neither candidate is structural, so both are true and only one is the answer a reader wanted: `e2e` beat `pkg/hooks/__spec__` on the letter `e`. H25 left this open on purpose, because ranking test directories against each other is a heuristic with its own failure modes, and asked for a measurement first. The measurement: five roster roots print a namesake root that came from the edge fallback at all, one of those has two or more directories reached that way, and none has one of them inside the root. The corpus holds no case, so the preference changes no line it prints; it settles a shape the corpus does not contain and is worth no more than that. Ranking on anything more than inside-or-outside is still unbuilt and still wants its own evidence | **done** `companions.mjs` (`inside`, read where the edge's directory is kept), pinned by `test/companions.test.mjs` |
| H32 | The fold line counts the folded directories, the files in directories under the floor, and the files at the repository root as three clauses, never as one number | One sentence carried all three populations and the noun in it named the first, so every file under no root at all was charged to the directories that folded. Measured on three repositories: this one printed `and 1 more directory holding 21 files` over a folded root holding 3, so 86% of the number named a directory that does not hold it; a Rails map printed 3,564 over 3,400, charging 164 files to `public` and `app/views`: 147 in 26 directories, `config` at 39 of them, `config/initializers` at 33 and `lib/tasks/archived` at 27, and 17 at the repository root; a front end printed 434 over 323. Nothing is enforced from the line and the totals still reconcile, so only the attribution was ever wrong. The repository root is its own clause rather than one of the directories under the floor: `visit` guards the floor test with `!isRoot`, so the root never took that test and failing it is not why it has no line, and it is not a place either, which is the answer `majorityDir` already gives. It holds 17 of that map's 164 and 14 of this repository's 18. `foldLine` already carried a second wording for the remainder and a `roots > 0` guard made it unreachable whenever anything folded, which is the shape all three repositories have | **done** `layout.mjs` (`layoutRoots`), `render-layout.mjs` (`foldLine`, and the reservation guard that decided whether the line got a line at all, which read the summed count), `layout-scan.mjs`, `facts.mjs` (schema 18), `measure-layout.mjs` (`foldCounts`, whose two anchored regexes could not read a three-clause line and would have failed the corpus recount on every repository, eighteen minutes in, with nothing in the suite covering it) |
| H33 | A runner group on an area's kinds line and on a root line says how many of itself sit under the directory the vote named, where that is fewer than the group | H30 fixed this on the tests line and left the sibling clause alone. The vote needs only a strict majority, so the name can speak for as few as half the group plus one: measured across two maps, five of the ten clauses carrying a voted directory name it a count it does not hold, `11 RSpec specs under oauth` over 6, `8 RSpec specs under admin` over 5, `8 ... under users` over 5, `9 ... under oauth` over 7, and `3 vitest specs under __tests__` over 2. The same generated file prints H30's honest pair over a 1-in-1369 gap two lines away, so a reader who has learned `N of M ... under X` as the spelling for a difference reads a bare count as the whole group. The name is a basename by design and stays one, so `admin` still names no reachable path where the directory is `spec/services/serializers/api/v1/admin`; that is H25's question and not this one. The count is of files whose immediate parent carries the winning name, not of a subtree, which matters where one basename is several directories | **done** `layout.mjs` (`sharedSub`, `testGroups`), `render-layout.mjs` (`testsParts`), `measure-layout.mjs`, whose recount keeps its own copy of the vote and would have failed every affected repository. No schema move: the reader already tolerates the field's absence on the sibling line, and a record without it reads as the whole group being under the name, which is what it printed |
| H34 | The empty-tail mirror is asked in both directions, held to a candidate whose top segment is a tree the repository keeps tests in | `mirrors` is one-directional and the empty-tail arm only ever asked whether the candidate ends in the root, so a root whose tree-less form is longer than the test tree's never matched. `app/mcp` is a Rails autoload root, `withoutTree` leaves `mcp/mcp` against `mcp`, and every file sitting directly in `app/mcp/mcp` read untested while the same files read tested one segment up: `0 of 13`, `0 of 11`, `0 of 8`, `4 of 22` and `3 of 11` against true figures of 1, 3, 2, 16 and 6, and a roster line of 54 of 115 against 57. That breaks the invariant two pins already hold, that a file answers the same whichever root above it is evaluated, on a trigger neither covers. H18 named one accepted hole, a source root whose own name is a tree word, and did not name this one. The reversed direction is where a false positive would come from, so it is guarded twice: `tail === ""`, which is load-bearing rather than tidy since `rootBare` is null on the nested path and the reversed call puts it in the receiver position, and the candidate's own top segment being in `TEST_TREES`. Measured with `measure-layout.mjs` over the whole corpus, which is the oracle H18's own throwaway probe left behind, at 6,849 areas of which 4,886 carry a companion count: 24 areas gain and none drops on this row's own account, every gain hand-verified, and every roster namesake line byte-identical outside the two repositories the defect was found on. H37 is the guard that measurement forced and accounts for the six that drop. Knowingly accepted and unmeasurable from the corpus, which holds no case: within one engine, where `assignOwners` cannot decide, `app/services/models` can still be credited from `spec/models`. Two ways it cannot: the tails tie, or it never looks, since it buckets by `language` where the loop now filters by engine, so a `.ts` test against `.tsx` sources finds no bucket at all | **done** `companions.mjs`, pinned by `test/companions.test.mjs`, where four of the new cases pin the guards rather than the feature |
| H35 | A Ruby file named `_spec.rb` or `_test.rb` is a test only where a test tree also says so. The dotted and hyphen forms still answer on the name alone | The third narrowing of the name rule, beside H7 and H29. The Ruby form is the one a non-test file wears in earnest, and a file outside every test tree has an address available to corroborate it. Measured across the 35-repository corpus and one Rails map, eight files move in five repositories and seven of them are plainly not tests: a RuboCop cop named for the `Rails.env.test?` guard it enforces, which printed a test group of its own in the file that loads every turn and told a reader to write the next cop with a `<cop>_test.rb` beside it; four Homebrew classes, `SoftwareSpec` and three `T::Struct`s, each of which has its own `*_spec_spec.rb` under `test/`; a one-line Cucumber support file; and a CI driver script that invokes the suite rather than being one. The eighth, a hand-run integration script with its own assertions, is a real false negative and is accepted at one file in a 694-spec repository. The largest candidate population in the corpus, 40 files under `gems/plugins/*/spec_canvas`, moves nothing, because every one of them answers on what the file itself says before its name is read. What this still does not do: it does not require an example, which H29 refuses on measurement, and the dotted and hyphen forms answer on the name alone because discourse writes 4,000 tests as `login-test.js` and nothing else in them says so | **done** `layout.mjs` (`isTestFile`), pinned by `test/layout.test.mjs`. Nothing pinned the old behaviour, so the suite was green either way before the pins were written |
| H36 | The scan summary spells a runner and its unit through the same pair the map's own lines print | The one surface the caller is watching read `1 test files under lib/rubocop/custom_cops` beside `1 test file` in the file it summarises, and `1369 rspec` where the map said `1368 of 1369 RSpec specs`. Two faults in one expression, a noun that never pluralised and a raw registry key where every other surface prints the reader's word for it, in the module that already exports the pair for exactly this reason and whose own docstring records a thirty-five repository corpus turning up `1 files hold syntax the parser rejected` on seven of them | **done** `render-layout.mjs` (`layoutSummary`) |
| H37 | A test file is evidence about a source file only where the two run on one engine, asked of the candidate rather than of the branch that matched it | The rule was written on one branch, the bare-basename pair at the top of two trees, and every other path to a match went without it. mastodon keeps a `spec/models` of Ruby specs beside a `spec/javascript` of TypeScript tests and holds a model of each name in both languages, so the mirror credited each with the other's files: `app/javascript/mastodon/models` read 8 of its 17 off the Ruby specs, and `app/models` read the TypeScript tests. Measured over the whole corpus, six areas were carrying such a credit before this row and none is left: discourse charged four directories of `.gjs` components to Ruby RSpec system specs, one of them naming `plugins/chat/spec/system` as the place its JavaScript is tested, and decidim charged two directories of `app/packs` JavaScript the same way. A tail that mirrors is evidence about a shape, and a shape two languages share is a repository that keeps both there rather than a test. The engine and not the language, which is finer than the question: `language` separates `.tsx` from `.ts` because that is the grammar the parser is asked for, and a component tested by a plain `.ts` file is the ordinary shape in every React repository there is. Measured either way over the corpus, no area moves between them, so this costs nothing today and states the rule that was meant. The bare-basename branch keeps asking `language`, because it is the one match with no structure behind it at all | **done** `companions.mjs`, pinned by `test/companions.test.mjs`. Found by the corpus rather than by a test: the unit suite was green on both directions of it, and the measurement that found it is the same one H34 records |
| H38 | A file the change added is asked whether it belongs where it was put, not only whether its contents match. A test added where the source root it covers has producers and no namesake test of its own is a FIX | Every other rule asks whether a file's contents match its directory's claims, so a file that creates its own directory is the only member of it and conforms with itself, every time. The one case guaranteed to pass is the one where a convention was most likely broken: `spec/mailers/cim_share_mailer_spec.rb` in a repository whose four mailers have no specs was examined and produced nothing. The counts to answer it were already taken, per source root, as `companions: {with, of}`. The directory the file is going into is asked first, and it is the nearest evidence there is: a test landing beside tests is following them, whatever a root's ratio says a level or two up. That is the half of issue #120's own sentence this rule was missing, "a test file in a directory holding no other test file, in a repository whose sibling ratio for that kind is 0 of N", and without it a third spec joining two in one directory was told there was no precedent for it. The caller answers it, because the two that ask differ twice over. On what "already" means: nothing the notice can see came from the write it is about, while a check has to exclude everything the same change brought or three of four specs are excused by the first one landing. And on where to look: the check reads what git tracks, which is the population every other reader here counts, because one ignored `scratch_spec.rb` sitting in a directory read as a test habit and took four findings to none; the notice reads the directory instead, since a `git ls-files` before every write is a subprocess before every write, so it is the quieter of the two where a stray file sits there and the check catches what it let past. Neither may read silence as an answer: a directory that could not be listed and an index that could not be read both say there is a test rather than that there is none, since what decides whether the finding prints is a fact one of them failed to read, and a `spec/mailers` at mode 111 was reported as holding no other test with a sibling spec in it the whole time (C33). A caller that cannot answer says nothing, which leaves the rule where it was; the degraded run with no merge base is the one such caller, and it is capped at NIT there anyway. The finding names that directory, which issue #120 called the strongest possible signal and this did not print. It says the directory holds no other test rather than that the change created it, which is the operative fact and is true either way; whether a directory is new is a second reading of the base that only `check` could make and the hook could not. Four more guards keep it honest and each was written for a case that broke it. `with` at zero means no namesake was matched, never that the directory is untested, so the repository has to be seen pairing tests with sources somewhere before this can say it does not here. The source root needs three producers, since one or two untested files is a directory that has not spoken and reading its silence as a rule turns whichever file was written first into the convention. The root's own test files are counted against the same three from the other side: a directory holding four hundred specs that pair with nothing has a habit. And a tail more than one root answers to is answered by none of them where any one is already paired, because longest is not nearest: a repository with a specced `app/mailers` beside an engine's own untested `app/mailers` told a spec sitting with its four siblings that it had no precedent, off the longer name, which is a directory it has nothing to do with. Where every root at a tail is untested the verdict is the same whichever it is, so the one with the most producers speaks. A root recorded for one level is skipped outright, since its record counts nothing its children hold: react's `packages/react-reconciler/src` reads 0 of 81 with 78 tests in the `__tests__` directly beneath it, and every one of the 78 was told the directory holding it had no precedent. A test is held to a source extension as well as to its name, because the name alone admits `Component.test.tsx.snap`, `seed.test.sql` and `tsconfig.test.json`, which were 42 of react's 461 matches. Measured over all 35 corpus repositories, every existing test file was asked whether it had precedent where it already sits, on the reasoning that a mature repository put them where it meant to: 41,899 paths matched the name before the extension guard and 40,855 after, and the guards above took the answer from 81 findings to 2, both of them in the repository the rule was written for. What no guard reaches is knowingly accepted and named here. The namesake match is case sensitive, so a repository whose tests live in their own tree can be fully covered and still read as zero, five Cypress specs named `thing0.cy.js` beside `Thing0.tsx` being the measured shape, and the sentence therefore states what was counted, `Nothing here was matched to a test by name`, rather than a conclusion about the directory that the count cannot carry; closing that properly means a looser companion match, which moves every count in the map and belongs to its own row. The tail shortens from the right only, so a mirrored tree carrying an extra segment, `test/unit/app/models`, never reaches `app/models`. Measured over the same 40,855 files: 26,004 of them, 63.7%, reach a root and are answered, and 14,765, 36.1%, reach none and are passed over in silence, which is the direction to be wrong in. That share is not spread evenly and the aggregate hides it: a repository whose tests mirror its sources one level down is answered almost entirely, Homebrew at 100%, appsmith at 99.8%, storybook at 97.7%, rubocop at 92.5%, while one whose tests sit under a tree of their own is barely reached at all, whitehall at 0.2%, puppet at 0.7%, typeorm at 1.6%, next.js at 4.9%. Shortening from the left as well, so that every run of segments in the tail is tried rather than every prefix, was built and measured against the same 40,855 files rather than argued about: it takes the answer from 2 findings to 214, all of them on tests that already sit where their repository put them. So it is not done, and the half of the corpus this rule is quiet on stays quiet. And a source directory under the layout's own floor is not in the layout at all, so the `app/mailers` of three files that prompted this is invisible in its own repository: the shape is caught, that instance is not, and the floor is a layout-wide decision rather than this rule's to move. The covered root is found by shortening the test's tail a segment at a time, because a root is a folded directory and a test sits under whichever leaf it is about: `spec/services/nda_agreements` is about `app/services`, and `src/pages/Listing/__tests__` is about `src/pages`, which is the directory the miss this was written for was in and which an exact tail match never reached. The ratio alone is what got walked through, so the tests a root does hold are named in the same clause as the zero, in the words the roster already uses for them: `0 of 1003 have a namesake test` never spoke about `__tests__/helper.test.ts`, because that is not a namesake, and so forbade nothing. A relocation counts as an arrival, since moving a test into a directory whose siblings have none is the same deviation as writing it there. It is read off the path the base version comes from rather than off the status letter, which spells one move two ways: `R` from the diff, and `M` carrying an `orig` from a working tree where the move is staged and not yet committed, which is the state a run before the commit is asked in. FIX and never higher, and where the siblings put their tests has more than one defensible answer. It drops to NIT only where no comparison against a base could be made, since that is what says a file arrived; a map with no pin caps at FIX like every other rule and does not stop the diff saying `A`, and reading the two as one printed a NIT saying the run could not establish what the change added, on a run that had. No `--created-only` flag: the finding already carries its own severity and dimension, and a sweep before handover that hides every other finding is the wrong shape for a sweep before handover. Three narrowings, each a departure from what the issues asked and each recorded here rather than left to be discovered. Only test files are asked: issue #120's rule says "has precedent for that kind of file" and its worked example is a test, and a rule over every kind needs a vocabulary of kinds this tool does not learn, so a first `app/policies` in a directory a change invents is silent. The tests a root does hold are named by their runner and their tree word, `2 vitest specs under __tests__`, rather than by what they cover, which #119's `both for a utils/ helper module` would need a reader of the tests themselves. And the whole rule turns off in a repository that pairs no namesake anywhere, so issue #119's own case, rebuilt in isolation with no other paired root, produces nothing; it fires on the repository it was found in because that one pairs elsewhere. That is the first guard doing its job rather than a hole, and it means the rule answers about a repository's habit rather than about a directory in the abstract. Issues #119 and #120 | **done** `plugins/anatomiya/lib/precedent.mjs`, `plugins/anatomiya/lib/check.mjs`, pinned by `test/precedent.test.mjs` and by two cases in `test/check.test.mjs` that scan and pin a real tree, because the hand-built facts fixture there writes no layout and so proves nothing about the wiring |
| H39 | The map states, in a sentence, that a directory with no test precedent outranks an instruction to always write a test | The map states measurements and a testing skill states an Iron Law. Read side by side the law wins, because one is phrased as data and the other as an instruction, and the session that prompted this was under both and resolved the conflict silently in favour of the skill. A principle rather than a second finding: it is about how to read the counts, not about a file. It appears only where such a directory exists to be misled about, and under the same first conjunct H38 uses, so a repository that pairs no tests anywhere is never told it has departed from something it never had. Separate from H38 because either could ship without the other: one answers after the branch is written and one is read before it is | **done** `plugins/anatomiya/lib/principles.mjs` (`test_precedent`), pinned by `test/principles.test.mjs` and by the boundary case in `test/precedent.test.mjs` that holds its floor and the finding's to the same answer |
| H40 | A hook answers from the repository the tool call is about, and from the working directory only where the call names no place | Both hooks resolved by walking up from the process working directory, which says where a shell happens to be rather than what the call is about. The walk stops at the first map it finds, so it is right going up and blind sideways: two checkouts under one parent served each other's roster and each other's directives, under the line saying it was counted from this repository's own code. The boundary that would have stopped it sits under the other checkout, which the walk never visits. Measured on 2.1.251 off captured payloads rather than off the tool definitions: five tools name a place, `Read`, `Write` and `Edit` under `file_path`, `NotebookEdit` under `notebook_path`, `Glob` and `Grep` under `path`. One walk serves all three shapes rather than a branch per tool, since a file answers with its parent, a directory with itself, and a path a write is inventing with the nearest ancestor that exists. Taking the parent of a directory would have been the same bug once more: a `Glob` at a repository root would answer from the directory holding every checkout. `Bash`, `Agent`, `WebFetch` and `ToolSearch` name nothing, and for `Bash` no field anywhere locates a `cat > file`, so the working directory stays the answer there and for a shell it is the right base anyway. That directory is read off the payload rather than off this process: it follows the agent, and one `cd` moved it for every payload after with nothing telling the hook. A relative path is read against that same base, because the tool read it against that one and nothing normalises `tool_input` on the way here; `targetIn` reads it against the base `aboutDir` used, or the two disagree about one payload and the hook says nothing about a write it has already located. Two outcomes change beyond the defect, and both are named here rather than left to be found. The issue called it right that a parent session gets nothing, and it no longer does: a real payload always carries a `cwd`, so the only silence left is a payload holding neither a path nor one of those. For a shell that is an improvement, since its own directory is the correct base. For an `Agent` call it means a subagent's first map is picked by wherever the parent's shell last was, which is this defect's own shape one level up and is why the payload's field is preferred to the process's. And a call naming a file in no checkout at all leaves the session's map standing rather than blanking it: reading a system file or another project's is ordinary, and answering nothing there takes the map off a turn that had one. The test is whether the call has a checkout, not whether one answered. A first draft fell back whenever no map came back, which reads three different answers as one and gave a nested repository with no map, and a mapped sibling whose map is empty, the enclosing checkout's counts: the exact thing the boundary walk exists to refuse, re-created by the fix that was meant to stop it. A repository that has nothing to say says nothing, and only a path belonging to no repository is answered from somewhere else. The tradeoff the issue asked to be weighed, that a session can be handed two maps in two turns, is now more frequent and not less: before, only a `cd` moved it, and now every read across a repository boundary does. That is the point rather than the price, since the map that arrives is about the file that arrived with it, and the alternative measured worse: one map per session is the wrong map for every call outside it. The walk is refused above 4096 bytes, which no filesystem can hold: it costs a stat and a copy per segment, and a payload inside the megabyte this reads held 400,000 of them and took 7.8 seconds against the 5 the declaration asks for, losing the turn its map and burning the budget before every tool call. The bound sits on the path the walk runs on rather than on the field it came from, since a first draft gated the target alone and a short target with a long `cwd` in the same payload went round it at 10.5 seconds. `targetIn` takes the same bound one segment deeper, where `resolveLinks` recurses per segment and the same payload overflowed the stack rather than costing seconds. Issue #123 | **done** `plugins/anatomiya/lib/hook.mjs` (`aboutDir`, `ABOUT_KEY`, `deepestDir`, `targetIn`), `plugins/anatomiya/lib/commands.mjs` (`runEcho`, `runNotice`), pinned by `test/commands.test.mjs`, whose fixture is two checkouts under one parent that is not itself a repository. Sourced in `docs/research/what-a-hook-payload-carries.md` |

## What is deliberately not built

- **No hook as the delivery channel.** Worst-trusted, highest complexity, measured being flagged as
  prompt injection, and measured at 10 to 40% adherence against 100% for the always-loaded file. A24
  adds a hook *beside* that channel rather than instead of it, which is a different question: the
  100% channel keeps carrying the map and the hook adds recency and a timestamp on top. If the hook
  were ever the only delivery, this refusal stands.
- **No MCP server.** Nothing to expose.
- **No skill.** It would spend resident context describing the tool instead of the repository.
- **No health score or grade.** It changes nothing about the next line of code.
- **No import-direction (layering) dimension.** Probed on four repositories (2026-08-16): a
  require() repository is invisible to an import-statement site, the cross-area graph is sparse
  everywhere but vscode, and at this tool's own Wilson gate 0 of 4 repositories put even a third
  of their pairs over the bar. `docs/measurements/2026-08-16-layering-probe.md` has the numbers
  and the revisit conditions.
- **No restating what the linter already enforces.**
- **No "catches bugs earlier" claim.** Measured: 1 of 317 defect comments across ten repositories was preventable by a conventions map. Nine of ten repositories scored zero.
