{
  "name": "crisnahine",
  "description": "Counts what your code already does, directory by directory, and puts those counts where the agent picks them up when it reads a file there",
  "owner": {
    "name": "crisnahine",
    "url": "https://github.com/crisnahine"
  },
  "plugins": [
    {
      "name": "anatomiya",
      "source": "./plugins/anatomiya",
      "description": "Counts what your code already does, directory by directory, and puts those counts where the agent picks them up when it reads a file there"
    },
    {
      "name": "ultracode-anywhere",
      "source": "./plugins/ultracode-anywhere",
      "description": "Ships three tested orchestrations you run by name, the agent types they spawn, and the reminder that keeps the Workflow tool in play at any effort level"
    }
  ]
}
